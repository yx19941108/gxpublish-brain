Owner: Codex
Task: manuscript_review R8 第一轮运行态回归
Status: In Progress
Updated: 2026-03-30 16:45:00 +08:00

# R8.4.5_第一轮运行态回归记录

## 1. 适用范围

本文记录 `gxpublish-brain / manuscript_review / R8 第一轮` 在 2026-03-30 运行态续工中的真实浏览器、接口、数据证据。

本文不替代：

1. `R8.0_问题排查续工交接.md`
2. `R8.2_问题池统一修复方案.md`
3. `R8.3_问题池分轮修复执行基线.md`
4. `R8.2.5_问题池统一回归测试用例.md`
5. `R8.4_第一轮修复执行提示词.md`
6. `R8.4.3_第一轮修复静态验证记录.md`
7. `R8.4.4_第一轮运行态回归续工提示词.md`

## 2. 本轮边界

本轮仍只验证第一轮问题与问题 2 补证：

1. 问题 9：新增权限未收紧
2. 问题 3：审校台账分页与数据源口径不一致
3. 问题 7：history 主文案与 actor
4. 问题 2：只做 `TC-04` 基准样本补证；`TC-03` 仍依赖失败样本

未进入：

1. 问题 1 / 4 / 5 / 6 / 8 的代码修复
2. 任何仓库代码修改

## 3. 运行态 ready 证据

### 3.1 进程与端口

1. `9999` 由 `node.exe` 监听
2. `9888` 由 `C:\kuguaHome\env\java\jdk17\bin\java.exe` 监听

### 3.2 最小 HTTP

1. `GET http://127.0.0.1:9999/ -> 200`
2. `GET http://127.0.0.1:9888/ -> 200`

## 4. 已执行用例与证据

## 4.1 问题 9：权限收口

### TC-26 审批人角色没有新增入口

结论：通过。

浏览器证据：

1. `mr_approver_l1 / 666666 / 1234` 登录成功
2. 直接访问 `/manuscript/review/form?mode=create` 后被重定向回 `/manuscript/review`
3. 在 `/manuscript/review` 与 `/manuscript/review/task-waiting` 页面均未发现“新增”入口文本

接口证据：

1. 审批人 token 直调 `POST /workflow/manuscript-review/submitAndFlowStart`
2. 返回 `code=403, msg=没有访问权限，请联系管理员授权`

数据证据：

1. 当前运行库 `sys_role_menu` 仍存在审批人角色绑定 `60007 审校表单`
2. 说明第一轮权限 SQL 尚未 apply，但当前前端守卫与后端鉴权已足以阻止越权新增

### TC-27 发起人角色保留新增入口

结论：通过。

浏览器证据：

1. `mr_initiator / 666666 / 1234` 登录成功
2. 直接访问 `/manuscript/review/form?mode=create` 成功进入新增页
3. 页面存在基础信息区、上传暂存区、外链录入区和“提交”按钮

### TC-28 直接调提交接口会被后端权限拦截

结论：通过。

接口证据：

1. 审批人 token 调 `POST /workflow/manuscript-review/submitAndFlowStart`
2. HTTP 200 包体返回 `code=403`
3. 说明 controller 权限拦截生效

### TC-29 `mode=create` 路由守卫有效

结论：通过。

浏览器证据：

1. 审批人直开 `/manuscript/review/form?mode=create`
2. 页面未停留在表单，而是回到 `/manuscript/review`

### TC-30 create / edit / resubmit / goApprove 权限边界

结论：部分通过，已拿到 create / goApprove / resubmit 证据。

已确认：

1. create：
   - `mr_initiator` 可进入新增页
   - `mr_approver_l1` 不可进入新增页
2. goApprove：
   - `mr_approver_l1` 的 `/manuscript/review/task-waiting` 页面存在“办理”按钮
3. resubmit：
   - 先由 `mr_approver_l1` 对 `reviewId=2038420521325998081` 执行退回
   - 再由 `mr_initiator` 查看详情，`permissionMatrix.canResubmit = true`
   - 使用正确载荷 `POST /workflow/manuscript-review/resubmit` body `{ "id": "2038420521325998081" }`
   - 返回 `code=200`
   - 主表回到 `LEVEL_1 / 待一级审批 / 审批中`
   - history 新增 `RESUBMIT`

说明：

1. 本轮未再额外补“edit”按钮级浏览器截图
2. 但 returned 样本详情中 `permissionMatrix.canEdit = true` 已出现

## 4.2 问题 3：台账口径与并集

### TC-05 台账改为数据库分页

结论：通过。

证据链：

1. `R8.4.3` 静态验证已确认 `ManuscriptReviewReadableFirstRoundGuardrailTest` 通过
2. 运行态 `GET /workflow/manuscript-review/list?pageNum=1&pageSize=20/30` 返回正常分页结构
3. 本轮未再出现“先查全表再 Java 内存分页”的 fresh 反证

### TC-06 发起人角色只看到自己发起/历史参与范围

结论：通过。

数据证据：

1. `mr_initiator(user_id=64000)` 对应 SQL 结果仅命中：
   - `initiator_user_id = 64000`
   - 或 `history.actor_user_id = 64000`
2. 未命中 `initiator_user_id = 64001 / 64005` 的他人样本

接口证据：

1. `mr_initiator` 调台账接口返回 `total=14`
2. 结果中不包含发起人为 `审校一级审批人` 或 `mr_certified_initiator` 的记录

### TC-07 审批人角色按当前审批状态可见

结论：通过。

证据：

1. `mr_approver_l1` 台账接口返回中存在 `LEVEL_1` 样本
2. 示例：
   - `SH20260330003 -> LEVEL_1`

### TC-08 多审批角色用户看到审批状态并集

结论：通过。

测试账号：

1. `multi_role_user_1 / 666666`
2. 角色：`manuscript_review_certified_initiator + manuscript_review_level_1_approver + manuscript_review_level_3_approver`

接口证据：

1. `GET /workflow/manuscript-review/list?pageNum=1&pageSize=30`
2. 返回中同时存在：
   - `LEVEL_1` 样本：`SH20260330003`
   - `LEVEL_3` 样本：`SH20260327002`

### TC-09 历史参与人兜底可见

结论：通过。

fresh 样本：

1. `reviewId=2038432954895609858`
2. 初始状态：`LEVEL_1`
3. `mr_approver_l1` 对其执行 `completeTask`
4. 样本进入 `LEVEL_2`

数据证据：

1. 主表：
   - `current_node_status = LEVEL_2`
2. history：
   - 新增 `APPROVE`
   - `actor_user_id = 64001`

接口证据：

1. `mr_approver_l1` 的一级待办接口不再包含该样本
2. 但审校台账接口仍包含该样本
3. SQL 复核显示该条样本对 `mr_approver_l1` 的命中来源为 `history.actor_user_id`

### TC-10 三类来源并集且去重

结论：通过。

证据：

1. `mr_approver_l1` 的审校台账中同时覆盖：
   - 当前一级审批来源
   - 本人发起来源
   - 历史参与人来源
2. `reviewId=2038432954895609858` 在转入 `LEVEL_2` 后仍只出现一次

## 4.3 问题 7：history actor 与主文案

### TC-18 每条新 history 都有 actor

结论：通过。

fresh 证据：

1. `APPROVE`：
   - `reviewId=2038432954895609858`
   - `actor_user_id=64001`
   - `actor_name=审校一级审批人`
2. `BACK`：
   - `reviewId=2038420521325998081`
   - `actor_user_id=64001`
   - `actor_name=审校一级审批人`
3. `RESUBMIT`：
   - `reviewId=2038420521325998081`
   - `actor_user_id=64000`
   - `actor_name=审校发起人`

### TC-19 退回主文案完整表达

结论：通过。

数据证据：

1. `BACK` 文案为：
   - `审校一级审批人在审校申请退回了流程。审批意见：R8.4.4 运行态回归退回发起人处理。`
2. 不再是“流程已退回发起人处理。”裸文案

### TC-20 审批通过 / 驳回 / 修改 / 再次提交主文案完整

结论：本轮已覆盖 `APPROVE / UPDATE / RESUBMIT`，`REJECT` 未在本轮 fresh 样本执行。

已确认：

1. `APPROVE`
   - `审校一级审批人在一级审批通过了流程。审批意见：R8.4.4 运行态回归一级审批通过。`
2. `UPDATE`
   - `审校一级审批人更新了审校流程单：说明由“啊手动阀”改为“啊手动阀阿斯顿发生”。`
3. `RESUBMIT`
   - `审校发起人再次提交了审校流程单。`

### TC-21 页面时间线展示不再靠第二行姓名补义

结论：通过。

接口/浏览器证据：

1. 详情接口 `timelineItems.eventText` 已可独立表达主语义
2. 页面详情时间线中，主句本身已经完整；`operatorName` 仅作补充显示

## 4.4 问题 2：补证队列

### TC-04 基准样本最小复核

结论：通过，仍未在基准样本复现问题 2。

样本：

1. `reviewId=2038409700130729985`

浏览器证据：

1. 打开详情页可见视频 `meeting_01.mp4`
2. 点击“预览”后打开新标签页
3. 页面存在 `<video>` 元素
4. `readyState = 4`
5. 播放 1.5 秒后：
   - `paused = false`
   - `currentTime = 1.460042`

接口/网络证据：

1. 视频资源请求命中：
   - `200 OK`
   - `206 Partial Content`

### TC-03 失败样本三元证据补取

结论：未执行。

原因：

1. 当前仍未拿到游工失败视频样本的 `reviewId`
2. 因此无法补：
   - 失败样本 `videoList.resourceUrl`
   - 对应 `sys_oss.service / file_name / url`

## 5. 风险与未完成项

1. `20260330_manuscript_review_first_round_permission_hardening_v01.sql` 当前运行库仍未 apply：
   - `sys_role_menu` 中审批人角色仍绑定 `60007 审校表单`
   - 但当前前端守卫与后端鉴权已阻断越权新增
2. `TC-03` 仍缺失败样本，不能把问题 2 宣称为“已修复”
3. `TC-20` 本轮未 fresh 执行 `REJECT`

## 6. 第一轮 gate 判断

### 6.1 已达到的 gate 条件

1. 审批人角色无法从前端或直接接口越权发起新增
2. 台账运行态已体现“发起人来源 / 审批人来源 / 历史参与人来源”并集
3. fresh history 已验证 `APPROVE / BACK / RESUBMIT` 均带 actor
4. 时间线主文案已能独立表达“谁在什么环节做了什么”

### 6.2 仍需显式保留的备注

1. 问题 2 的 `TC-03` 仍是样本缺口，不属于第一轮 gate 本体
2. 运行库里的第一轮权限 SQL 尚未 apply，属于环境与脚本落地差异，不是本轮代码运行态回退

### 6.3 当前判断

结论：第一轮核心 gate 已具备通过条件。

附带前提：

1. 需在汇报中明确区分：
   - 第一轮核心 gate
   - 问题 2 的失败样本补证
   - 权限 SQL 尚未 apply 的环境差异
