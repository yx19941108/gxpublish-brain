Owner: Codex
Task: manuscript_review WP6-G1-M1 执行侧续工提示词
Status: Active
Updated: 2026-03-25 09:25 +08:00

# R6.18_WP6-G1-M1执行侧续工提示词

以下内容可直接作为新执行侧会话的首条提示词使用。

```text
你现在不是主控审批人，你是“WP6-G1-M1 执行者”。

先读以下文件，且只以它们作为当前执行入口与裁决基线：
1. doc/manuscript_review/R6.18_WP6-G1-M1执行侧续工提示词.md
2. doc/manuscript_review/R6.17_WP6-G1主控审批结论.md
3. doc/manuscript_review/R6.16_WP6-G1执行侧阻塞记录.md
4. doc/manuscript_review/R6.15_WP6-G1执行侧续工提示词.md
5. doc/manuscript_review/R6.14_WP6-G1 Gate复核准备.md
6. doc/manuscript_review/R6.13_WP6-B3主控复审结论.md
7. doc/manuscript_review/R6.8_WP6-B3更新留痕执行基线.md
8. doc/manuscript_review/R6.5_WP6-shared接缝主控审计结论.md
9. doc/manuscript_review/R6.6_WP6-B2完成记录.md
10. doc/manuscript_review/R5.7_第6-7轮执行基线.md
11. doc/manuscript_review/R5_测试方案及结论.md
12. doc/manuscript_review/R5.5_冻结契约包.md
13. doc/manuscript_review/product/20260321_manuscript-review_product-requirements_v01.md

另外可参考文件：
- doc/manuscript_review 目录下其他 manuscript_review 文档
- Brain/script/sql/update/20260321_manuscript_review_ddl_v01.sql
- Brain/script/sql/update/20260321_manuscript_review_dml_v01.sql
- Brain/script/sql/update/20260323_manuscript_review_dev_login_seed_v01-废弃 .sql
- Brain/script/sql/update/20260323_manuscript_review_execution_role_matrix_seed_v01.sql
- Brain/script/sql/update/20260323_manuscript_review_readable_http_fixture_v01.sql
- Brain/brain-modules/brain-workflow/src/main/java/com/gxpublish/brain/workflow/service/impl/TestLeaveServiceImpl.java
- Brain/brain-modules/brain-workflow/src/main/java/com/gxpublish/brain/workflow/handler/FlowProcessEventHandler.java
- Brain/brain-modules/brain-workflow/src/main/java/com/gxpublish/brain/workflow/domain/vo/FlowTaskVo.java
- plus-ui-ts/src/types/manuscript-review/detail.ts
- plus-ui-ts/src/api/workflow/workflowCommon/index.ts

你的任务不是重开方案，不是进入 R7，不是去做前端，也不是去改 workflow 通用代码，而是只做 WP6-G1 当前还缺的 runtime/BPM 闭环补证。

【当前主控结论】
- `WP6-G1` 当前仍不通过。
- `R6.16` 这一轮执行合规，backend-private 新增 fresh 证据成立。
- 当前真正缺的是：流程定义、BPM 真正发起/撤销/流转、事件回写、详情返回真实跳转信息。

【你现在必须遵守的边界】
1. 不参考旧失败实现。
2. `brain-manuscript-review` 只作为代码落点，不作为实现基线。
3. `brain-editorial / plus-ui-ts editorial` 旧线不得引用、不得复制、不得借语义。
4. 不以现存代码形态反推冻结契约。
5. 不进入 R7。
6. 不新增 frontend 变更。
7. 不修改 shared 输出形态。
8. `workflow` 模块的通用 Java 代码先不要动。
9. 本轮不要监听 `ProcessDeleteEvent`，因为当前需求没有流程删除。
10. 如果你发现不改 `workflow` 通用 Java 代码就真的做不通，先停下，回 blocker，不要自己顺手改。

【你本轮只需要解决的事】
只围绕以下缺口补证：
1. `FR-05`
   - 发起人审批中撤销后，真实 workflow 实例也被撤销
   - 业务表状态变 `已取消`
   - 当前节点变 `流程已取消`
   - 时间线有撤销记录
   - 后续不能继续流转
2. `FR-06`
   - 三级审批驳回后，真实 workflow 流程终止
   - 业务表状态变 `已驳回`
   - 当前节点变 `流程已驳回`
   - 时间线有三级驳回记录
3. `FR-07 / FR-08 / PM-06 / NE-06`
   - “修改保存”和“去审批 / 再次提交”必须分两步
   - 去审批不是本地猜按钮，而是拿真实 `permissionMatrix + formPath + taskId`
   - 越权去审批、非法状态下重复提交/撤销/再次提交，在真实运行态被拒绝

【先说清楚：你不要先改哪一块】
- 不要先改 `Brain/brain-modules/brain-workflow/**` 里的通用实现。
- 不要先改 `plus-ui-ts/**`。
- 不要先碰 `editorial` 旧线。
- 不要先把 SQL、测试数据、菜单权限、流程定义全塞进一个大脚本。

【你应该按下面顺序做】

第 1 步：确认现在到底需要几条流程定义，不要猜。
- 去看 `Brain/brain-modules/brain-manuscript-review/src/main/java/.../ManuscriptReviewService.java` 里 `resolveSubmissionRoute` 的逻辑。
- 去看 `brain_manuscript_review_flow_config` 相关代码和现有数据口径。
- 确认：
  - `AUDIT` 用什么 `flowCode`
  - `PROOFREAD` 用什么 `flowCode`
  - 是一条流程定义复用两个流程类型，还是两个流程类型各自一条定义
- 没确认前，不要先写死 `manuscript_review_audit_flow` 或其他编码。
- 把你确认到的 `processType -> flowCode` 口径写进执行记录。

第 2 步：做流程定义 JSON 文件。
- 参考 `leave1` 的思路，但不要照抄 leave 的业务字段、变量和文案。
- 如果仓库里没有现成放流程定义 JSON 的正式目录，不要自己发明通用目录。
- 先把 JSON 放到 `doc/manuscript_review/` 下作为版本化执行资产，建议命名：
  - `doc/manuscript_review/runtime_<flowCode>_v01.json`
- JSON 里只保留本次需要的能力：
  - 固定三级串行审批
  - 一级/二级/三级审批
  - 退回发起人
  - 三级驳回
  - 发起人撤销
- 不要做：
  - 删除流程
  - 会签
  - 加签减签
  - 并行审批
  - 草稿
  - 详情页内审批
- `formPath` 不能猜，要和系统现有 BPM 办理页真实路径对齐。
- 如果 `AUDIT` 和 `PROOFREAD` 需要两条定义，就各写一份；如果最终确认共用一条，就只保留一份。

第 3 步：做 SQL，但必须拆开。
- 你至少要拆成 2 类 SQL：
  1. 正式长期 SQL：流程定义 / manuscript flow 配置 / 必要菜单权限角色
  2. dev-only SQL：本地测试账号 / 本地测试样例 / 本地验证用数据
- 不要把这两类混在同一个 SQL 文件里。
- 如有需要，可以拆成 3 类：
  1. `流程定义和配置`
  2. `菜单、按钮、角色、角色绑定`
  3. `dev-only 测试数据`

建议命名：
- `Brain/script/sql/update/20260325_manuscript_review_workflow_seed_v01.sql`
- `Brain/script/sql/update/20260325_manuscript_review_workflow_seed_rollback_v01.sql`
- `Brain/script/sql/update/20260325_manuscript_review_menu_role_seed_v01.sql`
- `Brain/script/sql/update/20260325_manuscript_review_menu_role_seed_rollback_v01.sql`
- `Brain/script/sql/update/20260325_manuscript_review_runtime_fixture_v01.sql`
- `Brain/script/sql/update/20260325_manuscript_review_runtime_fixture_rollback_v01.sql`

写 SQL 时必须遵守：
- 参考现有 manuscript_review SQL 的命名、分层、ID 风格，但不要照抄。
- 先核对你要插入的用户、角色、菜单、流程定义 ID 是否冲突。
- 本轮不给最终用户补“流程管理后台”的菜单权限。
- 如果只是为了让业务用户能发起、查看、审批，不要顺手给他们流程定义维护、流程实例管理等后台权限。
- 如果现有 `20260323_manuscript_review_execution_role_matrix_seed_v01.sql` 已经覆盖一部分测试账号和角色，就复用，不要重复造数据。
- 如果现有 `20260323_manuscript_review_readable_http_fixture_v01.sql` 已经有可用高位样例，就只补 runtime 缺的那部分，不要重复插一堆同类样例。

第 4 步：让 manuscript 模块真正调用 workflow，不要只本地改业务表。
- 先看 `Brain/brain-modules/brain-manuscript-review/pom.xml`。
- 如果 manuscript 模块现在还不能直接引用 workflow 里的现成服务接口，就先给 `pom.xml` 加 `brain-workflow` 依赖。
- 然后在 manuscript 的 service 里，直接调用 workflow 已有的发起/撤销/任务查询能力。
- 目标是把现在这几个本地动作改成“真实 workflow 驱动”：
  - `submitAndFlowStart`
  - `resubmit`
  - `cancelProcessApply`
- 不要自己在 manuscript 里重新造一套流程引擎。
- 不要为了省事，继续只本地写 `flowStatusLabel/currentNodeLabel` 冒充真实流程状态。

第 5 步：监听 BPM 事件，但只监听当前需要的。
- 参考 `TestLeaveServiceImpl` 的方式。
- 本轮只考虑：
  - `ProcessEvent`
  - `ProcessTaskEvent`
- 本轮不要接：
  - `ProcessDeleteEvent`

两个监听各自干什么：
- `ProcessEvent`
  - 负责把真实流程状态回写到 manuscript 业务表
  - 负责更新：
    - `flow_status_label`
    - `current_node_label`
    - `flow_instance_id`（如果事件里能拿到）
  - 负责补时间线记录，例如：
    - 提交
    - 再次提交
    - 持证跳级
    - 退回
    - 撤销
    - 三级驳回
    - 完成
- `ProcessTaskEvent`
  - 负责拿到当前待办任务信息
  - 重点确认详情页“去审批”所需的真实信息能否拿到：
    - `taskId`
    - 节点信息
    - 关联实例信息
- 如果 `taskId` 能在读时通过 workflow 查询实时拿到，就优先读时聚合，不要先加持久化字段。
- 如果详情接口现有契约里没有地方承载这类 workflow 信息，先回 blocker，不要擅自扩 shared 输出形态。

第 6 步：把“去审批”需要的真实信息在详情里聚合出来，但不要改前端。
- 前端这轮不改。
- 你只需要确认后端详情最终能拿到真实的：
  - `permissionMatrix`
  - `formPath`
  - 当前 `WAITING` 的 `taskId`
- 不要本地猜 `isCurrentApprover`。
- 不要自己拼假路由。
- 不要只返回 `formPath` 却拿不到 `taskId`，因为那样前端还是跳不过去。
- 参考：
  - `plus-ui-ts/src/types/manuscript-review/detail.ts`
  - `plus-ui-ts/src/api/workflow/workflowCommon/index.ts`

第 7 步：补测试和 fresh 证据。
- 先补单测/集成测，再跑最小 runtime 证据。
- 你本轮至少要跑：
  - `ManuscriptReviewServiceTest`
  - `ManuscriptReviewReadableServiceTest`
  - `ManuscriptReviewSharedApiMvcTest`
  - `ManuscriptReviewApiControllerTest`
  - `ManuscriptReviewFlowRoutingServiceTest`
  - `ManuscriptReviewDetailPermissionPolicyTest`
  - `ManuscriptReviewReadableVisibilityTest`
- 必须显式切 JDK17：
  - `cd Brain`
  - `set JAVA_HOME=C:\kuguaHome\env\java\jdk17`
  - `set PATH=%JAVA_HOME%\bin;%PATH%`
  - `mvn --% -pl brain-modules/brain-manuscript-review -am -Dtest=ManuscriptReviewServiceTest,ManuscriptReviewReadableServiceTest,ManuscriptReviewSharedApiMvcTest,ManuscriptReviewApiControllerTest,ManuscriptReviewFlowRoutingServiceTest,ManuscriptReviewDetailPermissionPolicyTest,ManuscriptReviewReadableVisibilityTest -Dsurefire.failIfNoSpecifiedTests=false -DskipTests=false test`

你本轮必须补的最小 runtime fresh 证据至少包括：
1. 发起人提交后，真实创建 workflow 实例
2. 待办里能看到对应任务，且能拿到 `taskId / formPath`
3. 发起人撤销后：
   - workflow 实例被撤销
   - 业务表变 `已取消 / 流程已取消`
   - history 正确
4. 走到三级后执行驳回：
   - workflow 流程终止
   - 业务表变 `已驳回 / 流程已驳回`
   - history 正确
5. 越权用户不能去审批
6. 非法状态下不能重复提交 / 撤销 / 再次提交
7. “修改保存”和“去审批 / 再次提交”没有被你在后端 shortcut 成一步

【允许改动的路径】
一、可以改
- `Brain/brain-modules/brain-manuscript-review/pom.xml`
- `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/domain/**`
- `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/gateway/**`
- `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/mapper/**`
- `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/service/**`
- `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/**`
- `Brain/script/sql/update/*manuscript_review*`
- `doc/manuscript_review/*.md`

二、默认先不要改
- `Brain/brain-modules/brain-workflow/**`

只有出现下面这种情况，才允许碰 workflow 代码，并且必须先在执行记录里写清楚原因：
- manuscript 侧已经补了流程定义、SQL、pom 依赖、service 调用、事件监听，但还是拿不到真实 `formPath/taskId` 或发起/撤销/驳回闭环
- 而且你已经确认问题不是配置、不是 SQL、不是 manuscript 侧调用方式

即便如此，也只允许改：
- 与 manuscript_review 流程定义读取
- 任务/实例查询
- 事件发布参数传递
直接相关的最小文件

不允许改：
- 泛化 workflow 引擎行为
- 与 manuscript_review 无关的流程治理
- 与其他业务线共享的通用策略

【默认禁改区】
- `plus-ui-ts/**`
- `Brain/brain-modules/brain-editorial/**`
- `plus-ui-ts/src/views/editorial/**`
- `plus-ui-ts/src/api/editorial/**`
- 与本轮缺口无关的 shared 契约和 shared 输出形态
- 与 manuscript_review 无关的 workflow 通用能力

【SQL 执行要求】
- 本轮允许在本地 dev/test 库执行你新写的 manuscript_review SQL，但前提是：
  - 先在 SQL 文件头写清 apply 顺序
  - 先写 rollback
  - 先确认 ID 和用户名不冲突
  - 先说明哪些是长期 SQL，哪些是 dev-only SQL
- 不要在执行记录里写成“已正式发布”，只能写本地验证结果。

【最终输出格式】
最终只允许输出：
- 已确认
- 风险
- 待确认
- 推进方案
- Verification
- Changed Files
- Next Gate

【落盘要求】
- 完成后必须新增版本化执行记录，不覆写 `R6.16`、`R6.17`、`R6.18`
- 建议命名：
  - 若完成补证：`doc/manuscript_review/R6.19_WP6-G1-M1执行完成记录.md`
  - 若中途发现 blocker：`doc/manuscript_review/R6.19_WP6-G1-M1执行阻塞记录.md`
- 落盘内容至少写清楚：
  - 本轮确认了几条 flowCode，分别对应什么 processType
  - 新增了哪些流程定义 JSON
  - 新增了哪些 SQL，分别负责什么
  - 是否修改了 `pom.xml`
  - 是否做到“workflow 通用 Java 代码不动”
  - 是否新增了 `ProcessEvent / ProcessTaskEvent` 监听
  - 是否明确不做 `ProcessDeleteEvent`
  - `FR-05 / FR-06 / FR-07 / FR-08 / PM-06 / NE-06` 各自补到了什么 fresh 证据
  - 当前是否仍有 blocker

【最重要的停止条件】
- 如果你发现以下任何一点还不清楚，就不要继续写实现，直接回 blocker：
  - `processType -> flowCode` 口径不清
  - 真实 BPM 办理页 `formPath` 口径不清
  - 详情现有契约里没有位置承载 workflow 真值，但主控又没允许扩 shared 输出
  - 需要改 workflow 通用 Java 代码才能做通，但你还没先排除配置/SQL/manuscript 侧接法问题
```

## 使用说明

- 这是 `R6.17` 之后的细化版执行提示词。
- 本版把“workflow 通用代码先不动、`ProcessDeleteEvent` 不纳入、SQL 必须拆层”写死了。
- 若后续主控再次调整边界，应继续新增版本，不覆写本文件。
