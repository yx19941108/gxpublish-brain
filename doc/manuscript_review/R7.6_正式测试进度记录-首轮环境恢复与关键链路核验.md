Owner: Codex
Task: manuscript_review 正式测试进度记录-首轮环境恢复与关键链路核验
Status: In Progress
Updated: 2026-03-27 13:24 +08:00

# R7.6_正式测试进度记录-首轮环境恢复与关键链路核验

## 1. 本文件是干什么的

这份文件用于在正式全量测试过程中做阶段停点记录，避免会话过长后丢失上下文。

当前只记录这一轮已经真实验证过的内容、已经发现的问题、还没做的内容，以及下一阶段应该怎么接着测。

## 2. 当前已确认事实

### 2.1 环境恢复情况

本轮开始时，正式测试环境并没有处于可测状态：

- `9999` 不通
- `9888` 不通
- 浏览器 DevTools 只有 `about:blank`

随后做了环境恢复，当前已确认：

- 前端 `9999` 已恢复，`HTTP 200`
- 后端 `9888` 已恢复，`HTTP 200`
- Redis `6379` 已恢复监听

恢复过程里还确认了一个关键原因：

- 后端第一次启动失败，不是代码报错，而是 Redis 容器当时还没真正 ready
- Redis 在 `2026-03-27 13:18:20` 才进入 `Ready to accept connections`
- 之后再次启动后端，后端成功连上 Redis 并恢复 `9888`

### 2.2 已完成的真实浏览器测试

#### 已完成 1：`mr_initiator` 登录成功

- 登录地址：`http://127.0.0.1:9999/login?redirect=/index`
- 登录账号：`mr_initiator / 666666 / 1234`
- 实际结果：
  - 成功登录
  - 成功进入 `/index`

#### 已完成 2：`mr_initiator` 台账页真实可打开

- 页面地址：`/manuscript/review`
- 实际结果：
  - 页面真实打开
  - 页面标题为“审校台账”
  - 页面存在搜索区、列表区、`新增` 按钮
  - 当前能看到 4 条样本记录

#### 已完成 3：`mr_initiator` 详情页真实可打开

- 样本：
  - `reviewId = 2037085206922715137`
  - `系统稿件号 = SH20260326003`
- 页面地址：
  - `/manuscript/review/detail?reviewId=2037085206922715137`
- 实际结果：
  - 页面真实打开
  - 页面标题为“审校详情”
  - 动作栏当前只有 `返回`
  - 详情区、资源区、统一时间线都能加载出来

#### 已完成 4：`mr_approver_l1` 详情页与审批页链路可用

- 登录账号：`mr_approver_l1 / 666666 / 1234`
- 复用样本：
  - `reviewId = 2037085206922715137`
- 详情页实际结果：
  - 页面真实打开
  - 动作栏存在 `修改 / 去审批 / 返回`
  - 页面状态为 `审批中 / 待一级审批`
- 审批页实际结果：
  - 点击“去审批”后成功进入审批页
  - 地址形如：
    - `/manuscript/review/approval?id=2037085206922715137&type=approval&taskId=2037331125394804737`
  - 页面标题为“审校审批”
  - 动作栏存在 `审批办理 / 返回`

#### 已完成 5：一级审批办理弹窗按钮集合核对通过

- 在审批页点击“审批办理”后，真实拉起办理弹窗
- 当前弹窗里已确认可见：
  - `审批意见`
  - `提交`
  - `转办`
  - `终止`
  - `退回`
  - `取消`

## 3. 已确认问题

### 3.1 已落正式问题单

已落盘问题文件：

- `doc/manuscript_review/R7.5_正式测试问题记录-详情页业务字段直接显示英文值.md`

问题内容简述：

- 详情页直接显示英文业务值
- 当前已确认出现的字段包括：
  - `媒体栏目 = Xinhua/Headlines`
  - `报送部门 = EditorialOffice`
  - `作者 = qauser`

这和正式测试方案里“页面展示应符合中文业务语义”的要求冲突。

## 4. 当前风险

1. 正式全量测试还没有真正跑到“新建并提交”的 fresh 写链路，当前更多是用已有样本做真实页面核验。
2. 资源、视频、视频标注、停用、下载、退回重提、撤销、三级驳回终止、菜单权限这些还没开始跑。
3. 如果继续在本会话里无节制往下堆证据，后面很容易忘掉：
   - 已恢复过的环境门禁
   - 已确认通过的审批页链路
   - 已落过的正式问题单

## 5. 还没做的测试

下面这些还属于正式测试未完成项：

1. `mr_initiator` 新建并提交 fresh 样本
2. `mr_certified_initiator` 持证跳级 fresh 样本
3. 一级、二级、三级完整审批推进
4. 退回后修改保存与再次提交
5. 审批中修改后再去审批
6. 发起人撤销
7. 三级审批驳回终止
8. 附件、外链、视频、视频标注
9. 资源停用、预览、下载
10. 历史时间线、数据库留痕核对
11. 非法输入与配置缺失拦截
12. 发起人待办、审批人我的待办/我的已办、菜单权限与按钮跳转

## 6. 下一阶段建议怎么接着做

建议下一阶段不要重读整条聊天，而是直接按下面顺序接：

1. 先确认环境还活着：
   - `9999`
   - `9888`
   - `6379`
2. 先读三份文件：
   - `R7.4_正式测试方案-全需求覆盖.md`
   - `R7.5_正式测试问题记录-详情页业务字段直接显示英文值.md`
   - `R7.6_正式测试进度记录-首轮环境恢复与关键链路核验.md`
3. 从 fresh 写链路开始，不要继续只看旧样本：
   - 先用 `mr_initiator` 做“新增并提交”
   - 再用 `mr_certified_initiator` 做“持证跳级”
4. 每发现一个正式问题，都继续按当前轮次在 `doc/manuscript_review/` 单独落问题单
5. 如果再遇到阻塞流程的问题，就单独落新的进度停点文件，不要把阻塞信息只留在聊天里

## 7. 当前结论

当前可以明确说：

- 正式测试已经开始，不是停留在看文档阶段
- 环境问题已经被恢复，不再是当前主阻塞
- 已拿到 `mr_initiator` 和 `mr_approver_l1` 的真实浏览器证据
- 已发现并落盘 1 个正式问题
- 整体测试仍处于进行中，远未结束
