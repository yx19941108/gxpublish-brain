Owner: Codex
Task: manuscript_review 正式测试进度记录-普通主链闭环与审批后回跳权限问题复现
Status: In Progress
Updated: 2026-03-27 15:31 +08:00

# R7.11_正式测试进度记录-普通主链闭环与审批后回跳权限问题复现

## 1. 本文件是干什么的

这份文件用于承接 `R7.8` 之后的下一阶段正式全量测试，记录本阶段已经拿到的真实浏览器、接口、MySQL 三层证据，以及本阶段新增问题和下一阶段应该怎么继续。

当前这份记录聚焦两件事：

1. 普通 fresh 主链样本是否已经真实走完一级、二级、三级审批闭环。
2. 审批成功后原审批人是否被错误带回无权查看的详情页。

## 2. 当前已确认事实

### 2.1 环境门禁已重新恢复

本阶段开始时，`9999 / 9888` 又一次掉线：

- `9999` 不通
- `9888` 不通
- Redis `6379` 仍在监听

随后已完成恢复并重新核验：

- 前端 `http://127.0.0.1:9999` -> `HTTP 200`
- 后端 `http://127.0.0.1:9888` -> `HTTP 200`
- 当前后端运行进程：
  - `java PID = 77684`
- 后端使用本轮已打包的新 jar：
  - `Brain/brain-admin/target/brain-admin.jar`
  - `LastWriteTime = 2026-03-27 14:08:35`

### 2.2 普通链路 fresh 样本已真实走完整条审批主链

本阶段继续使用 `R7.8` 已确认的普通 fresh 样本：

- `reviewId = 2037413044715327490`
- `manuscript_code = SH20260327005`

#### 一级审批

浏览器层：

- `mr_approver_l1` 打开详情页，状态为：
  - `审批中 / 待一级审批`
- 点击 `去审批` 成功进入审批页：
  - `taskId = 2037413045365444610`
- 办理弹窗可见：
  - `审批意见 / 提交 / 转办 / 终止 / 退回 / 取消`

接口层：

- `POST /dev-api/workflow/task/completeTask`
- 请求体关键字段：
  - `taskId = 2037413045365444610`
  - `message = 正式测试 L1 审批通过 - 2026-03-27 15:12`
- 返回：
  - `code = 200`
  - `msg = 操作成功`

MySQL 层：

- 主表推进到：
  - `current_node_label = 待二级审批`
  - `current_node_status = LEVEL_2`
- 历史新增：
  - `APPROVE`
  - `一级审批审批通过。审批意见：正式测试 L1 审批通过 - 2026-03-27 15:12。`
- 引擎待办推进到：
  - 旧 `first-review-node` -> `del_flag = 1`
  - 新 `second-review-node` -> `task_id = 2037428341346148353`

#### 二级审批

浏览器层：

- `mr_approver_l2` 打开详情页，状态为：
  - `审批中 / 待二级审批`
- 点击 `去审批` 成功进入审批页：
  - `taskId = 2037428341346148353`

接口层：

- `POST /dev-api/workflow/task/completeTask`
- 请求体关键字段：
  - `taskId = 2037428341346148353`
  - `message = 正式测试 L2 审批通过 - 2026-03-27 15:17`
- 返回：
  - `code = 200`
  - `msg = 操作成功`

MySQL 层：

- 主表推进到：
  - `current_node_label = 待三级审批`
  - `current_node_status = LEVEL_3`
- 历史新增：
  - `APPROVE`
  - `二级审批审批通过。审批意见：正式测试 L2 审批通过 - 2026-03-27 15:17。`
- 引擎待办推进到：
  - 旧 `second-review-node` -> `del_flag = 1`
  - 新 `final-review-node` -> `task_id = 2037429783771488258`

#### 三级审批

浏览器层：

- `mr_approver_l3` 打开详情页，状态为：
  - `审批中 / 待三级审批`
- 点击 `去审批` 成功进入审批页：
  - `taskId = 2037429783771488258`
- 三级办理弹窗仍真实可见：
  - `提交 / 转办 / 终止 / 退回 / 取消`

接口层：

- `POST /dev-api/workflow/task/completeTask`
- 请求体关键字段：
  - `taskId = 2037429783771488258`
  - `message = 正式测试 L3 审批通过 - 2026-03-27 15:23`
- 返回：
  - `code = 200`
  - `msg = 操作成功`

MySQL 层：

- 主表最终闭环为：
  - `flow_status_label = 已完成`
  - `current_node_label = 流程完成`
  - `current_node_status = FLOW_FINISHED`
- 历史新增：
  - `FINISH`
  - `流程审批已完成。`
- 引擎待办：
  - `final-review-node` -> `del_flag = 1`

结论：

- 普通 fresh 样本 `2037413044715327490` 已拿到浏览器、接口、MySQL 三层证据，确认真实走完整条三级审批主链并最终闭环。

### 2.3 持证跳级样本已被二级审批真实接住

本阶段继续使用 `R7.8` 已确认的持证 fresh 样本：

- `reviewId = 2037413420613046274`
- `manuscript_code = SH20260327006`

本阶段新增证据如下：

浏览器层：

- `mr_approver_l2` 打开详情页，状态为：
  - `审批中 / 待二级审批`
- 点击 `去审批` 成功进入审批页：
  - `taskId = 2037413421187665922`

接口层：

- `POST /dev-api/workflow/task/completeTask`
- 请求体关键字段：
  - `taskId = 2037413421187665922`
  - `message = 正式测试 持证链 L2 审批通过 - 2026-03-27 15:27`
- 返回：
  - `code = 200`
  - `msg = 操作成功`

MySQL 层：

- 主表推进到：
  - `current_node_label = 待三级审批`
  - `current_node_status = LEVEL_3`
- 历史现已包含：
  - `CREATE`
  - `SKIP_LEVEL_1`
  - `APPROVE`
- 引擎待办推进到：
  - 旧 `second-review-node` -> `del_flag = 1`
  - 新 `final-review-node` -> `task_id = 2037431878432071682`

结论：

- 持证跳级样本 `2037413420613046274` 已继续拿到浏览器、接口、MySQL 三层证据，确认其在跳过一级后被二级审批真实接住，并已推进到三级审批。

## 3. 本阶段新增问题

### 3.1 已新增正式问题单

本阶段新增问题文件：

- `doc/manuscript_review/R7.10_正式测试问题记录-审批通过后原审批人被带回无权详情页.md`

问题摘要：

1. 审批提交接口真实返回 `code = 200`。
2. MySQL 真实推进到下一节点或完成态。
3. 但前端会把刚完成审批的原审批人跳回详情页。
4. 该账号此时已无权查看该流程，详情接口真实返回：
   - `code = 500`
   - `msg = 当前用户无权查看该流程`
5. 浏览器最终呈现：
   - `详情数据加载失败，请稍后重试。`

该问题已在以下场景重复复现：

- 普通链路一级审批后
- 普通链路二级审批后
- 普通链路三级审批后
- 持证链路二级审批后

## 4. 当前风险

1. 普通主链已经闭环，但审批成功后的前端承接页仍不正确，会污染“已办/查看/回跳体验”相关结论。
2. 持证链路当前只推进到三级待办，尚未验证持证链最终闭环。
3. 退回后修改保存与再次提交、审批中修改后再去审批、发起人撤销、三级驳回终止这些分支还没有开始。
4. 资源/视频/视频标注/停用/下载/待办已办/菜单权限仍未展开。
5. 当前会话已经累积了多轮浏览器+接口+MySQL 证据，如果继续无节制推进，后面很容易丢失：
   - 普通主链已闭环的精确证据
   - 持证链当前停在三级待办的状态
   - `R7.10` 的适用范围已扩展到多级审批后的统一问题

## 5. 还没做的测试

下面这些仍属于正式全量测试未完成项：

1. 持证链路三级审批与最终闭环
2. 退回后修改保存与再次提交
3. 审批中修改后再去审批
4. 发起人撤销
5. 三级审批驳回终止
6. 附件、视频、视频标注
7. 资源停用、预览、下载
8. 我的待办、我的已办、菜单权限与按钮跳转
9. `R7.10` 修复前后的回跳/已办承接复测

## 6. 当前结论

当前已经可以明确说：

1. 普通 fresh 主链 `2037413044715327490` 已真实走完一级、二级、三级审批并闭环完成。
2. 持证 fresh 样本 `2037413420613046274` 已真实从跳级态推进到三级待办。
3. 本阶段新增的核心问题不是“审批没成功”，而是“审批成功后原审批人被错误跳回无权详情页”，且该问题已经跨多级审批重复复现。
4. 正式全量测试仍处于进行中，远未结束。

游工建议：
1、当前创建的审批单都是审核流程，希望补充校验流程的分支，后续测试样本可以改为校验流程分支的提交。
2、当前的所有测试样本都是提交的外链，后续样本可以补充提交附件或者视频的样本。
3、跳转建议，修改保存跳回详情页，审批完成后跳回列表页，审批页点击返回跳回入口页（待办进入跳待办列表，详情页进入跳详情），修改详情页点击返回同审批页。