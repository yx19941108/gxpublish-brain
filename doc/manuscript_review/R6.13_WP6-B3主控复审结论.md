Owner: Codex
Task: manuscript_review WP6-B3 主控复审结论
Status: Final
Updated: 2026-03-24 18:00 +08:00

# R6.13_WP6-B3主控复审结论

## 1. 审阅结论

- 结论：`通过`
- 结论边界：
  - 仅表示 `WP6-B3 最小整改` 已消除 `R6.10` 指出的 blocker，可进入 `WP6-G1`
  - 不表示 `repo 级冻结口径已闭合`
  - 不表示 `R7 可启动`

## 2. 结论原因

- 当前复审目标是 `R6.10` 指出的 4 个写动作缺少 record-level 修改权守卫；现 diff 仅在 `ManuscriptReviewService` 的 `addResource / disableResource / addVideoMark / disableVideoMark` 补齐 `ensureCanModify(requireRecord(...))`，与 `update` 同级，对齐 PRD 的“可审即可改 / 其他人不可改”。
- 当前 diff 仅触及 `R6.8` 允许的主改文件：`ManuscriptReviewService.java` 与 `ManuscriptReviewServiceTest.java`；未新增 SQL、workflow、frontend、shared 输出形态变更。
- `ManuscriptReviewServiceTest` 新增 4 组越权负测，锁住资源/标注四类写动作无修改权时抛出 `当前用户无权修改该流程`，且不落库、不写 history。
- 2026-03-24 fresh 验证结果：
  - service 定向：`Tests run: 19, Failures: 0, Errors: 0, Skipped: 0`
  - prompt 定向：`Tests run: 33, Failures: 0, Errors: 0, Skipped: 0`
- `ManuscriptReviewReadableServiceTest` 仍可读出 `UPDATE / RESOURCE_DISABLE / VIDEO_MARK_DISABLE` 时间线事件，满足 `R6.8` 对 timeline 可见性的通过条件。

## 3. 越界检查

- 结论：`WP6-B3 本体未越界`
- 说明：
  - 业务改动只落在 backend private 区 `.../service/**` 与 `.../src/test/**`
  - 未触碰 `controller/request/response`、`Brain/brain-modules/brain-workflow/**`、`plus-ui-ts/**`、`editorial` 旧线
  - 工作树仍存在与本包无关的噪声：`test_qa_flow.py` staged、`plus-ui-ts/.eslintrc-auto-import.json` 行尾告警；这些不属于本包放行证据，后续提交时不应混入

## 4. 旧失败实现污染检查

- 结论：`未发现新的旧语义回流`
- 说明：
  - 守卫逻辑复用现有 `ensureCanModify(existing)`，对齐 PRD 权限口径，没有引入旧 editorial / old readable / old count / old label 语义
  - 本轮未新增任何以现存旧页面、旧 DTO、旧 SQL 反推冻结契约的痕迹

## 5. shared / private 边界检查

- 结论：`未破坏`
- 说明：
  - 本轮没有继续扩 shared 面，也没有把 `video-mark/disable` 再扩成新的 shared/UI 包
  - 仍需沿用 `R6.5` 口径：`WP6 shared 接缝最小整改阶段有条件通过`、`冻结口径未整体闭合`、`R7 暂不放行`

## 6. 测试有效性检查

- 结论：`足够支撑 WP6-B3 复审通过，但不足以支撑 repo 级整体放行`
- 证据：
  - `ManuscriptReviewServiceTest` fresh 证明 4 个新增守卫未破坏既有正向用例，总计 `19` 绿
  - 四组新增负测直接覆盖 `addResource / disableResource / addVideoMark / disableVideoMark`
  - `ManuscriptReviewReadableServiceTest` 仍覆盖 timeline 读链可见性
  - `ManuscriptReviewSharedApiMvcTest` 与 `ManuscriptReviewApiControllerTest` 全绿，说明当前接口层未被本轮修正带坏
- 限制：
  - 当前测试结论只能支撑 `WP6-B3`，不能外推为 `WP6-G1 已放行` 或 `R7 可启动`
  - `WP6-G1` 仍需按 `R5` 第六章 P0 集单独判 gate

## 7. 下一步建议

- 进入 `WP6-G1` 主控 Gate 复核，严格按 `R5` 第六章 P0 集判断第 6 轮后端是否稳定
- 对外统一口径只允许上报：`WP6-B3 最小整改复审通过，可进入 WP6-G1；repo 级冻结口径未整体闭合，R7 暂不放行`

## 8. 若需整改

- 当前 `WP6-B3` 无强制整改项
- 若后续主控希望再加保险，最多补一条 HTTP 入口层的越权负测；该项属于增强证据，不是本次放行前置条件
