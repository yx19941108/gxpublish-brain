Owner: Codex
Task: manuscript_review 正式测试续工提示词-fresh提交修复后继续审批链路
Status: Ready
Updated: 2026-03-27 14:19 +08:00

# R7.9_正式测试续工提示词-fresh提交修复后继续审批链路

你在 `C:\kuguaHome\project\gxpublish-brain` 继续 `manuscript_review` 的正式全量测试。

## 1. 先读这些文件，不要回到旧聊天里找线索

1. `doc/manuscript_review/R7.4_正式测试方案-全需求覆盖.md`
2. `doc/manuscript_review/R7.5_正式测试问题记录-详情页业务字段直接显示英文值.md`
3. `doc/manuscript_review/R7.6_正式测试进度记录-首轮环境恢复与关键链路核验.md`
4. `doc/manuscript_review/R7.8_正式测试进度记录-fresh提交流程阻塞修复与双链路复测.md`

## 2. 当前已经确认的真实事实

1. 旧 `brain-admin.jar` 曾与仓库源码错配，现已用 JDK17 重新 `mvn -pl brain-admin -am -DskipTests package` 并重启。
2. fresh 提交流程真实代码缺陷已定位并修复：
   - 前端旧字段名：
     - `attachmentList`
     - `externalLinkList`
   - 后端真实契约：
     - `attachmentResources`
     - `externalLinks`
3. 定向前端测试已经通过：
   - `src/views/manuscript-review/form-state.spec.ts`
   - `src/views/manuscript-review/detail-readable-display.spec.ts`
4. 两条 fresh 浏览器链路已重新打通：
   - `mr_initiator` fresh 提交 -> `reviewId=2037413044715327490` -> `待一级审批`
   - `mr_certified_initiator` fresh 提交 -> `reviewId=2037413420613046274` -> `待二级审批`
5. 持证链路的数据库历史里已经真实写出：
   - `SKIP_LEVEL_1`

## 3. 当前不要重复做的事

1. 不要再重新证明旧 jar 为什么错配。
2. 不要再把 fresh 提交失败归因到浏览器脚本本身。
3. 不要再回到只看旧样本、不走 fresh 样本的阶段。

## 4. 下一步应该直接做什么

按下面顺序继续：

1. 先确认运行环境仍然活着：
   - `http://127.0.0.1:9999`
   - `http://127.0.0.1:9888`
2. 直接基于现有 fresh 样本推进审批链路：
   - 用 `mr_approver_l1` 推进普通链路一级审批
   - 用 `mr_approver_l2`、`mr_approver_l3` 继续推进普通链路
   - 用 `mr_approver_l2` 接住持证跳级样本
3. 在推进审批过程中继续覆盖：
   - 退回后修改保存与再次提交
   - 审批中修改后再去审批
   - 发起人撤销
   - 三级审批驳回终止
4. 在 fresh 主链稳定后，再继续资源/视频/视频标注/停用/下载/待办已办/菜单权限

## 5. 执行纪律

1. 每发现一个新问题，单独在 `doc/manuscript_review/` 新增问题单，不要混写进已有进度文件。
2. 如果上下文再次变长，优先停下落新进度记录或新续工提示词，不把关键判断链只留在聊天里。
3. 不要把“普通链路通过”和“持证跳级通过”压成一句“fresh 已通过”，要分别保留浏览器、接口、MySQL 三层证据。
