Owner: Codex
Task: manuscript_review 用例53-54浏览器最终回归
Status: Passed
Updated: 2026-03-28 13:40:30 +08:00

# R7.23_正式测试执行记录-用例53-54浏览器最终回归

## 1. 本轮承接范围

- 只承接 `R7.22` 中的剩余确认项：
  1. 在**加载最新后端进程**后，对 `R7.4 用例-53 / 54` 做浏览器最终回归
- 不回头重做：
  1. `R7.10 / R7.12` 已闭环内容
  2. `mp4` 上传上限 `1GB` 口径
  3. `我发起的 / 我的待办 / 我的已办` 查看跳转到 `detail`
  4. 详情页资源区 `停用 / 预览 / 下载`
  5. `R7.21 / R7.22` 已完成的代码修复与定向自动化

## 2. 运行态核验

### 2.1 前端

1. `9999` 当前仍为 `Vite` 开发服务：
   - 进程：`node`
   - 端口：`127.0.0.1:9999`
2. 由于是开发态承载，源码刷新即可读取到当前前端最新实现。

### 2.2 后端

本轮先核验 `9888` 是否加载到最新修复，结论如下：

1. 原运行进程：
   - `java -jar /home/kugua/dev/gxpublish-brain/Brain/brain-admin/target/brain-admin.jar`
   - 启动时间早于 `R7.22` 后端 class 编译时间
2. 原 `brain-admin.jar` 时间戳为旧产物，不能证明已包含 `R7.22` 最新修复。
3. 本轮已执行最小重载：
   - 重新打包 `Brain/brain-admin`
   - 新 jar 时间戳：`2026-03-28 13:29:51 +08:00`
   - 新启动进程：`pid = 31261`
4. 新后端启动日志已命中：
   - `Started BrainApplication`
   - `http-nio-9888`

结论：

- 本轮浏览器回归是在**确认已加载最新后端代码**之后执行，不是沿用旧 `9888` 进程。

## 3. 本轮复用样本

沿用 `R7.22` 的最小真实样本，不重建数据：

1. `reviewId = 2037757937328881665`
2. `attachmentId = 2037757937500848130`
3. `externalLinkId = 2037757937635065857`
4. `ossId = 2037757936959782914`

## 4. 浏览器执行方法

1. 使用浏览器真实页面渲染回归详情页，不改业务数据。
2. 登录口径：
   - 在浏览器上下文内调用真实登录接口
   - 写入 `Admin-Token`
   - 再打开详情页，确保页面、接口、点击动作都在浏览器环境中发生
3. 执行账号：
   - `mr_initiator / 666666`
4. 打开页面：
   - `/manuscript/review/detail?reviewId=2037757937328881665`

说明：

- 登录页 DOM 选择器本轮存在不稳定性，因此未把“手填登录框”作为必要证据点。
- 本轮正式结论以浏览器详情页真实渲染、真实点击、真实下载/跳转、真实接口返回为准。

## 5. 浏览器最终回归结果

### 5.1 页面可达与样本承接

浏览器事实：

1. 发起人登录成功。
2. 详情页真实 URL 为：
   - `http://127.0.0.1:9999/manuscript/review/detail?reviewId=2037757937328881665`
3. 页面隐藏锚点真实承接：
   - `data-review-id = 2037757937328881665`

结论：

- 旧样本仍可复用，本轮**无需重建**“1 个 pdf + 1 条外链”的最小样本。

### 5.2 当前有效资源区状态

浏览器事实：

1. 详情页资源区提示为：
   - `当前暂无附件数据。`
   - `当前暂无外链数据。`
   - `当前暂无视频数据。`
   - `当前暂无视频标注数据。`

接口事实：

1. `GET /workflow/manuscript-review/2037757937328881665`
2. 返回中：
   - `attachmentList.length = 0`
   - `externalLinkList.length = 0`

结论：

- 附件与外链停用后的“当前有效资源区清空”口径仍成立，与 `R7.22` 一致。

### 5.3 用例-53 浏览器最终回归

浏览器事实：

1. 时间线中已出现可点击附件名按钮：
   - `《R7.4-53-54附件-20260328050526.pdf》`
2. 点击后真实触发下载。
3. 浏览器下载建议文件名为：
   - `2b16ea3347bb4138af5a26e4233b9cae.pdf`

接口事实：

1. 详情接口 `timelineItems` 中，停用附件历史返回：
   - `eventCode = RESOURCE_DISABLE`
   - `relatedResourceName = R7.4-53-54附件-20260328050526.pdf`
   - `relatedResourceOssId = 2037757936959782914`

结论：

- `R7.4 用例-53` 浏览器最终回归通过。

### 5.4 用例-54 浏览器最终回归

浏览器事实：

1. 时间线中已出现可点击外链名按钮：
   - `《R7.4-53-54外链-20260328050526》`
2. 点击后真实打开新页面。
3. 新页面 URL 为：
   - `https://example.com/manuscript-review/r754/20260328050526`
4. 新页面标题为：
   - `Example Domain`

接口事实：

1. 详情接口 `timelineItems` 中，停用外链历史返回：
   - `eventCode = RESOURCE_DISABLE`
   - `relatedResourceName = R7.4-53-54外链-20260328050526`
   - `relatedExternalUrl = https://example.com/manuscript-review/r754/20260328050526`

结论：

- `R7.4 用例-54` 浏览器最终回归通过。

## 6. 本轮证据

保存在 `.agent-temp/manuscript_review_r7_22/`：

1. `r7_22_final_regression_detail.png`
2. `2b16ea3347bb4138af5a26e4233b9cae.pdf`
3. `backend_9888_reload.log`

## 7. 本轮结论

1. `R7.22` 的后端 + 前端修复在**加载最新后端进程后**已完成浏览器最终回归。
2. `R7.4 用例-53 / 54` 均为：
   - 修复前已真实失败
   - 修复代码已落地
   - 定向自动化已通过
   - 浏览器最终回归已通过
3. 当前样本 `2037757937328881665` 可继续作为后续资源区相关回归的只读样本复用。

## 8. 下一步建议

1. `R7.4 用例-53 / 54` 可从当前待确认列表中移出。
2. 若继续 `manuscript_review`，下一优先项可转入：
   - 多视频切换
   - 标注点击跳时
