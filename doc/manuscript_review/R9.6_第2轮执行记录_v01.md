Owner: Codex
Task: manuscript_review R9 round-2 execution record
Status: Done
Updated: 2026-03-31 17:40:48 +08:00

# R9.6_第2轮执行记录_v01

## 1. 范围

本轮严格锁定：

1. 问题 1：作者字段前后端必填口径一致
2. 问题 2：多视频切换后，新增视频标注当前视频实时联动
3. 问题 4：大文件上传显示进度且页面级阻断
4. 问题 11：新增视频标注校验提示就近显示

本轮未纳入：

1. 问题 3、5、6、7、8、9、10、12、13
2. 旧 `brain_manuscript_review_history` 数据修补
3. 与 `R9` 无关的历史返修
4. 第 1 轮已闭环项回头重做

## 2. 执行前复核结论

### 2.1 文档与真实代码对齐结果

本轮 4 个问题在 `R9.1` 与真实代码之间未发现新的方向性冲突，可直接按前端主闭环推进：

1. [form.vue](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\form.vue) 的作者字段存在，但前端规则缺 `authorName` 必填；后端 [ManuscriptReviewService.java](C:\kuguaHome\project\gxpublish-brain\Brain\brain-modules\brain-manuscript-review\src\main\java\com\gxpublish\brain\manuscript\review\service\ManuscriptReviewService.java) 仍通过 `validateAuthorName(...)` 强制非空。
2. [detail-page-shell.vue](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\detail-page-shell.vue) 的视频切换只改 `activeVideoId`，不会实时同步 `pendingVideoMarkResourceId`。
3. [form.vue](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\form.vue) 上传区只有 `beforeUpload / onSuccess / onError`，没有上传进度和页面级阻断状态。
4. [detail-page-shell.vue](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\detail-page-shell.vue) 标注新增校验失败时写入页顶 `errorMessage`，没有就近局部提示。

### 2.2 文档漂移但不纳入本轮

`R9.1` 中第 1 轮问题的“当前状态”有旧描述残留，但已被 [R9.5_第1轮执行记录_v01.md](C:\kuguaHome\project\gxpublish-brain\doc\manuscript_review\R9.5_第1轮执行记录_v01.md) 和真实代码纠正。本轮不回退这些已闭环问题。

## 3. 本轮变更结果

### 3.1 前端主代码

1. [form.vue](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\form.vue)
   - 作者字段前端规则前移，主表单现在会在提交前拦截 `authorName` 空值。
   - 上传区增加进度列表、上传中状态和页面级阻断：
     - 上传按钮上传中禁用
     - 主按钮上传中禁用
     - 删除暂存资源上传中禁用
     - 返回时弹二次确认
2. [detail-page-shell.vue](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\detail-page-shell.vue)
   - 顶部视频切换后，当前视频下拉同步切换到同一资源。
   - 新增视频标注校验提示改为局部 `videoMarkFormError`，就近显示在标注区内。

### 3.2 可单测 helper

1. [formState.ts](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\components\formState.ts)
   - 新增表单规则构造 helper
   - 新增上传进度状态 helper
2. [detail.contract.ts](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\detail.contract.ts)
   - 新增视频选择同步 helper
   - 新增视频标注草稿校验 helper

### 3.3 定向测试文件

1. [form-state.spec.ts](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\form-state.spec.ts)
   - 补作者必填断言
   - 补上传进度状态迁移与阻断断言
2. [form-layout.spec.ts](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\form-layout.spec.ts)
   - 补表单壳层中的上传进度与阻断挂钩断言
3. [detail-readable-display.spec.ts](C:\kuguaHome\project\gxpublish-brain\plus-ui-ts\src\views\manuscript-review\detail-readable-display.spec.ts)
   - 补视频切换同步断言
   - 补视频标注局部校验断言

## 4. 验证结果

### 4.1 静态验证

执行命令：

```powershell
pnpm exec eslint src/views/manuscript-review/form.vue src/views/manuscript-review/detail-page-shell.vue src/views/manuscript-review/components/formState.ts src/views/manuscript-review/detail.contract.ts src/views/manuscript-review/form-state.spec.ts src/views/manuscript-review/form-layout.spec.ts src/views/manuscript-review/detail-readable-display.spec.ts
```

结果：

1. 目标文件定向 eslint 通过。
2. 首次失败仅为 Prettier/CRLF 规范，已对本轮触达文件做定向 `prettier --write`，未扩展到仓库其他区域。

### 4.2 前端单测

执行命令：

```powershell
pnpm exec vitest run src/views/manuscript-review/form-state.spec.ts -t "includes authorName in the required front-end form rules|tracks upload progress items and exposes blocking state while uploads are in flight"
pnpm exec vitest run src/views/manuscript-review/form-layout.spec.ts -t "exposes author validation and upload progress blocking hooks in the form shell"
pnpm exec vitest run src/views/manuscript-review/detail-readable-display.spec.ts -t "keeps the pending video mark target synchronized with the selected video|returns local validation errors for incomplete pending video marks"
```

结果：

1. `authorName` 必填规则断言通过，对应 `TC-R9-01`。
2. 上传进度状态与阻断断言通过，对应 `TC-R9-05` 的前端单测证据。
3. 视频切换联动断言通过，对应 `TC-R9-02`。
4. 视频标注局部校验断言通过，对应 `TC-R9-12`。

说明：

1. 同文件中存在历史 baseline 红项，但不属于本轮范围，未混报为本轮失败。
2. 本轮只宣告与新增/直接相关断言的 fresh 结果。

### 4.3 浏览器级验证

运行态：

1. 前端：`http://127.0.0.1:9999`
2. 后端：`http://127.0.0.1:9888`
3. 浏览器账号：`mr_initiator / 666666 / 1234`

`TC-R9-02`：

1. 打开 `reviewId=2038771188202020865` 的详情页。
2. 该详情页存在 2 个有效视频，当前视频下拉初始值为 `2038771188428513281`。
3. 点击顶部第二个视频切换按钮后，当前视频下拉值同步变为 `2038786696813006849`。

结论：

1. 顶部视频切换与“新增视频标注-当前视频”下拉已实时联动。
2. 未再保留旧视频资源值。

`TC-R9-12`：

1. 在同一详情页直接点击“提交新增标注”，不填写开始时间。
2. 页面顶部 `errorMessage` 区域未出现新的页级错误。
3. 标注区内出现局部提示 `请先填写视频标注开始时间。`。

结论：

1. 新增视频标注校验提示已就近显示。
2. 页顶错误区未承担这类局部校验提示。

辅助证据：

1. 浏览器截图：`C:\kuguaHome\project\gxpublish-brain\.agent-temp\r9-round2-tc-r9-12-local-error.png`

## 5. 残留风险

1. `TC-R9-05` 本轮拿到了单测与静态证据，但未做“大文件真实上传”浏览器闭环；若游工要求把上传反馈提升到强运行态证据，建议在第 5 轮总回归补跑一次大文件场景。
2. 现有 `form-layout.spec.ts`、`detail-readable-display.spec.ts` 中仍有与本轮无关的历史断言漂移；本轮未顺手修这些旧测试，避免扩 scope。
3. 浏览器 MCP 取证阶段使用的是真实页面与真实账号，但未额外触发后端新增视频标注提交成功链路；本轮锁定的是问题 2/11 的交互与校验口径，不额外扩展到新增资源写入回归。

## 6. 是否可进入下一轮

可进入 `R9` 第 3 轮。

进入判断依据：

1. 本轮锁定的 4 个问题已经落到真实代码闭环。
2. `TC-R9-01 / TC-R9-02 / TC-R9-05 / TC-R9-12` 均拿到本轮要求内的验证证据。
3. 未回头改动第 1 轮已闭环项，也未扩展到第 3/4/5 轮范围。

建议下一轮起点：

1. 继续先读 `R9.1 / R9.2 / 本文`
2. 范围锁到问题 7、8、9、10
3. 维持“详情资源壳层收口，不回头混改表单/登录主链”的边界
