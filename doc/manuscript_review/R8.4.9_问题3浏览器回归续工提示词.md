Owner: Codex
Task: manuscript_review 问题3浏览器回归续工提示词
Status: Draft
Updated: 2026-03-30 20:40:00 +08:00

# R8.4.9_问题3浏览器回归续工提示词

## 1. 使用方式

下一轮若继续承接问题 3，不要回旧聊天，直接先读：

1. `doc/manuscript_review/R8.4.6_问题3台账口径冻结.md`
2. `doc/manuscript_review/R8.4.7_问题3修复续工提示词.md`
3. `doc/manuscript_review/R8.4.8_问题3浏览器收口回归记录.md`

若与 `R8.2 / R8.3 / R8.2.5` 冲突，按大编号文档和 `R8.4.8` 中已落盘事实执行。

## 2. 已完成事实

1. 本机已补 dev-only 样本 SQL：
   - `Brain/script/sql/update/manuscript_review/20260330_manuscript_review_issue3_browser_regression_seed_v01.sql`
2. 为恢复运行面，已修最小启动阻塞：
   - `Brain/brain-modules/brain-manuscript-review/src/main/resources/mapper/manuscript/review/ManuscriptReviewRecordMapper.xml`
   - 将第 28 / 43 行的 `<>` 改成 `<![CDATA[ <> ]]>`
3. 当前 `9999 / 9888` 都能启动
4. 浏览器已确认：
   - `mr_initiator`：台账仅 `详情`，且有 `新增`
   - `mr_mix_l1`：台账仅 `详情`，且有 `新增`
   - `mr_observer`：无 `新增`，列表为空
5. 浏览器已失败确认：
   - `mr_approver_l1`：关键词 `MR-I3-L1-` 下台账为空
   - `mr_mix_l1`：关键词 `MR-I3-MIX-` 下只出现发起样本，不出现待办/已办样本
6. 数据库已确认上述待办/已办样本真实存在并已指向对应用户，因此当前失败不是“样本没种进去”

## 3. 本轮不要重做

1. 不要重做 `mr_initiator` 与 `mr_observer` 的浏览器基础证据
2. 不要重新造新一批样本，优先复用 `MR-I3-*`
3. 不要再去讨论“新增按钮是不是 `create`”，已冻结为 `manuscript:review:submit`

## 4. 下一轮首要任务

按下面顺序推进：

1. 追前端台账实际命中的接口链：
   - `GET /workflow/manuscript-review/list`
2. 补取 `mr_approver_l1` 与 `mr_mix_l1` 命中该接口时的响应体
3. 定位该接口后端实现：
   - controller
   - service
   - mapper / SQL
4. 对照 `R8.4.6` 判断：
   - 是否仍只走发起人来源
   - 是否完全没合入待办/已办来源
   - 是否前端用错了列表接口

## 5. 本轮固定口径

1. 台账页“新增”按钮按 `manuscript:review:submit`
2. 台账页列表只允许 `详情`
3. `修改 / 去审批` 只允许出现在详情页
4. 问题 3 收口判定核心仍是：
   - 审批人台账能否看到 `我的待办 ∪ 我的已办`
   - 混合角色台账能否看到 `我发起的 ∪ 我的待办 ∪ 我的已办`

## 6. 可直接复用的下一轮开场提示词

```text
你现在继续承接 `gxpublish-brain / manuscript_review / 问题 3 浏览器收口回归`，不要回旧聊天，直接先读：
1. `doc/manuscript_review/R8.4.6_问题3台账口径冻结.md`
2. `doc/manuscript_review/R8.4.7_问题3修复续工提示词.md`
3. `doc/manuscript_review/R8.4.8_问题3浏览器收口回归记录.md`
4. `doc/manuscript_review/R8.4.9_问题3浏览器回归续工提示词.md`

已知事实直接承接：
1. `manuscript:review:submit` 是台账页“新增”按钮当前正确口径
2. 台账页列表只允许 `详情`
3. `修改 / 去审批` 只允许在详情页出现
4. `MR-I3-*` 样本 SQL 已落在 `Brain/script/sql/update/manuscript_review/20260330_manuscript_review_issue3_browser_regression_seed_v01.sql`
5. 浏览器已确认：
   - `mr_initiator` 正常
   - `mr_observer` 负例正常
6. 浏览器已失败确认：
   - `mr_approver_l1` 台账看不到 `MR-I3-L1-WAIT-001 / MR-I3-L1-FIN-001`
   - `mr_mix_l1` 台账只看到 `MR-I3-MIX-INIT-001`，看不到 `MR-I3-MIX-WAIT-001 / MR-I3-MIX-FIN-001`
7. 数据库已确认这些待办/已办样本真实存在并指向对应用户

本轮不要重做已通过的 initiator / observer 证据。
下一步直接追：
1. 前端 `GET /workflow/manuscript-review/list`
2. 该接口响应体
3. 该接口后端 controller / service / mapper / SQL

目标不是重造样本，而是解释并修复：
为什么审批人和混合角色在浏览器台账上看不到待办/已办样本。
```
