Owner: Codex
Task: manuscript_review R8 第二轮预览鉴权稳方案运行态回归
Status: Done
Updated: 2026-03-31 06:48:30 +08:00

# R8.5.4_第二轮预览鉴权稳方案运行态回归记录

## 1. 适用范围

本文只承接以下冻结边界：

1. `R8.3` 的第二轮：问题 `1 / 8 / 4 / 5`
2. `R8.5.3` 的稳方案：preview token 绑定 `resourceId + userId + clientid + 过期时间`
3. 浏览器 gate 只验证第二轮最小集与第一轮附加守门项

本文不进入：

1. 问题 `2` 的独立代码修复
2. 问题 `6`
3. 第一轮问题 `3 / 7 / 9` 的重新实现

## 2. 本轮代码与运行态结论

### 2.1 已确认实现

1. 后端新增 `GET /workflow/manuscript-review/resource/preview-ticket/{resourceId}`
2. `previewResource` 允许：
   - 登录态直连
   - 或短时效 `previewToken` 访问
3. preview token 当前绑定：
   - `resourceId`
   - `userId`
   - `userType`
   - `clientid`
   - `expireAtEpochSecond`
4. 前端详情页在加载后，会先为附件/视频资源申请 preview ticket，再把浏览器原生预览/视频播放 URL 改成：
   - `/dev-api/workflow/manuscript-review/resource/preview/{resourceId}?previewToken=...&clientid=...`
5. 下载链路仍保持 `/resource/oss/download/{ossId}`，未与 preview 混用

### 2.2 当前运行面

1. `9999 -> 200`
2. `9888 /actuator/health -> 401`
3. `9888` 已切到新 jar，并在启动日志中确认：
   - `Starting BrainApplication using Java 17.0.18`
   - `Starting ProtocolHandler ["http-nio-9888"]`
4. preview-ticket 接口已实测返回：
   - `resourceUrl` 带 `previewToken`
   - `clientid = e5cd7e4891bf95d1d19206ce24a7b32e`

## 3. 本轮执行用例与结果

执行来源：`.agent-temp/r8_second_round/summary.json`

### 3.1 第二轮最小回归集

1. `TC-01`：通过
   - 浏览器证据：`.agent-temp/r8_second_round/tc01_pending_delete_success.png`
   - 接口证据：`DELETE /workflow/manuscript-review/resource/pending/{ossId} -> http 200 / body code 200`
   - 数据证据：`pendingOssId=2038749816514527234`
2. `TC-02`：通过
   - 接口证据：旧全局删除接口返回 `code=403`
   - 接口证据：新模块内删除接口成功
   - 数据证据：`sysOssId=2038749816514527234`
3. `TC-11`：通过
   - 浏览器证据：点击详情页附件“预览”后未出现 `access denied`
   - 浏览器证据：截图 `.agent-temp/r8_second_round/tc11_tc12_detail_pdf_preview_trigger.png`
   - 补充说明：浏览器内建 PDF viewer 的地址文本捕获不稳定，`popupUrl` 未作为主证据使用
4. `TC-12`：通过
   - 接口证据：详情返回资源 URL 仍为模块内路径 `/workflow/manuscript-review/resource/preview/{resourceId}`
   - 接口证据：preview-ticket 再下发浏览器实际使用的带票据地址
   - 数据证据：`reviewId=2038749832083783681`
5. `TC-13`：通过
   - 浏览器证据：详情页存在视频标注新增区
   - 数据证据：`select=1 / input=2 / textarea=1`
6. `TC-14`：通过
   - 浏览器证据：新增后当前标注列表可见 `R8.5.2-VIDEO-MARK-20260330224632`
   - 浏览器证据：时间线新增 `VIDEO_MARK_ADD`
   - 截图：`.agent-temp/r8_second_round/tc13_tc14_video_mark_added.png`
7. `TC-22`：通过
   - 接口证据：详情资源 URL 不再暴露 OSS 直链
   - 数据证据：`directOssExposureRemoved=true`
8. `TC-23`：通过
   - 浏览器证据：`video.currentTime=1.463314`
   - 浏览器证据：`video.readyState=4`
   - 浏览器证据：`currentSrc` 已带 `previewToken` 与真实 `clientid`
   - 接口证据：preview 响应状态 `206, 206, 206`
   - 接口证据：`Content-Range=bytes 0-66636419/66636420`
   - 接口证据：`Accept-Ranges=bytes`
9. `TC-24`：通过
   - 浏览器证据：`browserCanAdvance=true`
   - 浏览器证据：`browserSideCurrentSrc` 为同域 `/dev-api/...preview?...previewToken=...`
10. `TC-25`：通过
    - 浏览器证据：下载文件名 `R8.5.2-PDF-20260330224632.pdf`
    - 接口证据：下载请求仍走 `/resource/oss/download/{ossId}`
    - 结论：`downloadSeparatedFromPreview=true`

### 3.2 第一轮附加守门项

1. `TC-26`：通过
   - 浏览器证据：审批人访问新增页被拉回 `/manuscript/review`
   - 浏览器证据：截图 `.agent-temp/r8_second_round/tc26_approver_create_guard.png`
2. `TC-28`：通过
   - 接口证据：审批人直调提交接口返回 `code=403`
3. `TC-05`：通过
   - 接口证据：台账分页结构正常，`page1Rows=1 / page2Rows=2`
4. `TC-18`：通过
   - 数据证据：本轮 fresh 样本 `reviewId=2038749832083783681`
   - 浏览器/接口证据：新增视频标注后时间线出现明确操作人文案

## 4. 第二轮 gate 结论

### 4.1 是否达到第二轮 gate

结论：达到。

已满足：

1. 问题 `1`：暂存删除已切到模块内权限接口
2. 问题 `8 / 4`：浏览器原生 PDF 与视频已走 preview token 稳方案
3. 视频播放已拿到 `206 / Content-Range / Accept-Ranges`
4. 问题 `5`：视频标注新增入口与新增后刷新已通过运行态验证
5. 第一轮附加守门项 `TC-26 / TC-28 / TC-05 / TC-18` 未被带坏

### 4.2 是否允许进入第三轮

结论：允许进入第三轮。

原因：

1. 第二轮目标问题浏览器 gate 已闭环
2. 当前未再发现必须阻塞第三轮的第二轮遗留 red
3. 问题 `6` 仍保持未进入状态，可按原冻结计划单独承接

## 5. 本轮证据工件

1. `.agent-temp/r8_second_round/summary.json`
2. `.agent-temp/r8_second_round/tc01_pending_delete_success.png`
3. `.agent-temp/r8_second_round/tc11_tc12_detail_pdf_preview_trigger.png`
4. `.agent-temp/r8_second_round/tc13_tc14_video_mark_added.png`
5. `.agent-temp/r8_second_round/tc26_approver_create_guard.png`
