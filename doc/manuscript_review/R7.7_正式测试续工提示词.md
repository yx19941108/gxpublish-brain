Owner: Codex
Task: manuscript_review 正式测试续工提示词
Status: Ready
Updated: 2026-03-27 13:24 +08:00

# R7.7_正式测试续工提示词

你在 `C:\kuguaHome\project\gxpublish-brain` 继续 `manuscript_review` 的正式全量测试。

## 1. 先读这些文件，不要先凭聊天记忆开测

1. `doc/manuscript_review/R7.4_正式测试方案-全需求覆盖.md`
2. `doc/manuscript_review/R7.5_正式测试问题记录-详情页业务字段直接显示英文值.md`
3. `doc/manuscript_review/R7.6_正式测试进度记录-首轮环境恢复与关键链路核验.md`

## 2. 当前已确认的真实事实

1. 这一轮开始时，`9999 / 9888` 都不通，后面已经恢复。
2. 后端第一次启动失败的根因是 Redis 容器未 ready，不是代码本身启动参数错。
3. 当前已确认：
   - `9999` 可达
   - `9888` 可达
   - `6379` 可达
4. 已跑过的真实浏览器链路：
   - `mr_initiator` 登录成功，台账页成功，详情页成功
   - `mr_approver_l1` 登录成功，详情页成功，审批页成功，审批办理弹窗成功拉起
5. 已落盘的正式问题：
   - 详情页直接显示英文业务值

## 3. 当前不要重复做的事

1. 不要重新论证环境为什么要起。
2. 不要回到只看旧文档、不进浏览器的阶段。
3. 不要把已经落盘的英文值问题再口头复述一遍就算处理。

## 4. 下一步应该直接做什么

按下面顺序继续：

1. 先复核端口：
   - `http://127.0.0.1:9999`
   - `http://127.0.0.1:9888`
2. 继续真实浏览器测试，不要退化成只调接口：
   - `mr_initiator` fresh 新增并提交
   - `mr_certified_initiator` fresh 持证跳级
   - 二级、三级审批推进
   - 退回后修改保存与再次提交
   - 审批中修改后去审批
   - 发起人撤销
   - 三级审批驳回终止
   - 附件/外链/视频/视频标注
   - 资源停用、预览、下载
   - 菜单权限、待办、已办按钮跳转
3. 每发现一个新问题，就继续在 `doc/manuscript_review/` 新增一份正式问题单。
4. 如果遇到新的流程阻塞，就新增一份正式进度停点文件。

## 5. 本轮输出要求

1. 问题描述必须用通俗中文，避免堆英文术语。
2. 每个问题单都要写建议解决方案。
3. 如果上下文再次变长，优先停下落盘，再给新的续工提示词。
