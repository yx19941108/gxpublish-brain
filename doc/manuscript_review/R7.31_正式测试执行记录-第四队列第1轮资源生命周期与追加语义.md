Owner: Codex
Task: manuscript_review 第四队列第1轮资源生命周期与追加语义
Status: Passed
Updated: 2026-03-29 22:40:00 +08:00

# R7.31_正式测试执行记录-第四队列第1轮资源生命周期与追加语义

## 1. 本轮承接边界

本轮严格只从以下文档恢复上下文并执行：

1. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.30_正式测试执行记录-第三队列当前态最小回归.md`
2. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.26_正式测试待办基线-四队列收口计划.md`
3. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.4_正式测试方案-全需求覆盖.md`

同时沿用当前会话给定前提：

1. 第二队列已通过。
2. 第三队列已通过。
3. manuscript-review 仍走专属任务页：
   - `/manuscript/review/my-document`
   - `/manuscript/review/task-waiting`
   - `/manuscript/review/task-finish`
4. SQL 改动统一收口在：
   - `/home/kugua/dev/gxpublish-brain/Brain/script/sql/update/manuscript_review/`
5. 本轮不扩到第四队列第 2 轮负向校验项。

## 2. 第四队列第 1 轮范围与取舍

### 2.1 本轮最终纳入范围

本轮按“资源生命周期与追加语义里更靠前、可成组验证”的原则，最终纳入：

1. `用例-20`
2. `用例-21`
3. `用例-22`
4. `用例-23`
5. `用例-24`
6. `用例-25`
7. `用例-26`
8. `用例-60`

### 2.2 本轮暂不纳入

1. `用例-55`

原因：

1. `用例-20` 至 `用例-26` 可以围绕同一批资源样本完成“暂存删除 -> 正式入库 -> 修改追加 -> 停用 -> 当前有效资源区 -> 外链可点击”的连续取证。
2. `用例-60` 与 `用例-22` 高度重合，且本轮样本一次保存即可同时验证“附件追加”和“外链追加”，纳入成本低。
3. `用例-55` 需要把“新增暂存区 / 修改页 / 详情页”三态外链点击都补成最终证据，更适合作为下一轮跨页面补证项；本轮只顺带拿到详情页外链点击证据，不足以独立盖章 `用例-55`。

## 3. 当前判定口径

本轮继续沿用第三队列的保守口径：

1. `通过`
   - 本轮当前态证据已与 `R7.4` 预期一致。
2. `未通过`
   - 本轮当前态证据已与 `R7.4` 预期发生明确冲突。
3. `证据不足`
   - 现有证据不足以裁定通过，也没有直接反证。

补充说明：

1. 若脚本入口或等待条件过时，只记为“脚本问题”，不直接判产品失败。
2. 本轮优先拿真实运行态证据，不为追求形式统一而把 API 当前态直证硬判成产品失败。

## 4. 运行态与复用能力

### 4.1 当前工作树与分支

已确认：

1. 当前分支仍为：
   - `manuscript-review-tdd-20260321`
2. 当前工作树为干净状态。

### 4.2 运行态恢复与核验

本轮开始时，`9888 / 9999` 已掉线；本轮以最小方式恢复后确认：

1. 前端：
   - `npm run dev -- --host 127.0.0.1 --port 9999`
   - `http://127.0.0.1:9999/` 返回 `200`
2. 后端：
   - `java -jar brain-admin/target/brain-admin.jar --spring.profiles.active=dev`
   - `http://127.0.0.1:9888/actuator/health` 返回 `401`
3. 监听情况：
   - `127.0.0.1:9999 -> node(vite)`
   - `*:9888 -> java(brain-admin.jar)`

结论：

1. 本轮证据均在恢复后的真实 `9888 / 9999` 运行态上取得。

### 4.3 本轮复用的既有能力

本轮未重造测试体系，直接复用：

1. `.agent-temp/manuscript_review_r7_28/` 既有 `login + token 注入 + Playwright + API` 骨架
2. 现成样本文件：
   - `doc/manuscript_review/test_file/2b16ea3347bb4138af5a26e4233b9cae.pdf`
3. 现有正式接口：
   - `POST /workflow/manuscript-review/submitAndFlowStart`
   - `PUT /workflow/manuscript-review`
   - `PUT /workflow/manuscript-review/resource/disable`
   - `DELETE /resource/oss/{ossId}`
4. `.agent-temp` 作为临时工件目录

## 5. 本轮实际执行

### 5.1 本轮临时脚本

新增临时脚本：

1. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_31_queue4_round1_evidence.mjs`

输出工件：

1. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_31_queue4_round1_evidence.json`
2. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_31_case21_detail_before_edit.png`
3. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_31_case22_edit_before_save.png`
4. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_31_case22_detail_after_save.png`
5. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_31_case24_26_after_disable.png`

### 5.2 脚本与产品边界说明

本轮在 `用例-20` 上先尝试真实浏览器点击“删除暂存附件”按钮，两次都卡在前端等待条件收敛，不适合直接拿来判产品失败。

因此本轮处理方式为：

1. 记录该点为“脚本等待条件风险”
2. 改用真实 `upload / delete pending oss / list` 当前态证据裁定产品行为
3. 不把脚本卡点混写成产品失败

## 6. 逐项结论

### 6.1 通过项

#### 用例-20 未提交前删除暂存附件

结论：

1. 通过

本轮证据：

1. 真实上传返回：
   - `ossId = 2038263990412234753`
   - `fileName = R7.31-Q4-CASE20-20260329143618.pdf`
2. 真实删除暂存资源返回：
   - `DELETE /resource/oss/{ossId} -> HTTP 200`
3. 以本轮唯一关键字检索正式台账结果为：
   - `listCountByKeyword = 0`

说明：

1. 本轮对 `用例-20` 的最终裁定基于真实运行态 API 直证。
2. 浏览器按钮等待条件本轮未稳定收敛，但该问题未被计入产品失败。

#### 用例-21 新增提交时附件与外链一体入库

结论：

1. 通过

本轮证据：

1. 新提交样本：
   - `reviewId = 2038263991943155713`
2. 当前详情返回：
   - `attachmentNames = [R7.31-Q4-原附件-20260329143618.pdf]`
   - `externalLinkNames = [R7.31-Q4-原外链-20260329143618]`
3. 时间线首条事件为：
   - `CREATE`
4. 浏览器详情截图：
   - `r7_31_case21_detail_before_edit.png`

#### 用例-22 修改保存只处理本次新增资源

结论：

1. 通过

本轮证据：

1. 修改页“当前已入库资源”仅展示已入库附件和外链，没有把它们混入本次草稿资源。
2. 保存后当前有效资源区变为：
   - `原附件 + 追加附件 + 原外链 + 追加外链`
3. 详情接口返回：
   - `attachmentNamesAfterSave = [原附件, 追加附件]`
   - `externalLinkNamesAfterSave = [原外链, 追加外链]`
4. 本轮 `UPDATE` 文案明确写出：
   - `新增附件《...》`
   - `新增外链《...》`

#### 用例-23 修改保存不推进流程

结论：

1. 通过

本轮证据：

1. 保存前后状态均为：
   - `businessStatusLabel = 审批中`
   - `currentNodeLabel = 待一级审批`
2. 保存后新增 `UPDATE` 数量为：
   - `1`
3. 未出现流程推进到下一节点的直接证据。

#### 用例-24 已入库资源停用

结论：

1. 通过

本轮证据：

1. 浏览器详情页对新增附件执行：
   - `停用`
2. 确认框文案为：
   - `确定停用该资源吗？`
3. 停用后详情接口中，该附件已从当前附件列表移除：
   - `attachmentPresentAfterDisable = false`
4. 时间线新增 `RESOURCE_DISABLE`：
   - `审校一级审批人停用了附件《R7.31-Q4-追加附件-20260329143618.pdf》。`

#### 用例-25 外链展示可点击

结论：

1. 通过

本轮证据：

1. 详情页点击新增外链：
   - `R7.31-Q4-追加外链-20260329143618`
2. 浏览器真实打开新页面：
   - `url = https://example.com/r7-31/q4/added/20260329143618`
   - `title = Example Domain`

#### 用例-26 当前有效资源区只显示有效资源

结论：

1. 通过

本轮证据：

1. 停用新增附件后，当前有效资源区只剩：
   - `原附件`
   - `原外链`
   - `追加外链`
2. 已停用附件不再显示：
   - `disabledAttachmentStillVisible = false`
3. 原有效资源仍保留可见：
   - `originalAttachmentStillVisible = true`
   - `linkStillVisible = true`

#### 用例-60 修改操作中附件与外链是追加不是覆盖

结论：

1. 通过

本轮证据：

1. 修改保存后仍保留原附件：
   - `originalAttachmentStillPresent = true`
2. 同时新增附件成功写入：
   - `addedAttachmentPresent = true`
3. 修改保存后仍保留原外链：
   - `originalLinkStillPresent = true`
4. 同时新增外链成功写入：
   - `addedLinkPresent = true`
5. 修改历史仍只有一条合并后的：
   - `UPDATE`

### 6.2 未通过项

本轮纳入范围内：

1. 无

### 6.3 证据不足项

本轮主动未纳入正式盖章，但已拿到部分关联证据的项：

1. `用例-55`

当前状态：

1. 本轮已顺带拿到“详情页外链可点击”的直接证据。
2. 但 `R7.4` 要求的是“新增暂存区、修改页、详情页都可点击跳转”。
3. 本轮未补齐新增暂存区与修改页的点击证据，因此仍记为：
   - `证据不足`

## 7. 本轮覆盖清单

### 7.1 本轮实际覆盖的第四队列用例

1. `用例-20`
2. `用例-21`
3. `用例-22`
4. `用例-23`
5. `用例-24`
6. `用例-25`
7. `用例-26`
8. `用例-60`

### 7.2 本轮通过情况

1. 已通过：
   - `用例-20`
   - `用例-21`
   - `用例-22`
   - `用例-23`
   - `用例-24`
   - `用例-25`
   - `用例-26`
   - `用例-60`
2. 未通过：
   - 无
3. 证据不足：
   - `用例-55`（本轮未纳入正式盖章，只拿到详情页局部证据）

## 8. 是否建议进入第四队列第 2 轮

建议：

1. 是

原因：

1. 本轮缩边界后的“资源生命周期与追加语义主线”已经可以收口。
2. 但第四队列整体仍未收口，至少还剩：
   - `用例-55`
   - `用例-31`
   - `用例-32`
   - `用例-33`
   - `用例-34`
   - `用例-35`
   - `用例-36`
   - `用例-49`
   - `用例-50`
3. 其中 `用例-55` 与本轮资源主线强相关，适合作为下一轮最先补盖章的项。

## 9. 最终结论

对“第四队列第 1 轮现在是否可收口”的直接回答是：

1. **如果本轮按本文收窄后的正式范围执行，即 `用例-20` 至 `用例-26` 加 `用例-60`，现在可以收口。**
2. **如果坚持把候选清单中的 `用例-55` 也算进本轮正式盖章范围，则当前还不能整体收口，因为 `用例-55` 仍是证据不足。**

本轮更稳妥的收口口径应为：

1. 第四队列第 1 轮的“资源生命周期与追加语义主线”已通过并可落档收口。
2. 随后进入第四队列第 2 轮，先补 `用例-55`，再按边界推进剩余项。
