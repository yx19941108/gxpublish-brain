Owner: Codex
Task: manuscript_review 第一队列浏览器最终回归
Status: Passed
Updated: 2026-03-29 01:16:00 +08:00

# R7.27_正式测试执行记录-第一队列浏览器最终回归

## 1. 本轮承接范围

- 只承接 `R7.26` 第一队列，不回头重做已闭环通过项：
  1. `用例-51`
  2. `用例-52`
  3. `用例-56`
  4. `用例-57`
  5. `用例-59`
- 不扩到：
  1. 第二队列
  2. 第三队列
  3. 第四队列
- 本轮目标：
  1. 先确认 `9888 / 9999` 仍承载最新代码
  2. 优先用真实浏览器补最终运行态证据
  3. 若仍失败先收敛根因，再决定是否改代码

## 2. 运行态核验

### 2.1 当前分支与端口

已确认：

1. 当前分支仍为：
   - `manuscript-review-tdd-20260321`
2. `9999` 当前仍为 `Vite` 开发服务：
   - `pid = 29381`
   - `node /home/kugua/dev/gxpublish-brain/plus-ui-ts/node_modules/.bin/vite serve --mode development --host 127.0.0.1 --port 9999`
3. `9888` 当前仍为 `brain-admin.jar` 进程：
   - `pid = 22227`
   - `java -jar /home/kugua/dev/gxpublish-brain/Brain/brain-admin/target/brain-admin.jar`
4. 当前 `brain-admin.jar` 时间戳仍为：
   - `2026-03-29 00:15:09 +08:00`

结论：

- 本轮浏览器最终回归仍是在 `R7.25` 已确认的最新运行态上执行，不是落回旧代码。

### 2.2 历史样本现状

本轮先复核 `R7.16` 的旧资源样本，结果如下：

1. 旧 `reviewId = 2037465008115294210`
2. 旧 `reviewId = 2037466003721428994`
3. 当前详情接口对旧样本返回：
   - `code = 500`
   - `msg = 稿件审校流程不存在`

结论：

- `R7.16` 旧资源样本已不在当前库中。
- `用例-51 / 52` 不能再依赖旧 `reviewId`，因此本轮改为最小真实样本重建。

## 3. 本轮新建最小真实样本

本轮只为第一队列补证，创建 `3` 个最小真实样本：

1. 资源按钮样本：
   - `reviewId = 2037940197709647874`
   - `businessCode = JD20260329003`
   - 标题：`R7.26 第一队列资源按钮样本 20260328170939`
   - 有效资源：
     - `pdf` `R7.26-资源按钮-PDF-20260328170939.pdf`
     - `docx` `R7.26-资源按钮-DOCX-20260328170939.docx`
     - 外链 `R7.26-资源按钮-外链-20260328170939`
2. PDF 样本：
   - `reviewId = 2037940200310116353`
   - `businessCode = JD20260329004`
   - 标题：`R7.26 第一队列 PDF 样本 20260328170939`
   - 有效资源：
     - `pdf` `R7.26-PDF停用预览下载-20260328170939.pdf`
3. 已办样本：
   - `reviewId = 2037940202298216449`
   - `businessCode = JD20260329005`
   - 标题：`R7.26 第一队列已办样本 20260328170939`
   - 一级审批待办任务：
     - `taskId = 2037940203090939906`
   - 一级审批完成后已办记录：
     - `taskId = 2037940205368446977`
     - `flowTaskStatus = pass`
     - `targetNodeCode = second-review-node`

说明：

1. 本轮浏览器工作页始终控制在 `3` 个以内。
2. 资源预览与外链打开均采用真实浏览器新页/弹页，不依赖伪造 DOM。

## 4. 浏览器最终回归结果

### 4.1 用例-51 最终通过

用例口径：

- 详情页资源区展示停用、预览、下载三个按钮。

真实浏览器事实：

1. 审批人打开：
   - `/manuscript/review/detail?reviewId=2037940197709647874`
2. `pdf` 资源：
   - `R7.26-资源按钮-PDF-20260328170939.pdf`
3. 该 `pdf` 行真实动作按钮为：
   - `停用`
   - `预览`
   - `下载`
4. 外链资源：
   - `R7.26-资源按钮-外链-20260328170939`
5. 外链行真实动作按钮为：
   - `停用`
   - `打开链接`

结论：

- `用例-51` 浏览器最终回归通过。

### 4.2 用例-52 最终通过

用例口径：

- 资源停用确认、`pdf` 预览与资源下载。

真实浏览器事实：

1. 审批人打开：
   - `/manuscript/review/detail?reviewId=2037940200310116353`
2. 点击 `预览` 后，真实弹出新页：
   - `http://127.0.0.1:9000/brain/2026/03/29/b7e177a90d6d4e3fb2fa8a6060856d3d.pdf`
3. 点击 `下载` 后，真实下载文件名为：
   - `R7.26-PDF停用预览下载-20260328170939.pdf`
4. 点击 `停用` 后，真实确认框文案为：
   - `确定停用该资源吗？`
5. 点击确认后，详情页资源区真实变为：
   - `当前暂无附件数据。`
6. 同步详情接口返回：
   - `attachmentList.length = 0`
7. 时间线新增停用记录：
   - `mr_approver_l1停用了附件《R7.26-PDF停用预览下载-20260328170939.pdf》。`

执行说明：

1. 当前页面承载的是“当前有效资源区”。
2. 因此资源一旦停用，当前行会立即从当前有效区移出。
3. 为了同时拿到 `pdf` 预览、下载、停用确认三项真实证据，本轮执行顺序为：
   - 先验证 `预览`
   - 再验证 `下载`
   - 最后验证 `停用确认 + 停用成功`

结论：

- `用例-52` 的三项能力均已在真实浏览器成立，可判定为最终通过。

### 4.3 用例-56 最终通过

用例口径：

- 发起人相关角色具有待办菜单权限。

真实浏览器事实：

1. `mr_initiator` 当前会话菜单可见：
   - `我的任务`
   - `我发起的`
   - `审校台账`
2. `mr_initiator` 真实可进入：
   - `/task/myDocument`
3. 页面真实显示发起人流程列表，并可见：
   - `查看`
   - `撤销`
4. `mr_certified_initiator` 当前会话菜单同样可见：
   - `我的任务`
   - `我发起的`
   - `审校台账`
5. `mr_certified_initiator` 真实可进入：
   - `/task/myDocument`
6. 当前版本对发起人侧的待办入口命名为：
   - `我发起的`

结论：

- 按当前项目实际菜单语义，发起人待办入口由 `我发起的` 承载。
- `mr_initiator / mr_certified_initiator` 均已拿到菜单可见与页面可进入的真实浏览器证据。
- `用例-56` 最终通过。

### 4.4 用例-57 最终通过

用例口径：

- 审批人相关角色具有我的待办、我的已办菜单权限且不体现我的抄送。

真实浏览器事实：

1. `mr_approver_l1` 当前会话菜单可见：
   - `我的任务`
   - `我的待办`
   - `我的已办`
   - `审校台账`
2. `mr_approver_l2` 当前会话菜单可见：
   - `我的任务`
   - `我的待办`
   - `我的已办`
   - `审校台账`
3. `mr_approver_l3` 当前会话菜单可见：
   - `我的任务`
   - `我的待办`
   - `我的已办`
   - `审校台账`
4. 三个审批人本轮都可真实进入：
   - `/task/taskWaiting`
5. 三个审批人会话中本轮均未体现：
   - `我的抄送`

结论：

- `用例-57` 浏览器最终回归通过。

### 4.5 用例-59 最终通过

用例口径：

- 我的待办、我的已办中的查看与办理按钮跳转正确。

真实浏览器事实：

1. 一级审批人 `我的待办` 中，`JD20260329004` 行真实展示：
   - `查看`
   - `办理`
2. 在 `我的待办` 点击 `查看` 后，真实跳转到：
   - `http://127.0.0.1:9999/manuscript/review/detail?reviewId=2037940200310116353&type=view&taskId=2037940201241251841&returnTo=/workflow/task/taskWaiting`
3. 在 `我的待办` 点击 `办理` 后，真实跳转到：
   - `http://127.0.0.1:9999/manuscript/review/approval?id=2037940200310116353&type=approval&taskId=2037940201241251841`
4. 从同一条详情页再点击动作栏 `去审批` 后，真实跳转到：
   - `http://127.0.0.1:9999/manuscript/review/approval?id=2037940200310116353&type=approval&taskId=2037940201241251841&returnTo=/manuscript/review/detail?reviewId=2037940200310116353%26type=view%26taskId=2037940201241251841%26returnTo=/workflow/task/taskWaiting`
5. 两条审批跳转的核心承接对象一致：
   - 同一 `reviewId`
   - 同一 `taskId`
   - 同一路由 `/manuscript/review/approval`
6. 一级审批人本轮已办样本 `JD20260329005` 已真实生成：
   - `flowTaskStatus = pass`
7. 在 `我的已办` 点击 `查看` 后，真实跳转到：
   - `http://127.0.0.1:9999/manuscript/review/detail?reviewId=2037940202298216449&type=view&taskId=2037940205368446977&returnTo=/workflow/task/taskFinish`

结论：

1. `我的待办 -> 查看` 已跳到我们的 `detail` 页。
2. `我的待办 -> 办理` 与 `详情页 -> 去审批` 承接的是同一条审批路由语义。
3. `我的已办 -> 查看` 已跳到我们的 `detail` 页。
4. `用例-59` 浏览器最终回归通过。

## 5. 本轮证据

保存在 `.agent-temp/manuscript_review_r7_22/`：

1. `r7_26_case_51_resource_actions.png`
2. `r7_26_case_52_pdf_preview_download_disable.png`
3. `R7.26-PDF停用预览下载-20260328170939.pdf`
4. `r7_26_case_59_waiting_finish_navigation.png`
5. `r7_26_queue1_browser_regression.json`
6. `r7_26_queue1_browser_regression.mjs`
7. `r7_26_case_59_compare_approve_route.mjs`

## 6. 本轮结论

第一队列 `5` 个用例本轮全部已拿到最终结论：

1. `用例-51`：
   - 已最终通过
2. `用例-52`：
   - 已最终通过
3. `用例-56`：
   - 已最终通过
4. `用例-57`：
   - 已最终通过
5. `用例-59`：
   - 已最终通过

补充结论：

1. 本轮不需要新增代码修改。
2. 第一队列已可从 `R7.26` 的“已做待盖章项”中整体移出。
3. 当前上下文尚未到必须分会话的程度，但后续若继续第二队列，建议直接从本文件接续，避免回读本轮浏览器证据。
