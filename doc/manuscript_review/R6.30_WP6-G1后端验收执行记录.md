Owner: Codex
Task: manuscript_review WP6-G1 后端验收执行记录
Status: Draft
Updated: 2026-03-26 13:33 +08:00

# R6.30_WP6-G1后端验收执行记录

## 已确认

- 本轮验收严格按 `WP6-G1 + R6.26 + R6.27 + R6.28` 执行，没有回退到旧 `保存/提交分离` 口径。
- 当前运行目标为：
  - `http://127.0.0.1:9888`
  - `C:\kuguaHome\env\java\jdk17\bin\java.exe -jar Brain\brain-admin\target\brain-admin.jar`
- 登录链 fresh 可用：
  - `clientId=e5cd7e4891bf95d1d19206ce24a7b32e`
  - `grantType=password`
  - `mr_*` 账号密码 `666666`
- `gxpublish_brain` 中 `brain_manuscript_review*` 业务表已存在，Redis 当前无 `manuscript*` 相关 key。

### 1. 新增提交一体化 fresh 结果

#### 1.1 非持证新增提交

- 样本：`reviewId=2037038308950110209`
- 主表：
  - `manuscript_code=SH20260326007`
  - `flow_status_label=审批中`
  - `current_node_label=一级审批`
- history：
  - 仅 1 条 `CREATE`
  - 未复现旧 `SUBMIT`
  - 未复现旧 `RESOURCE_ADD`
- BPM：
  - `flow_instance.node_code=first-review-node`
  - `flow_task` 存在一级审批待办 `taskId=2037038311030484994`
- 结论：
  - `R6.26 / R6.27` 要求的“非持证新增提交只写 CREATE”本轮 fresh 已满足
  - 但 `current_node_label` 文案不满足冻结口径

#### 1.2 持证新增提交

- 样本：`reviewId=2037038699032965122`
- 主表：
  - `manuscript_code=SH20260326011`
  - `flow_status_label=审批中`
  - `current_node_label=二级审批`
- history：
  - `SKIP_LEVEL_1` @ `2026-03-26 13:27:27`
  - `CREATE` @ `2026-03-26 13:27:28`
- BPM：
  - `flow_instance.node_code=second-review-node`
  - 二级待办存在 `taskId=2037038700068958210`
- 结论：
  - 持证跳级路由本身已生效，未落一级待办
  - 但 history 顺序错误，当前是 `SKIP_LEVEL_1 -> CREATE`，与冻结要求 `CREATE -> SKIP_LEVEL_1` 冲突
  - `current_node_label` 文案同样不满足冻结口径

### 2. 修改保存与去审批 fresh 结果

- 样本：`reviewId=2037038317049311234`
- 期望：
  - 当前审批人可查看详情
  - 可执行 `PUT /workflow/manuscript-review`
  - 保存阶段产生 `UPDATE`
  - 保存后再独立去审批
- 实际：
  - 一级审批人待办存在
  - 但 manuscript 详情读取被拒绝：`code=500`，`msg=当前用户无权查看该流程`
  - 审批人执行 `PUT /workflow/manuscript-review` 被拒绝：`code=500`，`msg=当前用户无权修改该流程`
  - 因此本轮未拿到 `UPDATE` history
  - 仅验证到 workflow 审批动作本身仍可直接通过 task API 写入可读 `APPROVE`
- history：
  - `CREATE`
  - `APPROVE` 一级通过
  - `APPROVE` 二级通过
- 结论：
  - `APPROVE` 文案可读这一点是绿的
  - 但 `修改保存 + 去审批分步` 主链未通过，因为详情/修改权限解析被当前节点文案卡死

### 3. 退回后修改再重提 fresh 结果

- 样本：`reviewId=2037038938255093762`
- 期望：
  - 一级审批人退回成功
  - 主表进入 `待发起人处理`
  - 发起人可修改保存
  - 再次提交成功
- 实际：
  - 一级审批待办存在
  - `POST /workflow/task/backProcess` 返回 `code=500`，`msg=未找到跳转类型匹配的目标节点!`
  - 主表仍保持：
    - `flow_status_label=审批中`
    - `current_node_label=一级审批`
  - 发起人 `PUT /workflow/manuscript-review` 返回 `code=500`，`msg=当前用户无权修改该流程`
  - `POST /workflow/manuscript-review/resubmit` 返回 `code=500`，`msg=仅退回给发起人的流程可再次提交`
- history：
  - 仅 `CREATE`
- 结论：
  - `退回 -> 修改 -> 再次提交` 整条链路本轮 fresh 明确不通过

### 4. 定向单测结果

- 执行命令：
  - `mvn -f Brain/pom.xml --% -pl brain-modules/brain-manuscript-review -am -Dtest=TableBackedManuscriptReviewSerialGatewayTest,ManuscriptReviewServiceTest,ManuscriptReviewReadableServiceTest,ManuscriptReviewSharedApiMvcTest,ManuscriptReviewApiControllerTest,ManuscriptReviewFlowRoutingServiceTest,ManuscriptReviewDetailPermissionPolicyTest,ManuscriptReviewReadableVisibilityTest,ManuscriptReviewServiceSpringConstructorTest -Dsurefire.failIfNoSpecifiedTests=false -DskipTests=false test`
- 结果：
  - `Tests run: 65, Failures: 1, Errors: 0, Skipped: 0`
  - 功能相关测试大部分为绿
  - 失败项是 `ManuscriptReviewServiceSpringConstructorTest`
  - 失败原因为 Spring 上下文里缺少 `ManuscriptReviewSysOssMapper` 候选 bean
- 结论：
  - 仓库当前定向测试结论整体为 `red`
  - 即使不考虑 runtime blocker，也还不能报告“测试全绿”

## 风险

- 当前最核心风险不是旧 `SUBMIT / RESOURCE_ADD`，而是 `current_node_label` 使用了：
  - `一级审批`
  - `二级审批`
  - `三级审批`
  而服务/读侧权限判断与冻结契约都依赖：
  - `待一级审批`
  - `待二级审批`
  - `待三级审批`
- 这一偏差已经直接导致：
  - 审批人 detail 查看失败
  - 审批人修改保存失败
  - 发起人退回后修改/再次提交链路无法进入
- 持证 history 顺序当前是 `SKIP_LEVEL_1 -> CREATE`，不是冻结要求的 `CREATE -> SKIP_LEVEL_1`。
- 当前定向单测整体仍是 `red`，不能作为放行证据。

## 待确认

- 是否将 `current_node_label` 文案与权限解析脱节，认定为当前 `WP6-G1` 在新冻结口径下的主 blocker。
- `ManuscriptReviewServiceSpringConstructorTest` 是否属于本轮 gate 范围内必须清零的定向测试失败项。

## 推进方案

1. 先把当前 gate 结论固定为：`WP6-G1 后端验收未通过`。
2. 下一最小整改应优先只修：
  - `current_node_label` 与冻结业务文案一致性
  - 持证 `CREATE / SKIP_LEVEL_1` 顺序
  - `backProcess` 返回发起人链路
3. 修完后按本轮同样链路重跑：
  - 非持证新增提交
  - 持证新增提交
  - 审批人修改保存
  - 退回后修改再重提
  - 定向 JDK17 测试

## Verification

- fresh HTTP 样本：
  - 非持证新增：`2037038308950110209`
  - 持证新增：`2037038699032965122`
  - 修改/审批样本：`2037038317049311234`
  - 退回/重提样本：`2037038938255093762`
- 关键 fresh 返回：
  - 审批人详情查看失败：`当前用户无权查看该流程`
  - 审批人修改失败：`当前用户无权修改该流程`
  - 退回失败：`未找到跳转类型匹配的目标节点!`
  - 重提失败：`仅退回给发起人的流程可再次提交`
- fresh SQL：
  - 非持证样本仅 `CREATE`
  - 持证样本 `SKIP_LEVEL_1 -> CREATE`
  - 修改样本无 `UPDATE`
  - 重提样本无 `BACK / UPDATE / RESUBMIT`
- fresh BPM：
  - 非持证一级待办存在
  - 持证二级待办存在
  - `form_path` 仍为 `/manuscript/review/approval`
- 定向测试：
  - `65` 跑
  - `1` 失败

## Changed Files

- 新增本文档：`doc/manuscript_review/R6.30_WP6-G1后端验收执行记录.md`
- 临时探针：`.agent-temp/manuscript_review_wp6g1_acceptance.ps1`

## Next Gate

- 当前只允许上报：
  - `WP6-G1` 在 `R6.26~R6.28` 新口径下，后端验收未通过
  - fresh 证据已锁定主 blocker，不再是泛泛“证据不足”
- 当前不允许上报：
  - `WP6-G1 已放行`
  - `后端已通过`
  - `R7 可启动`
