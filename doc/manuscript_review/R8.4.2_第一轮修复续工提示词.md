Owner: Codex
Task: manuscript_review R8 第一轮修复续工提示词
Status: Draft
Updated: 2026-03-30 13:08:00 +08:00

# R8.4.2_第一轮修复续工提示词

```text
你现在接手的是 `gxpublish-brain` 的 `manuscript_review` R8 第一轮修复执行续工，直接承接以下文件，不要重新发明方案，不参考旧失败实现，只基于已冻结与已通过材料承接：

1. `doc/manuscript_review/R8.0_问题排查续工交接.md`
2. `doc/manuscript_review/R8.2_问题池统一修复方案.md`
3. `doc/manuscript_review/R8.3_问题池分轮修复执行基线.md`
4. `doc/manuscript_review/R8.2.5_问题池统一回归测试用例.md`
5. `doc/manuscript_review/R8.4_第一轮修复执行提示词.md`
6. `doc/manuscript_review/R8.4.1_第一轮修复执行checkpoint.md`
7. `doc/manuscript_review/R8.4.2_第一轮修复续工提示词.md`

执行裁决规则不变：
1. 分轮与实现边界以 `R8.3` 为准
2. 问题级修复目标与游工意见以 `R8.2` 为准
3. 回归与最终验收以 `R8.2.5` 为准
4. 若 `R8.2.5` 与 `R8.3` 的轮次表述存在不一致，先按 `R8.3` 执行本轮修复，不擅自进入下一轮；只记录为文档待修项，不阻塞本轮代码修复

本轮只做第一轮，硬边界如下：
1. 只修问题 9、问题 3、问题 7
2. 不进入问题 1、4、5、6、8
3. 问题 2 继续只补证，不改代码
4. 问题 3 与问题 7 必须同轮收口，不得拆开
5. 不得顺手扩 scope

当前已验证到的红灯证据，直接承接，不要重新摸排：

一、问题 9
前端命令：
`npx vitest run src/views/manuscript-review/access-control.spec.ts src/views/manuscript-review/route-shell.spec.ts src/views/manuscript-review/ledger-layout.spec.ts`

已知失败点：
1. 台账页没有 `canCreate`
2. “我发起的”页没有 `canCreate`
3. `router/index.ts` 的 `review/form` 没有 `permissions`
4. `form.vue` 没有 create 模式前置权限校验与回退

后端命令：
`$env:JAVA_HOME='C:\kuguaHome\env\java\jdk17'; $env:Path='C:\kuguaHome\env\java\jdk17\bin;' + $env:Path; mvn -pl brain-modules/brain-manuscript-review -DskipTests=false "-Dtest=ManuscriptReviewApiControllerTest,ManuscriptReviewReadableFirstRoundGuardrailTest,ManuscriptReviewFirstRoundGuardrailTest" test`

已知失败点：
1. `update` 没有 `manuscript:review:edit`
2. legacy submit 不拦无发起权限用户
3. integrated submit 不拦无发起权限用户

二、问题 7
同一组 JDK17 定向测试已知失败点：
1. `SKIP_LEVEL_1` history 没有 actor
2. `BACK` workflow history 没有 actor
3. `APPROVE` history 没有 actor
4. 主文案还没有统一成“谁在什么环节做了什么”

三、问题 3
同一组 JDK17 定向测试已知失败点：
1. `ManuscriptReviewReadableService.listLedger(...)` 仍调用 `recordMapper.selectList(...)`
2. 仍属于全量查主表 / 全量查 history / Java 过滤 / Java 内存分页

当前工作树边界继续保持：
1. 绝对不要碰 `plus-ui-ts/.eslintrc-auto-import.json`
2. 绝对不要碰 `test_qa_flow.py`
3. `ManuscriptReviewServiceTest.java` 已恢复回 `HEAD`，不要再在这个大文件里堆本轮新增测试
4. 当前本轮新增红灯测试文件有：
   - `plus-ui-ts/src/views/manuscript-review/access-control.spec.ts`
   - `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewFirstRoundGuardrailTest.java`
   - `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewReadableFirstRoundGuardrailTest.java`

继续执行要求：
1. 先给 `已确认 / 风险 / 待确认 / 推进方案`
2. 不要重新开方案比较
3. 先收问题 9
4. 再同轮收问题 3 与问题 7
5. 不得把问题 3 和问题 7 拆开
6. 实现完成后，再严格按 `R8.2.5` 执行本轮对应回归
7. 汇报时必须列：
   - 实际执行的用例编号
   - 每条通过/失败
   - 浏览器证据
   - 接口证据
   - 数据证据
   - 是否达到第一轮 gate
```
