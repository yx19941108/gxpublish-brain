Owner: Codex
Task: manuscript_review R9 round-1 continuation prompt
Status: Draft
Updated: 2026-03-31 16:20:00 +08:00

# R9.4_第一轮续工提示词

## 1. 用途

本文是 `R9` 第 1 轮会话的专用启动提示词。

适用目标：

1. 开启一个新会话
2. 只推进 `R9` 第 1 轮
3. 不扩到第 2/3/4/5 轮

## 2. 续工提示词

```text
你现在在仓库 `C:\kuguaHome\project\gxpublish-brain` 中继续推进 `manuscript_review` 的 `R9` 第 1 轮。

先读以下文档，再开始任何代码分析或修改：
1. `doc/manuscript_review/R9.0_问题池汇总与修复准备.md`
2. `doc/manuscript_review/R9.1_问题池修复方案与影响评估.md`
3. `doc/manuscript_review/R9.2_问题池详细测试用例.md`
4. `doc/manuscript_review/R9.3_问题池会话执行基线.md`
5. `AGENTS.md`

本轮范围锁死为 `R9` 第 1 轮，不允许擅自扩到其他轮次：
1. 问题 3：流程类型冻结
2. 问题 5：资源区不再展示技术链接
3. 问题 6：资源区补齐操作人、操作时间、文件大小
4. 问题 12：主动退出登录不再串页

本轮明确不做：
1. 问题 1、2、4、7、8、9、10、11
2. 问题 13 首页设计或实现
3. 旧 `brain_manuscript_review_history` 数据修补
4. 与 `R9` 无关的历史返修

执行要求：
1. 必须先基于真实代码确认 `R9.1` 中这 4 个问题的当前落点，不允许只复述文档。
2. 在写代码前，必须按以下格式给出分步计划，每步都要写：
   - Goal
   - Affected Files
   - Risk
   - Verification
   - Rollback
3. 代码修改必须遵守“小到大”原则，先做最小闭环，再扩展。
4. 不要因为顺手就修改第 2/3 轮问题，即使它们就在同一个文件里。
5. 若发现 `R9.1` 与真实代码存在冲突，先汇报 `已确认 / 风险 / 待确认 / 推进方案`，不要直接按猜测改。

本轮优先修改面预期包括但不限于：
1. 后端：
   - `Brain/brain-modules/brain-manuscript-review/.../ManuscriptReviewService.java`
   - `Brain/brain-modules/brain-manuscript-review/.../ManuscriptReviewReadableService.java`
   - `Brain/brain-modules/brain-manuscript-review/.../controller/response/ManuscriptReviewDetailResponse.java`
2. 前端：
   - `plus-ui-ts/src/views/manuscript-review/form.vue`
   - `plus-ui-ts/src/views/manuscript-review/detail.contract.ts`
   - `plus-ui-ts/src/views/manuscript-review/detail-page-shell.vue`
   - `plus-ui-ts/src/layout/components/Navbar.vue`
   - `plus-ui-ts/src/views/login.vue`

本轮最低验收目标锁死为：
1. `TC-R9-03`
2. `TC-R9-04`
3. `TC-R9-06`
4. `TC-R9-07`
5. `TC-R9-13`
6. `TC-R9-14`

验证要求：
1. 后端验证：
   - 直接受影响测试或新增测试
   - 至少证明 `update(...)` 不再改 `flowCode`
   - 至少证明详情读侧已输出资源元信息新字段
2. 前端验证：
   - 至少覆盖资源区展示与登录退出回跳
   - 若能跑单测则优先跑单测
3. 浏览器级验证：
   - 必须验证主动退出后切换账号不再串页
4. 汇报时不要把“静态通过”和“浏览器已闭环”混成一句话。

输出格式要求：
1. 进展汇报默认使用：
   - 已确认
   - 风险
   - 待确认
   - 推进方案
2. 结束时必须给出：
   - 变更结果
   - 验证结果
   - 残留风险
   - 产物路径
   - 下一轮是否可进入

产物要求：
1. 本轮执行完成后，新增一份本轮执行记录到 `doc/manuscript_review/`
2. 若上下文开始变长，主动建议停在 handoff，不要硬撑到下一轮

先开始读取文档与真实代码，给出第 1 轮执行计划，然后再动手改代码。
```

## 3. 使用说明

1. 新会话直接粘贴“续工提示词”代码块即可。
2. 若新会话开始后环境、分支、工作树与当前不同，应先在新会话里报告差异，再决定是否继续。
