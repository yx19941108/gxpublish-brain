Owner: Codex
Task: manuscript_review runtime dual fix handoff
Status: Ready
Updated: 2026-03-25 17:52 +08:00

# 新会话提示词（可直接粘贴）

你在 `C:\kuguaHome\project\gxpublish-brain` 继续 `manuscript_review` 修复。

本会话目标只做两件事，不扩 scope：

1. 修复“持证发起人自动跳过一级审批”与流程引擎不一致的问题  
- 现状：业务表/历史写了 `SKIP_LEVEL_1`，但 workflow 仍生成 `first-review-node` 待办。  
- 目标：持证发起人提交后，引擎真实进入二级审批，不再产生一级待办任务。

2. 修复 `brain_manuscript_review_history` 缺失一级/二级审批通过记录  
- 现状：L1/L2 `completeTask` 成功后，history 仍只有 `CREATE/RESOURCE_ADD/SUBMIT/SKIP_LEVEL_1`，没有 `APPROVE`。  
- 目标：L1、L2 通过动作都写入 `APPROVE` 记录，`actionText` 业务可读。

必须先读：
- `doc/agent/manuscript-review-runtime-dual-fix_20260325-174900/reports/20260325_manuscript-review_runtime-dual-issue-report_v01.md`
- `doc/manuscript_review/R6.8_WP6-B3更新留痕执行基线.md`
- `doc/manuscript_review/R6.13_WP6-B3主控复审结论.md`

关键代码落点（优先）：
- `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewService.java`
- 若涉及流程配置/变量传递，再最小触达：
  - `Brain/brain-modules/brain-workflow/**`（仅必要处）
  - `Brain/script/sql/update/20260325_manuscript_review_workflow_seed_v01.sql`（如需条件跳转修复）

修复完成后，必须给 fresh 证据（不可复用旧 reviewId）：

1. 新建一条 `mr_certified` 样本，提交后证明：
- `flow_task` 不存在 `first-review-node` 待办（该样本）
- `flow_task` 直接出现二级待办（或等价引擎证据）
- `brain_manuscript_review.current_node_label` 与引擎节点一致

2. 在同一样本上跑 L1/L2（或对应真实通过链路）后证明：
- `brain_manuscript_review_history` 出现 `APPROVE`（一级通过）  
- `brain_manuscript_review_history` 出现 `APPROVE`（二级通过）  
- `actionText` 可读且节点语义正确

3. 报告输出结构固定为：
- 已确认
- 风险
- 待确认
- 推进方案
- fresh reviewId / fresh taskId / fresh history SQL 行

约束：
- 不进入 R7，不改前端页面，不做无关重构。
- 仅在必要边界内改动；每次结论必须由 fresh 命令/SQL支撑。

