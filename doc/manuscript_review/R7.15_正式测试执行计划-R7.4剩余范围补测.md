Owner: Codex
Task: manuscript_review 正式测试执行计划-R7.4剩余范围补测
Status: Draft
Updated: 2026-03-27 17:20:02 +08:00

# R7.15_正式测试执行计划-R7.4剩余范围补测

## 1. 文档定位

- 本文是 `R7.4_正式测试方案-全需求覆盖.md` 的本轮补测执行计划，仅覆盖当前尚未展开的正式用例。
- 本文不回退到旧聊天，不重做 `R7.10 / R7.12` 已完成的修复闭环，只承接你已确认的最新事实与当前仓库/运行态证据。
- 本文只输出计划，不写“已通过”或“已修复”结论；正式执行结果另写新一轮记录文档。

## 2. 本轮已确认事实

### 2.1 已确认业务边界

1. `R7.10` 已修复：
   - approval 成功后统一返回 `manuscript-review` 台账。
   - approval/detail/back 已按入口承接。
2. `R7.12` 已修复：
   - 退回后 edit 页可承接 detail data。
   - save 不推进 BPM。
   - resubmit 回到 `待一级审批`。
3. 已取证样本：
   - 普通 L1 审批样本：`2037447692543598594`
   - 退回/edit/save/resubmit 样本：`2037448049361428481`
   - 持证 L3 闭环样本：`2037451142417358849`

### 2.2 本轮新增前置核对

1. 运行环境：
   - `http://127.0.0.1:9999` -> `200`
   - `http://127.0.0.1:9888` -> `200`
2. 进程承载：
   - 前端：`node`
   - 后端：`C:\kuguaHome\env\java\jdk17\bin\java.exe`
3. 数据层：
   - `Redis` 当前无 `*manuscript*` 相关键。
   - `sys_role_menu` 已存在 `审校台账 / 我的待办 / 我的已办 / 去审批 / 再次提交` 等角色绑定。
   - `flow_task.form_path` 当前命中样本均为 `/manuscript/review/approval`。

### 2.3 当前静态高风险点

1. 前端 `detail-page-shell.vue` 当前只看到资源分组静态展示，未看到 `停用 / 预览 / 下载` 的显式交互承接。
2. 前端 `manuscript-review` API 当前仅暴露：
   - `resource` 新增
   - `resource/disable`
   - `video-mark` 新增
   未看到 `video-mark/disable` 对应前端 API 导出。
3. 详情页当前存在“视频播放器区”占位文案，但未看到多视频切换、标注点击跳时等显式前端交互代码。

说明：
- 上述三点目前只是“静态高风险”，不是本轮正式缺陷结论。
- 是否判定为缺陷，以浏览器实测 + 网络命中 + MySQL 留痕为准。

## 3. 本轮补测范围

### 3.1 纳入本轮

按 `R7.4` 剩余正式用例优先级，本轮纳入三组：

1. A 组：附件 / 视频 / 视频标注 / 资源停用预览下载
   - 重点覆盖：`用例-20` `用例-21` `用例-22` `用例-24` `用例-27` `用例-28` `用例-29` `用例-30` `用例-31` `用例-32` `用例-33` `用例-51` `用例-52` `用例-53` `用例-54` `用例-55`
2. B 组：待办 / 已办 / 菜单权限 / 按钮跳转
   - 重点覆盖：`用例-56` `用例-57` `用例-58` `用例-59`
3. C 组：持证链账号语义核对
   - 只核对 `mr_certified` 与 `mr_certified_initiator` 的角色与待办归属
   - 不扩成业务实现修复

### 3.2 明确不纳入本轮

1. `R7.10 / R7.12` 已完成闭环内容
2. 非 `R7.4` 剩余范围的新修复
3. 未经单独确认的 Playwright 安装
4. 数据库直接写入修复动作

## 4. 样本策略

### 4.1 复用样本

1. `2037447692543598594`
   - 用于普通链路审批中样本核对
   - 优先承接待办/查看/办理路径核对
2. `2037448049361428481`
   - 用于已退回后再次提交样本核对
   - 只在待办/按钮口径需要时做只读复用
3. `2037451142417358849`
   - 用于持证链已办/终态只读核对

### 4.2 需要新建的样本

本轮资源与视频用例不能只依赖现有三个样本，原因如下：

1. 已确认样本当前可读到外链，但不足以覆盖“附件 + 视频 + 视频标注 + 停用 + 预览 + 下载”完整集合。
2. `brain_manuscript_review_attachment` / `brain_manuscript_review_video_marker` 对上述三个样本未命中可直接复用的完整覆盖数据。

因此本轮执行前，计划新建最小资源样本：

1. `正式测试-资源视频补测-<timestamp>`
2. 同一时间戳下统一命名：
   - 附件名
   - 视频名
   - 标注内容
   - 外链标题

## 5. 执行顺序

### 5.1 阶段一：环境与账号入口核对

1. 复核 `9999/9888`
2. 以发起人、审批人、持证相关账号登录
3. 核对菜单可见性与基础入口可达

### 5.2 阶段二：A 组资源与视频补测

1. 新建资源样本
2. 覆盖附件 / 视频 / 外链一体提交
3. 覆盖视频标注新增、点击、停用
4. 覆盖详情页资源区停用、预览、下载
5. 覆盖历史中的资源下载与外链跳转

### 5.3 阶段三：B 组菜单与待办已办补测

1. 发起人角色核对 `待办` 菜单
2. 审批人角色核对 `我的待办 / 我的已办`
3. 发起人待办列表核对 `查看 / 撤销 / 编辑`
4. 审批人 `我的待办 / 我的已办` 核对 `查看 / 办理`
5. 核对 workflow 通用页跳转与 manuscript 页承接是否一致

### 5.4 阶段四：C 组账号语义核对

1. 只核对 `mr_certified` 与 `mr_certified_initiator` 的角色绑定与待办归属
2. 如发现语义不一致，只记录问题，不顺手改实现

## 6. 证据链要求

每个正式用例按以下三层取证：

1. 浏览器层
   - 菜单是否可见
   - 页面是否可达
   - 按钮是否存在
   - 点击后跳转是否正确
   - 必要截图
2. 网络层
   - 关键请求是否命中预期接口
   - 关键返回码/返回体是否正确
3. 数据层
   - 主表状态
   - 附件/外链/视频标注记录
   - 历史留痕
   - `flow_task`/已办待办归属

## 7. MCP 与 Skills 计划

### 7.1 MCP

1. `chrome-devtools`
   - 真实登录、菜单、页面、按钮、网络与截图
2. `mysql`
   - 主表、附件表、外链表、视频标注表、历史表、`flow_task`、菜单权限
3. `redis`
   - 只做相关键核对
4. `sequential_thinking`
   - 本轮测试策略与风险收敛

### 7.2 Skills

1. `using-superpowers`
2. `senior-qa-engineer`

## 8. 风险口径

1. 若资源页没有 `停用 / 预览 / 下载` 按钮，先记为正式失败候选，不在本轮验证中直接改代码。
2. 若视频播放器区仅为占位展示，视频切换/标注跳时按真实浏览器行为判定，不做“按设计推测通过”。
3. 若菜单权限已落库但菜单不显示，需区分：
   - 角色绑定问题
   - 登录态/缓存问题
   - 前端菜单渲染问题
4. 若需进一步核对持证链账号语义，只核对角色与待办归属，不顺手改业务实现。

## 9. 批准后输出物

批准执行后，本轮将新增正式执行记录到 `doc/manuscript_review/`，至少包含：

1. 执行环境
2. 实际执行用例清单
3. 每个用例的 PASS / FAIL / SKIP
4. 关键截图、网络命中、MySQL 证据
5. 新发现问题与复现步骤
6. 明确区分：
   - 已确认事实
   - 风险
   - 待确认项
   - 下一步建议

## 10. 当前请求

- 若游工确认本计划，我按本文范围开始正式补测。
- 未获确认前，我不执行正式浏览器用例，不改代码，不把静态高风险直接写成缺陷结论。
