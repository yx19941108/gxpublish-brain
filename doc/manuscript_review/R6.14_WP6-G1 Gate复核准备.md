Owner: Codex
Task: manuscript_review WP6-G1 Gate复核准备
Status: Draft
Updated: 2026-03-24 18:30 +08:00

# R6.14_WP6-G1 Gate复核准备

## 1. 范围与裁决边界

- 本文只复核 `WP6-G1` 是否可判定“第 6 轮后端可放行”。
- 本文不把结论外推为：
  - `repo 级冻结口径已整体闭合`
  - `R7 可启动`
  - `shared/BPM runtime blocker 已全部消除`
- 本文只使用以下材料：
  - `doc/manuscript_review/R6.8_WP6-B3更新留痕执行基线.md`
  - `doc/manuscript_review/R6.13_WP6-B3主控复审结论.md`
  - `doc/manuscript_review/R6.5_WP6-shared接缝主控审计结论.md`
  - `doc/manuscript_review/R6.6_WP6-B2完成记录.md`
  - `doc/manuscript_review/R5.7_第6-7轮执行基线.md`
  - `doc/manuscript_review/R5_测试方案及结论.md`
- 不参考旧失败实现，不以现存旧代码形态反推冻结契约。

## 2. Gate 判定标准

- `R5.7` 对 `WP6-G1` 的定义是：`Affected Files：无新增实现范围，只做 Gate 判断`。
- `R5.7` 同时明确：只有 `R5_测试方案及结论.md` 第六章 `P0` 集全部通过，才允许放行第 7 轮前端包；任何 `P0` 未过，仍停留在第 6 轮。
- `R6.13` 的边界已经写死：
  - `WP6-B3 最小整改` 复审通过，只表示可进入 `WP6-G1`
  - 不表示 `WP6-G1 已放行`
  - 不表示 `R7 可启动`
- `R6.5` 与 `R6.6` 仍保留的边界是：
  - `WP6 shared 接缝最小整改阶段有条件通过`
  - `冻结口径未整体闭合`
  - `R7 暂不放行`
  - `WP6-B2 private 能力补齐了一批关键缺口，但仍有 workflow/BPM runtime blocker`

## 3. Fresh 证据

### 3.1 JDK17 定向验证

执行命令：

```bash
cd Brain
set JAVA_HOME=C:\kuguaHome\env\java\jdk17
set PATH=%JAVA_HOME%\bin;%PATH%
mvn --% -pl brain-modules/brain-manuscript-review -am -Dtest=ManuscriptReviewServiceTest,ManuscriptReviewReadableServiceTest,ManuscriptReviewSharedApiMvcTest,ManuscriptReviewApiControllerTest,ManuscriptReviewFlowRoutingServiceTest,ManuscriptReviewDetailPermissionPolicyTest,ManuscriptReviewReadableVisibilityTest -Dsurefire.failIfNoSpecifiedTests=false -DskipTests=false test
```

2026-03-24 18:19 +08:00 结果：

- `ManuscriptReviewApiControllerTest`: `5/5`
- `ManuscriptReviewSharedApiMvcTest`: `4/4`
- `ManuscriptReviewDetailPermissionPolicyTest`: `1/1`
- `ManuscriptReviewFlowRoutingServiceTest`: `5/5`
- `ManuscriptReviewReadableServiceTest`: `5/5`
- `ManuscriptReviewReadableVisibilityTest`: `3/3`
- `ManuscriptReviewServiceTest`: `19/19`
- 合计：`42/42` 通过，`0` 失败，`0` 错误

### 3.2 B3 续承证据

- `R6.13` 已确认 `WP6-B3` 只修复了 `R6.10` 指出的资源/标注四类写动作 record-level 修改权守卫缺口。
- `R6.13` 已确认 `ManuscriptReviewReadableServiceTest` 仍能读出新增 history actionType。
- `R6.13` 已明确提醒：当前测试结论只足以支撑 `WP6-B3`，`WP6-G1` 仍需按 `R5` 第六章 `P0` 集单独判 gate。

## 4. R5 第六章 P0 集覆盖盘点

### 4.1 已有 fresh 覆盖

| 用例 | 当前 fresh 证据 | 判断 |
| --- | --- | --- |
| `DR-01` | `ManuscriptReviewServiceTest.shouldGenerateCodeAndWriteFirstAndLatestSubmitTimeWhenSubmitStarts` | 已覆盖 |
| `DR-02` | `ManuscriptReviewServiceTest.shouldRejectCreateWhenExternalCodeDuplicated` | 已覆盖 |
| `FR-02` | `ManuscriptReviewFlowRoutingServiceTest.shouldSkipLevelOneAndWriteSystemHistoryWhenInitiatorIsCertified` | 已覆盖 |
| `FR-03` | `ManuscriptReviewFlowRoutingServiceTest.shouldRerouteReturnedRecordOnResubmit` | 已覆盖 |
| `PM-01` | `ManuscriptReviewDetailPermissionPolicyTest` + `ManuscriptReviewReadableVisibilityTest` | 已覆盖 |
| `RL-01` | `ManuscriptReviewServiceTest.shouldAddVideoResourceWithoutReusingExternalLinkSemantics` + `shouldWriteExternalLinkHistoryWhenAddingResource` | 已覆盖 |
| `RL-02` | `ManuscriptReviewServiceTest.shouldDisableResourceByFlagInsteadOfDeletingRow` + `ManuscriptReviewReadableServiceTest.shouldAssembleReadableDetailWithFrozenFieldsAndCurrentResources` | 已覆盖 |
| `API-01` | `ManuscriptReviewSharedApiMvcTest.shouldReturnFrozenLedgerPageWithCodeBasedFiltersForApi01` | 已覆盖 |
| `API-02` | `ManuscriptReviewSharedApiMvcTest.shouldReturnFrozenDetailVoFromNewWriteSideFieldsForApi02` | 已覆盖 |
| `API-03` | `ManuscriptReviewSharedApiMvcTest.shouldExposeSeparatedNonStubWriteEndpointsForApi03` | 已覆盖 |
| `DS-01` | `ManuscriptReviewApiControllerTest.shouldSerializeFrozenLedgerItemWithoutPermissionLeakage` + `shouldSerializeFrozenDetailContractWithResourceUrlAndPermissionMatrix` | 已覆盖 |
| `NE-01` | `ManuscriptReviewFlowRoutingServiceTest.shouldRejectSubmitWhenApprovalChainConfigMissing` | 已覆盖 |
| `NE-02` | `ManuscriptReviewFlowRoutingServiceTest.shouldRejectSubmitWhenCertifiedRoleCannotBeResolved` | 已覆盖 |

### 4.2 只有部分覆盖，当前证据不足以下“已过 Gate”结论

| 用例 | 当前已有证据 | 仍缺什么 | 判断 |
| --- | --- | --- | --- |
| `DR-06` | `shouldRejectSubmitWhenNoEffectiveResourceExists` 证明“无有效资源时拦截”；`shouldAddVideoResourceWithoutReusingExternalLinkSemantics` 证明视频资源可入库 | 未证明“仅普通附件提交可过 / 仅外链提交可过 / 仅视频提交按附件满足可过”三类正向样本 | 部分覆盖 |
| `FR-01` | `shouldRouteSubmitToLevelOneForNormalInitiator` 证明普通发起人首提路由到一级 | 未证明“一级 -> 二级 -> 三级 -> 完成”的 runtime 闭环 | 部分覆盖 |
| `FR-04` | `shouldRerouteReturnedRecordOnResubmit` + 权限策略测试可支撑“退回后可改可重提” | 未证明“任意审批节点退回”样本闭环 | 部分覆盖 |
| `FR-07` | 权限策略与详情读模型证明“当前审批人可改、可去审批、且无 shortcut” | 未证明“修改保存本身不产生审批结果，审批只能经 BPM 办理页完成”的 fresh 运行证据 | 部分覆盖 |
| `FR-08` | `API-03` 明确 `update` 与 `resubmit` 分接口，权限策略也关闭 shortcut | 未证明“修改页保存后不直接重提”的显式负向拦截证据 | 部分覆盖 |
| `PM-06` | 详情返回 `permissionMatrix`、列表不下沉权限布尔值已有 controller/MVC 证据 | 未证明消费侧完全不本地猜测 `isCurrentApprover`；`R5` 还把该项与 BPM 跳转前置条件绑定 | 部分覆盖 |
| `TN-04` | `shouldRejectVideoMarkWhenDurationMissing` 覆盖“总时长未识别”分支 | 未覆盖“结束时间早于开始”“超出总时长”两个分支 | 部分覆盖 |
| `NE-03` | 现有 fresh 用例只覆盖“外部稿件编号重复”“作者分隔符错误” | 未覆盖“非法 URL / 重复 URL / 长度超限”分支 | 部分覆盖 |
| `NE-05` | 当前只覆盖“视频总时长未识别” | 未覆盖“时间越界”分支 | 部分覆盖 |
| `NE-06` | 已覆盖“越权访问详情”“资源/标注四类写动作越权” | 未覆盖“越权修改 / 越权去审批 / 非法状态下重复提交、撤销、再次提交” | 部分覆盖 |

### 4.3 当前未见对应 fresh 覆盖

| 用例 | 缺口说明 | 判断 |
| --- | --- | --- |
| `DR-05` | 未见标题/正文/说明/外链标题/URL 长度边界 fresh 用例 | 未覆盖 |
| `DR-07` | 未见外链协议合法性与同流程 URL 重复 fresh 用例 | 未覆盖 |
| `FR-05` | 未见“发起人审批中撤销后状态变已取消、当前节点变流程已取消、时间线记撤销、后续不再流转”的 fresh 用例 | 未覆盖 |
| `FR-06` | 未见“三级审批驳回终止当前流程”的 fresh 业务闭环用例；当前只有 rejected readable 可见性证据 | 未覆盖 |
| `TN-03` | 未见非法时间格式（如 `3:7`、`abc`、`99:99:99`）fresh 拦截用例 | 未覆盖 |

## 5. 当前结论

### 5.1 Gate 结论

- 结论：`不通过`

### 5.2 判定原因

- `WP6-B3` 的 fresh 绿证据只能证明：
  - 资源/标注四类写动作的越权守卫已补齐
  - 更新留痕闭环已补齐
  - 详情读链仍能看到新增 history
- 但 `WP6-G1` 的判定门槛不是“`WP6-B3` 已修好”，而是“`R5` 第六章 `P0` 集已经足够稳定”。
- 按本轮 fresh 证据盘点，当前至少存在：
  - `5` 项 `未覆盖`
  - `10` 项 `部分覆盖`
- `R6.6` 仍保留 `workflow/BPM runtime blocker`，且当前 fresh 验证没有新增证据消除 `FR-05 / FR-06 / FR-07` 这类依赖流程运行态的缺口。

### 5.3 结论边界

- 当前只可上报为：
  - `WP6-B3 最小整改复审通过，可进入 WP6-G1`
  - `当前 WP6-G1 Gate 证据不足，未达放行`
- 当前不可上报为：
  - `第6轮后端已放行`
  - `repo 级冻结口径已闭合`
  - `R7 可启动`

## 6. 若要转成可放行，最小补证/最小整改项

1. 补 `DR-05 / DR-07 / NE-03` 的定向后端测试，至少锁住长度上限、非法 URL、重复 URL 三组缺口。
2. 补 `TN-03 / TN-04 / NE-05` 的时间校验测试，至少覆盖非法格式、结束早于开始、超出总时长。
3. 补 `FR-05 / FR-06` 的后端闭环测试，至少锁住“撤销 -> 已取消”“三级驳回 -> 已驳回”两条状态/节点/时间线证据。
4. 补 `NE-06` 与 `FR-07 / FR-08 / PM-06` 的最小证据，至少覆盖：
   - 越权修改
   - 越权去审批
   - 非法状态下重复提交/撤销/再次提交
   - “修改保存”和“去审批/再次提交”确实分步

## 7. 回滚边界

- 本轮未新增 SQL、workflow、frontend、shared 输出形态或业务实现。
- 本轮只新增 Gate 复核准备文档。
- 若需回滚，本轮只删除 `doc/manuscript_review/R6.14_WP6-G1 Gate复核准备.md`，无需回退代码。
