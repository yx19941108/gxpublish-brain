Owner: Codex
Task: manuscript_review R8 第一轮修复静态验证记录
Status: In Progress
Updated: 2026-03-30 13:55:00 +08:00

# R8.4.3_第一轮修复静态验证记录

## 1. 适用范围

本文记录 `gxpublish-brain / manuscript_review / R8 第一轮修复` 在本轮代码落地后的静态验证状态。

本文不替代：

1. `R8.0_问题排查续工交接.md`
2. `R8.2_问题池统一修复方案.md`
3. `R8.3_问题池分轮修复执行基线.md`
4. `R8.2.5_问题池统一回归测试用例.md`
5. `R8.4_第一轮修复执行提示词.md`
6. `R8.4.1_第一轮修复执行checkpoint.md`
7. `R8.4.2_第一轮修复续工提示词.md`

## 2. 本轮硬边界

本轮仍只处理第一轮问题：

1. 问题 9：新增权限未收紧
2. 问题 3：审校台账分页与数据源口径不一致
3. 问题 7：history 主文案不完整

未进入：

1. 问题 1 / 4 / 5 / 6 / 8
2. 问题 2 代码修复

## 3. 已完成代码变更

### 3.1 问题 9

已落地：

1. `plus-ui-ts/src/views/manuscript-review/index.vue`
   - 台账页新增入口继续按 `canCreate = checkPermi(['manuscript:review:submit'])` 收紧
2. `plus-ui-ts/src/views/manuscript-review/my-document.vue`
   - 我发起页补齐 `canCreate` 显隐与点击前置拦截
3. `plus-ui-ts/src/views/manuscript-review/form.vue`
   - create 模式进入时补权限前置校验
   - 无权发起时提示并 `router.replace('/manuscript/review')`
4. `plus-ui-ts/src/router/index.ts`
   - `/manuscript/review/form` 补 `permissions`
   - 明确声明 `submit / edit / resubmit / goApprove`
5. `Brain/.../ManuscriptReviewApiController.java`
   - `create / update / submitAndFlowStart / resubmit` 均补 `@SaCheckPermission`
6. `Brain/.../ManuscriptReviewService.java`
   - 新增发起权限二次校验 `ensureCurrentUserCanInitiate(...)`
   - 提交链路不再允许无发起权限用户越权发起
7. `Brain/script/sql/update/manuscript_review/20260330_manuscript_review_first_round_permission_hardening_v01.sql`
   - 新增第一轮菜单收紧脚本
   - 从审批人角色解绑 `60007 审校表单`

### 3.2 问题 3

已落地：

1. `Brain/.../ManuscriptReviewRecordMapper.java`
   - 新增 `customSelectVisibleLedgerPage(...)`
2. `Brain/.../resources/mapper/manuscript/review/ManuscriptReviewRecordMapper.xml`
   - 台账改为数据库内查询
   - 可见性按以下三类来源并集实现：
     - 历史参与人来源：`history.actor_user_id = 当前用户`
     - 发起人来源：当前用户拥有发起侧角色时命中 `initiator_user_id = 当前用户`
     - 审批人来源：当前用户拥有审批角色时命中对应 `current_node_status`
   - 查询在 SQL 内完成筛选、排序、分页
3. `Brain/.../ManuscriptReviewReadableService.java`
   - `listLedger(...)` 不再全量查主表 / history 后 Java 过滤分页
   - 改为构造作用域参数后直接调用自定义分页 SQL

### 3.3 问题 7

已落地：

1. `Brain/.../ManuscriptReviewService.java`
   - `insertSystemHistory(...)` 补齐 `actor_user_id / actor_name`
   - `insertWorkflowHistory(...)` 补齐 `actor_user_id / actor_name`
   - `BACK / APPROVE / REJECT / FINISH / SKIP_LEVEL_1` 主文案统一补成完整自然语言
   - 不再保留“流程已退回发起人处理。”这类裸文案

## 4. 静态验证结果

### 4.1 前端定向回归

命令：

```powershell
& 'C:\kuguaHome\tools\devTools\nvm\v20.19.6\pnpm.cmd' exec vitest run src/views/manuscript-review/access-control.spec.ts src/views/manuscript-review/route-shell.spec.ts src/views/manuscript-review/ledger-layout.spec.ts
```

结果：

1. `access-control.spec.ts` 3/3 通过
2. `route-shell.spec.ts` 1/1 通过
3. `ledger-layout.spec.ts` 5/5 通过

### 4.2 后端定向回归

命令：

```powershell
$env:JAVA_HOME='C:\kuguaHome\env\java\jdk17'
$env:Path='C:\kuguaHome\env\java\jdk17\bin;' + $env:Path
mvn -pl brain-modules/brain-manuscript-review -DskipTests=false "-Dtest=ManuscriptReviewApiControllerTest,ManuscriptReviewReadableFirstRoundGuardrailTest,ManuscriptReviewFirstRoundGuardrailTest,ManuscriptReviewReadableVisibilityTest" test
```

结果：

1. `ManuscriptReviewApiControllerTest` 6/6 通过
2. `ManuscriptReviewFirstRoundGuardrailTest` 6/6 通过
3. `ManuscriptReviewReadableFirstRoundGuardrailTest` 1/1 通过
4. `ManuscriptReviewReadableVisibilityTest` 6/6 通过
5. 合计 19/19 通过

### 4.3 前端构建验证

命令：

```powershell
& 'C:\kuguaHome\tools\devTools\nvm\v20.19.6\pnpm.cmd' build:prod
```

结果：

1. 构建通过
2. 本轮前端改动未引入构建错误

## 5. 当前不能宣称已完成的项

以下证据本轮尚未拿到，因此不能把第一轮直接宣称为已验收或可进下一轮：

1. 浏览器级证据：
   - `9999` 未监听
2. 接口级证据：
   - `9888` 未监听
   - 尚未拿到真实 HTTP 响应证据
3. 数据级证据：
   - 尚未对真实库做台账并集结果和 history actor 留痕的 SQL 复核

## 6. 当前端口状态

本轮核对结果：

1. `PORT_9888=NOT_LISTEN`
2. `PORT_9999=NOT_LISTEN`

因此本轮只能报告：

1. 代码实现已落地
2. 静态测试与构建验证通过
3. 运行态浏览器 / 接口 / 数据证据待下一步补齐

## 7. 建议下一步

从本文件继续时，固定顺序如下：

1. 启动或接管最新前后端运行态
2. 按 `R8.2.5` 执行第一轮对应回归
   - 问题 9：`TC-26 ~ TC-30`
   - 问题 3：`TC-05 ~ TC-10`
   - 问题 7：`TC-18 ~ TC-21`
3. 补齐：
   - 浏览器证据
   - 接口证据
   - 数据证据
4. 再做第一轮 gate 判断

## 8. 用户边界外文件

仍不得触碰：

1. `plus-ui-ts/.eslintrc-auto-import.json`
2. `test_qa_flow.py`
