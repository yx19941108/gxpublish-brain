Owner: Codex
Task: manuscript_review R8 第三轮启用能力扩展轮下一轮提示词
Status: Ready
Updated: 2026-03-31 09:33:24 +08:00

# R8.6.2_第三轮启用能力扩展轮下一轮提示词

```text
你现在接手的是 `gxpublish-brain` 的 `manuscript_review` R8 第三轮启用能力扩展轮运行态续工，不要重新发明范围，不回退第一轮/第二轮，不把问题 2 拉进来，也不要再比较替代方案。

先读这些文件，不要跳过：
1. `doc/manuscript_review/R8.2_问题池统一修复方案.md`
2. `doc/manuscript_review/R8.2.5_问题池统一回归测试用例.md`
3. `doc/manuscript_review/R8.3_问题池分轮修复执行基线.md`
4. `doc/manuscript_review/R8.4.5_第一轮运行态回归记录.md`
5. `doc/manuscript_review/R8.5.4_第二轮预览鉴权稳方案运行态回归记录.md`
6. `doc/manuscript_review/R8.6.0_第三轮启用能力扩展轮新会话提示词.md`
7. `doc/manuscript_review/R8.6.1_第三轮启用能力扩展轮阶段交接.md`
8. `doc/manuscript_review/R8.6.2_第三轮启用能力扩展轮下一轮提示词.md`

执行裁决规则：
1. 分轮与实现边界以 `R8.3` 为准
2. 第三轮硬边界以 `R8.6.0` 为准
3. 当前阶段代码/静态验证进展以 `R8.6.1` 为准
4. 回归口径以 `R8.2.5` 为准

当前必须承接的已冻结事实：
1. 第三轮只做问题 `6`
2. 所有停用必须落历史
3. 所有启用仅在历史中做
4. 当前有效资源区与当前有效标注列表仍只展示有效项
5. 不新增“已停用资源区”
6. 不把已停用项混回当前有效资源区
7. 前端停用/启用按钮当且仅当 `permissionMatrix.canEdit = true` 时显示
8. 后端 service 继续用 `ensureCanModify(...)` 做等价权限兜底

当前代码已完成的点：
1. 后端已有：
   - `PUT /workflow/manuscript-review/resource/enable`
   - `PUT /workflow/manuscript-review/video-mark/enable`
2. history action 已新增：
   - `RESOURCE_ENABLE`
   - `VIDEO_MARK_ENABLE`
3. 读侧时间线已保留 enable 所需结构化目标：
   - `eventCode`
   - `relatedResourceId`
   - `relatedResourceType`
4. 前端详情页已在“停用历史项 + canEdit=true”时渲染 enable 动作
5. 启用成功后会重新 `fetchDetail()`

当前已通过的定向验证：
1. 后端：
   - `ManuscriptReviewApiControllerTest#shouldExposeThirdRoundEnableEndpoints`
   - `ManuscriptReviewReadableServiceTest#shouldExposeNewHistoryActionTypesInTimelineItems`
   - `ManuscriptReviewThirdRoundServiceTest#shouldEnableDisabledAttachmentAndAppendNewHistory`
   - `ManuscriptReviewThirdRoundServiceTest#shouldEnableDisabledVideoMarkAndAppendNewHistory`
2. 前端：
   - `src/views/manuscript-review/detail-layout.spec.ts`
   - `src/views/manuscript-review/second-round-contract.spec.ts`
   - `src/views/manuscript-review/third-round-history-contract.spec.ts`

当前仍未完成的，是第三轮运行态最小回归，不要把“单测已绿”说成“第三轮 gate 已通过”。

下一轮只做这些事：
1. 先确认 `9999/9888` ready
2. 用浏览器登录并找到 fresh 样本
3. 若当前登录账号对样本 `canEdit=false`，立刻切到当前可编辑账号，不在只读账号上空跑
4. 执行第三轮最小回归：
   - `TC-15`
   - `TC-16`
   - `TC-17`
5. 如涉及 history actor/相邻能力，补：
   - `TC-18`
   - `TC-13`
   - `TC-14`
   - `TC-23`
6. 结果必须按：
   - 浏览器证据
   - 接口证据
   - 数据证据
   分开汇报

当前已知运行态线索：
1. 服务当前 ready：
   - `http://127.0.0.1:9999/ -> 200`
   - `http://127.0.0.1:9888/ -> 200`
2. 发起人账号可登录：
   - `mr_initiator / 666666 / 1234`
3. 当前台账 fresh 样本可见：
   - `reviewId=2038771188202020865`
4. 该样本 detail 已有：
   - 多个当前有效附件/视频
   - 多条 `VIDEO_MARK_ADD`
   - 至少一条 `VIDEO_MARK_DISABLE`
5. 但用 `mr_initiator` 查看该样本时：
   - `permissionMatrix.canEdit = false`
   - 这与冻结规则一致，说明下一轮需要切到当前可编辑账号继续验证 enable 按钮

本轮禁止事项：
1. 不得重开方案讨论
2. 不得新增“已停用资源区”
3. 不得把 enable 挂到当前有效资源区
4. 不得把问题 `2` 扩成代码修复
5. 不得触碰 `plus-ui-ts/.eslintrc-auto-import.json`
6. 不得触碰 `test_qa_flow.py`
7. 不得把定向单测通过误报成第三轮 gate 已通过

开始时先给：
1. 已确认
2. 风险
3. 待确认
4. 推进方案

然后给本轮运行态执行计划；每一步必须包含：
1. Goal
2. Affected Files
3. Risk
4. Verification
5. Rollback
```
