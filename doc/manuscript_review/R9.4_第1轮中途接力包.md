Owner: Codex
Task: manuscript_review R9 round-1 handoff
Status: In Progress
Updated: 2026-03-31 16:03:50 +08:00

# R9.4_第1轮中途接力包

## 1. 本工件定位

本文件用于在 `R9` 第 1 轮执行中途停止时，给下一轮会话提供可直接接手的上下文基线。

本轮范围仍然锁死为 4 个问题：
1. 问题 3：流程类型冻结
2. 问题 5：资源区不再展示技术链接
3. 问题 6：资源区补齐操作人、操作时间、文件大小
4. 问题 12：主动退出登录不再串页

明确不做：
1. 问题 1、2、4、7、8、9、10、11
2. 问题 13 首页设计或实现
3. 旧 `brain_manuscript_review_history` 数据修补
4. 与 `R9` 无关的历史返修

## 2. 已确认

### 2.1 文档与真实代码落点已核对

1. 问题 3：
   - `Brain/.../ManuscriptReviewService.java:update(...)` 原始实现会使用 `command.getProcessType()` 重新写主表字段，并重算 `flowCode`
   - `plus-ui-ts/src/views/manuscript-review/form.vue` 修改页中的流程类型原本仍是可编辑 `el-select`
2. 问题 5：
   - `plus-ui-ts/src/views/manuscript-review/detail.contract.ts` 原始实现把附件/视频 `resourceUrl` 映射到了 `note`
   - `plus-ui-ts/src/views/manuscript-review/detail-page-shell.vue` 原始实现会直接渲染 `item.note`
   - `plus-ui-ts/src/views/manuscript-review/components/formState.ts` 修改页“当前已入库资源”原始实现会把 `externalUrl ?? resourceUrl` 放进 `note`
3. 问题 6：
   - `Brain/.../ManuscriptReviewDetailResponse.java` 原始 `ResourceItemVO` 只有 `displayName/externalUrl/createdTime/resourceUrl`
   - `Brain/.../ManuscriptReviewReadableService.java` 原始读侧组装没有输出 `operatorName/operatorTime/fileSize*`
   - `VideoMarkItemVO` 原始实现也没有 `operatorName/operatorTime`
4. 问题 12：
   - `plus-ui-ts/src/layout/components/Navbar.vue` 原始主动退出会拼 `login?redirect=<current fullPath>`
   - `plus-ui-ts/src/views/login.vue` 原始登录成功会优先吃 `query.redirect`

### 2.2 已经落下的代码修改

当前工作树内，以下文件已被修改但尚未完成整轮闭环：

1. `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewService.java`
2. `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewReadableService.java`
3. `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/controller/response/ManuscriptReviewDetailResponse.java`
4. `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/domain/entity/ManuscriptReviewSystemUserEntity.java`
5. `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewServiceTest.java`
6. `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewReadableServiceTest.java`

非本轮边界内但当前工作树里还存在一个无关改动：
1. `plus-ui-ts/.eslintrc-auto-import.json`

## 3. 已拿到的测试证据

### 3.1 定向红灯已拿到

已执行命令：

```powershell
$env:JAVA_HOME='C:\kuguaHome\env\java\jdk17'
$env:Path='C:\kuguaHome\env\java\jdk17\bin;' + $env:Path
& 'C:\kuguaHome\tools\devTools\apache-maven-3.9.12\bin\mvn.cmd' `
  -pl brain-modules/brain-manuscript-review `
  '-Dtest=ManuscriptReviewServiceTest#shouldRejectUpdateWhenProcessTypeChangesAfterCreation+shouldKeepExistingFlowCodeWhenUpdateSavesMainForm,ManuscriptReviewReadableServiceTest#shouldAssembleReadableDetailWithFrozenFieldsAndCurrentResources' `
  -DskipTests=false test
```

目标红灯结论：
1. `shouldRejectUpdateWhenProcessTypeChangesAfterCreation`
   - 现状：没有抛异常
   - 结论：后端仍允许修改请求带不同 `processType`
2. `shouldKeepExistingFlowCodeWhenUpdateSavesMainForm`
   - 现状：得到 `manuscript_review_audit_flow`
   - 期望：保持 `manuscript_review_custom_locked_flow`
   - 结论：`update(...)` 仍在重写 `flowCode`
3. `shouldAssembleReadableDetailWithFrozenFieldsAndCurrentResources`
   - 现状：`operatorName/operatorTime/fileSize*` 至少有字段仍为 `null`
   - 结论：详情读侧资源元信息尚未闭环

### 3.2 中间编译阻塞已处理过

已处理的非目标阻塞：
1. `ResourceItemVO/VideoMarkItemVO` 扩字段后，旧 controller test 构造器调用断裂
2. 已通过给 DTO 增加兼容构造器消除了这一类编译阻塞

## 4. 当前未完成状态

### 4.1 后端

1. `ManuscriptReviewService.java`
   - 已改：`update(...)` 增加了 `assertProcessTypeImmutable(...)`
   - 已改：`applyWriteFields(...)` 改为使用持久化 `processType`
   - 已改：`entity.setFlowCode(existing.getFlowCode())`
   - 待验证：该改动是否让 2 个目标单测转绿
2. `ManuscriptReviewReadableService.java`
   - 已改：`getDetail(...)` 已开始构建 `operatorNameMap`
   - 已改：`toAttachmentItem/toVideoItem/toExternalLinkItem/toVideoMarkItem` 已扩成带 `operatorNameMap` 版本
   - 已改：新增了 `resolveOperatorNames/resolveOperatorName/resolveSystemUserDisplayName/formatFileSize`
   - 待验证：编译通过后，目标读侧测试是否转绿
3. `ManuscriptReviewSystemUserEntity.java`
   - 已改：补了 `userName/nickName`
   - 目的：让读侧可把 `createBy` 解析成可读账号名，而不是纯数字 ID

### 4.2 前端

当前还没有进入前端正式实现。也就是说：
1. `form.vue` 还没冻结“流程类型”只读
2. `detail.contract.ts` 还没把资源区从 `note=resourceUrl` 改成业务元信息
3. `detail-page-shell.vue` 还没把资源元信息渲染从单行 `note` 改成元信息行
4. `components/formState.ts` 还没改修改页“已入库资源区”的展示结构
5. `Navbar.vue/login.vue` 还没开始修主动退出不串页

## 5. 风险

1. 当前已经是“后端半完成 + 测试半完成 + 前端未开始”的状态，继续在原会话推进容易漏掉联动面
2. `ReadableService.java` 这次改动同时影响：
   - `getDetail`
   - `getResourceItem`
   - `getVideoMarkItem`
   下一轮接手时要先确认是否还有旧调用点没补齐
3. `plus-ui-ts/.eslintrc-auto-import.json` 不是本轮产物，下一轮不要误处理

## 6. 下一轮建议执行顺序

1. 先读：
   - `doc/manuscript_review/R9.0_问题池汇总与修复准备.md`
   - `doc/manuscript_review/R9.1_问题池修复方案与影响评估.md`
   - `doc/manuscript_review/R9.2_问题池详细测试用例.md`
   - `doc/manuscript_review/R9.3_问题池会话执行基线.md`
   - 本文件 `doc/manuscript_review/R9.4_第1轮中途接力包.md`
2. 先只做后端收口：
   - 修完 `ReadableService.java` 剩余编译/调用点
   - 回跑 3 个定向后端测试
3. 后端定向绿后，再做前端：
   - `form.vue`
   - `detail.contract.ts`
   - `detail-page-shell.vue`
   - `components/formState.ts`
   - `Navbar.vue`
   - `login.vue`
4. 前端优先跑单测；浏览器验证最后单独做，不要和静态验证混说

## 7. 下一轮启动提示词

可直接使用下面这段：

```text
你现在在仓库 C:\kuguaHome\project\gxpublish-brain 中继续推进 manuscript_review 的 R9 第 1 轮。

先读以下文档，再开始任何代码分析或修改：
1. doc/manuscript_review/R9.0_问题池汇总与修复准备.md
2. doc/manuscript_review/R9.1_问题池修复方案与影响评估.md
3. doc/manuscript_review/R9.2_问题池详细测试用例.md
4. doc/manuscript_review/R9.3_问题池会话执行基线.md
5. doc/manuscript_review/R9.4_第1轮中途接力包.md
6. AGENTS.md

本轮范围仍锁死为 4 个问题：
1. 问题 3：流程类型冻结
2. 问题 5：资源区不再展示技术链接
3. 问题 6：资源区补齐操作人、操作时间、文件大小
4. 问题 12：主动退出登录不再串页

先不要扩到第 2/3 轮。

先做两件事：
1. 收口当前后端半完成状态，修通编译与调用点
2. 回跑这 3 个定向后端测试：
   - ManuscriptReviewServiceTest#shouldRejectUpdateWhenProcessTypeChangesAfterCreation
   - ManuscriptReviewServiceTest#shouldKeepExistingFlowCodeWhenUpdateSavesMainForm
   - ManuscriptReviewReadableServiceTest#shouldAssembleReadableDetailWithFrozenFieldsAndCurrentResources

后端 3 个定向测试转绿后，再开始前端实现：
1. form.vue 冻结 edit mode 的流程类型
2. detail.contract.ts / detail-page-shell.vue / components/formState.ts 改为展示业务元信息，不展示技术链接
3. Navbar.vue / login.vue 修主动退出不串页，并保留未登录拦截后的正常 redirect

汇报格式继续使用：
- 已确认
- 风险
- 待确认
- 推进方案

结束时必须给出：
- 变更结果
- 验证结果
- 残留风险
- 产物路径
- 下一轮是否可进入
```

## 8. 产物路径

1. 当前接力工件：
   - `doc/manuscript_review/R9.4_第1轮中途接力包.md`
