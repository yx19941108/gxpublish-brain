Owner: Codex
Task: manuscript_review 第二队列回归完成
Status: Passed
Updated: 2026-03-29 17:00:00 +08:00

# R7.30_正式测试执行记录-第二队列回归完成

## 1. 本轮目标

本轮目标是在完成以下策略切换后，重新完成第二队列回归：

1. 通用 workflow 页停止承载 manuscript-review 特判
2. manuscript-review 使用自己的专属任务页：
   - `/manuscript/review/my-document`
   - `/manuscript/review/task-waiting`
   - `/manuscript/review/task-finish`
3. 相关角色菜单改为指向专属页
4. 第二队列目标用例完成当前态回归并正式落盘

本轮回归覆盖的第二队列用例：

1. `用例-37`
2. `用例-39`
3. `用例-41`
4. `用例-42`
5. `用例-43`
6. `用例-44`
7. `用例-45`
8. `用例-46`
9. `用例-47`
10. `用例-48`
11. `用例-58`

## 2. 运行态

本轮前后端由本次回归过程重新启动并确认：

1. 前端：
   - `npm run dev -- --host 127.0.0.1 --port 9999`
   - `http://127.0.0.1:9999/` 返回 `200`
2. 后端：
   - `mvn -pl brain-admin -am package -DskipTests`
   - `java -jar brain-admin/target/brain-admin.jar --spring.profiles.active=dev`
   - `9888` 端口监听成功
3. 后端启动后对 `http://127.0.0.1:9888/actuator/health` 的访问返回 `401`
   - 说明服务已启动且鉴权生效，不是空端口状态

补充说明：

1. 启动日志中存在 `snail-job` 连接失败告警
2. 该告警不影响本轮 manuscript-review 第二队列回归

## 3. 本轮执行的验证

### 3.1 后端单测

已通过：

1. `mvn -pl brain-modules/brain-manuscript-review -am -DskipTests=false -Dtest=ManuscriptReviewServiceTest,ManuscriptReviewReadableServiceTest test`
2. 结果：
   - `45` 个测试全部通过

### 3.2 前端单测

已通过：

1. `npm exec vitest run src/views/manuscript-review/detail-navigation.spec.ts src/views/manuscript-review/task-center.spec.ts src/views/manuscript-review/detail-readable-display.spec.ts`
2. 结果：
   - `3` 个测试文件
   - `22` 个测试全部通过

### 3.3 菜单验证

已通过：

1. `mr_initiator` 命中：
   - `审校台账`
   - `我发起的`
2. `mr_approver_l1` 命中：
   - `审校台账`
   - `我的待办`
   - `我的已办`
3. 组件指向均为 manuscript-review 专属页：
   - `manuscript-review/my-document`
   - `manuscript-review/task-waiting`
   - `manuscript-review/task-finish`

### 3.4 浏览器与治理脚本

本轮实际采用的运行态证据分为四组：

1. `r7_28_queue2_browser_regression.json`
   - 本轮刷新了 `用例-37 / 39` 证据
2. `r7_28_queue2_governance_check.json`
   - 本轮刷新了 `用例-43 / 44 / 45` 证据
3. `r7_29_task_center_smoke.json`
   - 本轮刷新了 `用例-41 / 46 / 47 / 48 / 58` 证据
4. `r7_29_case42_termination_check.json`
   - 本轮新增 `用例-42` 当前态证据

## 4. 工件清单

本轮关键工件位于：

1. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_28_queue2_browser_regression.json`
2. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_28_queue2_governance_check.json`
3. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_29_task_center_smoke.json`
4. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_29_case42_termination_check.json`
5. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_28_case37_case39_detail_before.png`
6. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_29_case42_terminated.png`
7. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_28_case46_case48_level2.png`
8. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_28_case47_case48_level3.png`
9. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_28_case41_cancel.png`
10. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_28_case58_my_document.png`

## 5. 逐项回归结论

### 5.1 用例-37 审批人可审即可改

结论：

1. 通过

本轮证据：

1. 详情页动作栏为：
   - `修改 / 去审批 / 返回`
2. 当前截图工件：
   - `r7_28_case37_case39_detail_before.png`

### 5.2 用例-39 修改与去审批必须分两步

结论：

1. 通过

本轮证据：

1. 保存后流程仍停留：
   - `审批中`
   - `待一级审批`
2. 保存只新增：
   - `UPDATE`
3. 再去审批后流程推进到：
   - `待二级审批`
4. 推进后新增：
   - `APPROVE`

### 5.3 用例-41 发起人审批中撤销

结论：

1. 通过

本轮证据：

1. 专属 `我发起的` 页面审批中样本按钮为：
   - `查看 / 撤销`
2. 撤销后时间线包含：
   - `CREATE`
   - `CANCEL`

### 5.4 用例-42 三级审批驳回终止

结论：

1. 通过

本轮证据：

1. 三级审批弹窗按钮为：
   - `提交 / 终止 / 退回 / 取消`
2. 点击 `终止` 后终态为：
   - `businessStatusLabel = 已驳回`
   - `currentNodeLabel = 流程已驳回`
3. 时间线存在：
   - `eventCode = REJECT`
   - `eventText = 三级审批驳回，当前流程已终止。`

### 5.5 用例-43 审批链配置存在且完整

结论：

1. 通过

本轮证据：

1. `AUDIT` 配置存在并完整
2. `PROOFREAD` 配置存在并完整
3. 一级、二级、三级审批角色均存在有效成员

### 5.6 用例-44 缺审批链配置时提交被拦截

结论：

1. 通过

本轮证据：

1. 临时停用 `PROOFREAD` 配置后提交返回：
   - `code = 500`
   - `msg = 审校流程审批链配置缺失`
2. 还原配置后提交恢复：
   - `code = 200`
   - `businessStatusLabel = 审批中`

### 5.7 用例-45 缺有效审批成员时提交被拦截

结论：

1. 通过

本轮证据：

1. 临时停用三级审批角色成员后提交返回：
   - `code = 500`
   - `msg = 待三级审批审批角色无有效成员`
2. 还原成员后提交恢复：
   - `code = 200`
   - `businessStatusLabel = 审批中`

### 5.8 用例-46 一级、二级审批模态框按钮集合正确

结论：

1. 通过

本轮证据：

1. 一级审批弹窗按钮为：
   - `提交 / 退回 / 取消`
2. 二级审批弹窗按钮为：
   - `提交 / 退回 / 取消`

### 5.9 用例-47 三级审批模态框按钮集合正确

结论：

1. 通过

本轮证据：

1. 三级审批弹窗按钮为：
   - `提交 / 终止 / 退回 / 取消`

### 5.10 用例-48 退回或驳回目标节点只能选择申请人

结论：

1. 通过

本轮证据：

1. 一级审批退回目标仅：
   - `审校申请`
2. 二级审批退回目标仅：
   - `审校申请`
3. 三级审批退回目标仅：
   - `审校申请`

### 5.11 用例-58 发起人任务页按钮与跳转

结论：

1. 通过

本轮证据：

1. 专属 `我发起的` 页面审批中样本按钮为：
   - `查看 / 撤销`
2. 退回样本按钮为：
   - `查看 / 编辑 / 撤销`
3. 退回样本编辑真实跳转到：
   - `/manuscript/review/form?...returnTo=/manuscript/review/my-document`

## 6. 关于旧脚本兼容性的说明

需要单独说明：

1. 旧脚本 `r7_28_queue2_browser_regression.mjs` 在本轮执行时，后段跑到旧通用页入口 `/task/myDocument` 的 `用例-58` 分支时发生超时
2. 根因不是产品失败，而是脚本仍假设 manuscript-review 继续挂在通用 workflow 页
3. 在当前策略下，这一脚本只保留用于：
   - 本轮刷新 `用例-37 / 39` 证据
4. `用例-41 / 46 / 47 / 48 / 58` 的正式当前态结论，以新专属页脚本 `r7_29_task_center_smoke.json` 为准
5. `用例-42` 的正式当前态结论，以本轮新增脚本 `r7_29_case42_termination_check.json` 为准

## 7. 第二队列最终结论

本轮回归后，第二队列可确认为：

1. `用例-37` 通过
2. `用例-39` 通过
3. `用例-41` 通过
4. `用例-42` 通过
5. `用例-43` 通过
6. `用例-44` 通过
7. `用例-45` 通过
8. `用例-46` 通过
9. `用例-47` 通过
10. `用例-48` 通过
11. `用例-58` 通过

结论：

1. 第二队列本轮已完成回归
2. 第二队列当前可以整体标记为：
   - `通过`
3. 后续不需要再把第二队列保留为“待实现”或“待补证”状态
