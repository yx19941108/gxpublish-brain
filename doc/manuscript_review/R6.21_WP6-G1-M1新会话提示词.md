Owner: Codex
Task: manuscript_review WP6-G1-M1 新会话提示词
Status: Active
Updated: 2026-03-25 16:30 +08:00

# R6.21_WP6-G1-M1新会话提示词

以下内容可直接作为新会话首条提示词使用。

```text
你现在不是主控审批人，你是“WP6-G1-M1 执行控制器”。

先读以下文件，且只以它们作为当前执行入口，不要凭聊天记忆直接动手：
1. doc/manuscript_review/R6.21_WP6-G1-M1新会话提示词.md
2. doc/manuscript_review/R6.20_WP6-G1-M1续工准备包.md
3. doc/manuscript_review/R6.19_WP6-G1-BPM执行基线.md
4. doc/manuscript_review/R6.18_WP6-G1-M1执行侧续工提示词.md
5. doc/manuscript_review/R6.17_WP6-G1主控审批结论.md
6. doc/manuscript_review/R6.14_WP6-G1 Gate复核准备.md
7. doc/manuscript_review/product/20260321_manuscript-review_product-requirements_v01.md

当前轮次只做一件事：
闭环后端 `manuscript_review` 的 BPM/runtime 主线。

明确边界：
- 不做 `plus-ui-ts/**` 前端实现
- 不进入 `R7`
- 不扩 shared 输出形态
- 不默认改 `Brain/brain-modules/brain-workflow/**` 通用 Java
- `brain-manuscript-review` 只作为代码落点，不作为反推冻结契约的依据

非常重要：
- 本轮要的是真实后端 runtime 证据，不是前端联调证据。
- 前端尚未开工，不影响后端先定义和验证 `formPath=/manuscript/review/approval`。
- 如需真实运行态取证，真实登录态和真实 token 只是后端 API 取证前置，不是前端交付物。
- “去审批”的后端真值口径必须是：`permissionMatrix + formPath + taskId`。

你本轮必须完成或确认的关键点：
1. `submitAndFlowStart / resubmit / cancelProcessApply` 必须是真实 workflow 驱动，不允许继续只本地写业务状态冒充流程闭环。
2. manuscript 侧要有 `ProcessEvent / ProcessTaskEvent` 最小监听和必要回写。
3. runtime 资产和 SQL 要按分层思路补齐，不要把长期 SQL 和 dev-only fixture 混成一个大脚本。
4. 如环境允许，要补最小后端 runtime fresh 证据，至少包括：
   - 发起人提交后，真实创建 workflow 实例
   - 待办里能看到对应任务，且能拿到真实 `taskId / formPath`
   - 发起人撤销后，workflow 实例与业务表同步进入取消态，history 正确
   - 流程走到三级后执行驳回终止，workflow 与业务表同步进入驳回终止态，history 正确
   - 越权去审批、非法状态下重复提交/撤销/再次提交，在真实运行态被拒绝

你的执行顺序：
1. 重新对齐基线和当前代码/SQL现状
2. 只在允许范围内识别还缺什么
3. 小步补齐 manuscript 侧 BPM 接通、runtime 资产、SQL、测试
4. 跑 JDK17 定向验证
5. 能取到后端 runtime 证据就继续取证
6. 如果被前置环境阻塞，就落阻塞记录，不编造通过结论

允许修改范围：
- Brain/brain-modules/brain-manuscript-review/pom.xml
- Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/**
- Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/**
- Brain/script/sql/update/*manuscript_review*
- doc/manuscript_review/*

默认禁改范围：
- plus-ui-ts/**
- Brain/brain-modules/brain-editorial/**
- Brain/brain-modules/brain-workflow/** 通用 Java
- shared 输出形态

如果发现以下任一情况，先停下来给 blocker，不要自己脑补：
- `processType -> flowCode` 口径不清
- `formPath` 口径不清
- 详情现有契约无法承载 `permissionMatrix + formPath + taskId`，且主控未允许扩 shared
- 你还没排除配置/SQL/manuscript 接法问题，就想直接改 workflow 通用 Java

汇报格式固定为：
- 已确认
- 风险
- 待确认
- 推进方案
- Verification
- Changed Files
- Next Gate

收尾要求：
- 结束时必须在 `doc/manuscript_review` 下新增版本化记录文件，不覆写 R6.20-R6.22
- 如果本轮把缺的 BPM/runtime fresh 证据补齐，结论只能写“可回主控继续推 WP6-G1 复判”
- 不得直接写“WP6-G1 已自动通过”或“可直接进入 R7”
```
