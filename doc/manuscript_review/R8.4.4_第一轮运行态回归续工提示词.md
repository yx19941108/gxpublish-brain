Owner: Codex
Task: manuscript_review R8 第一轮运行态回归续工提示词
Status: Draft
Updated: 2026-03-30 14:02:00 +08:00

# R8.4.4_第一轮运行态回归续工提示词

```text
你现在接手的是 `gxpublish-brain` 的 `manuscript_review` R8 第一轮运行态回归续工。

先读以下文件，不得跳过：
1. `doc/manuscript_review/R8.0_问题排查续工交接.md`
2. `doc/manuscript_review/R8.2_问题池统一修复方案.md`
3. `doc/manuscript_review/R8.3_问题池分轮修复执行基线.md`
4. `doc/manuscript_review/R8.2.5_问题池统一回归测试用例.md`
5. `doc/manuscript_review/R8.4_第一轮修复执行提示词.md`
6. `doc/manuscript_review/R8.4.1_第一轮修复执行checkpoint.md`
7. `doc/manuscript_review/R8.4.2_第一轮修复续工提示词.md`
8. `doc/manuscript_review/R8.4.3_第一轮修复静态验证记录.md`
9. `doc/manuscript_review/R8.4.4_第一轮运行态回归续工提示词.md`

执行裁决顺序保持不变：
1. 分轮与实现边界以 `R8.3` 为准
2. 问题级修复目标与游工意见以 `R8.2` 为准
3. 回归与最终验收以 `R8.2.5` 为准

当前已确认事实，直接承接，不要重新摸排：
1. 第一轮代码范围仍只锁定问题 `9 / 3 / 7`
2. 问题 `9 / 3 / 7` 的代码改动已经落地
3. 前端定向静态回归已通过：
   - `access-control.spec.ts`
   - `route-shell.spec.ts`
   - `ledger-layout.spec.ts`
4. 后端定向静态回归已通过：
   - `ManuscriptReviewApiControllerTest`
   - `ManuscriptReviewReadableFirstRoundGuardrailTest`
   - `ManuscriptReviewFirstRoundGuardrailTest`
   - `ManuscriptReviewReadableVisibilityTest`
5. `pnpm build:prod` 已通过
6. 当前未完成项不是代码实现，而是运行态证据
7. Brain/script/sql/update/manuscript_review/20260330_manuscript_review_first_round_permission_hardening_v01.sql未执行，相关数据还在表里。

本轮硬边界：
1. 只补第一轮运行态证据，不进入第二轮问题 `1 / 4 / 5 / 6 / 8`
2. 问题 `2` 仍只补证，不改代码
3. 不得重开方案比较
4. 不得把静态绿灯直接说成第一轮 gate 已过

继续执行要求：
1. 先汇报 `已确认 / 风险 / 待确认 / 推进方案`
2. 前后端已经启动于9999/9888
3. 运行态起来后，严格按 `R8.2.5` 执行第一轮对应回归：
   - 问题 9：`TC-26 ~ TC-30`
   - 问题 3：`TC-05 ~ TC-10`
   - 问题 7：`TC-18 ~ TC-21`
4. 汇报时必须分开给出：
   - 浏览器证据
   - 接口证据
   - 数据证据
5. 最后单独判断：
   - 第一轮 gate 是否达到
   - 是否允许进入下一轮

本轮优先执行顺序：
1. 运行态 ready 证据
2. 浏览器最小存量回归：
   - 发起人仍可进入新增页
   - 台账页仍可打开
   - 详情页仍可打开
3. 问题 9 运行态回归
4. 问题 3 运行态回归
5. 问题 7 运行态回归
6. 真实数据 SQL 复核
7. 第一轮 gate 判断

SQL / 数据层至少补这些事实：
1. 问题 9：
   - 审批人角色是否仍可直接访问 create 入口
   - 无发起权限用户直接调提交接口是否被拦截
2. 问题 3：
   - 台账数据是否符合“发起人来源 ∪ 审批人来源 ∪ 历史参与人来源”
   - 不同角色组合用户的结果是否符合并集口径
3. 问题 7：
   - 新写入的 `brain_manuscript_review_history` 是否都有 `actor_user_id / actor_name`
   - `BACK / APPROVE / REJECT / FINISH / SKIP_LEVEL_1` 是否落为完整自然语言主句

禁止事项：
1. 不得把问题 3 回退成 Java 内存分页
2. 不得把问题 7 放宽成“系统动作可以没有 actor”
3. 不得把问题 9 只验证前端显隐，不验接口和 service 二次校验
4. 不得触碰 `plus-ui-ts/.eslintrc-auto-import.json`
5. 不得触碰 `test_qa_flow.py`

如果本轮上下文再次出现遗忘风险，立即停止，并把阶段工件落到 `doc/manuscript_review/`，前缀必须为 `R8.4.x_`。
```
