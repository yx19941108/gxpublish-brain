Owner: Codex
Task: manuscript_review WP6-G1-M1 续工准备包
Status: Active
Updated: 2026-03-25 16:30 +08:00

# R6.20_WP6-G1-M1续工准备包

## 已确认
- 当前唯一执行主线是：闭环后端 `manuscript_review` 的 BPM/runtime 主线。
- 当前轮次不做：
  - `plus-ui-ts/**` 前端实现
  - `Brain/brain-modules/brain-workflow/**` 通用 Java 改造
  - shared 输出形态扩容
  - `R7` 讨论或前端交互实现包
- `formPath` 当前正式基线已经锁定为 `/manuscript/review/approval`。
- 前端尚未开工，但这不影响后端先定义 `formPath` 和闭环后端 BPM 线。
- 当前轮次要的是真实后端 runtime 证据，不是前端联调证据。
- 如需跑真实后端 API 验证，真实登录态和真实 token 只是后端取证前置，不是前端交付物。
- “去审批”的后端真值口径已经锁定为：`permissionMatrix + formPath + taskId`。
- 现有 manuscript 后端工作树里已经有 BPM 半成品和定向单测通过记录，但这还不能等价为 `WP6-G1` 已通过。

## 风险
- 代码和单测即使是绿的，也不能证明 workflow 实例、待办任务和事件回写在真实运行态都成立。
- 如果 runtime JSON、SQL seed、菜单/角色/测试账号没有真正落库，后端 HTTP 验证会出现假阴性。
- 如果 `taskId / formPath` 在真实运行态拿不到，`FR-07 / FR-08 / PM-06 / NE-06` 仍不能算闭环。
- 只有先排除配置、SQL、runtime 资产和 manuscript 自身接法问题后，才允许把 `brain-workflow` 通用 Java 判断为 blocker。

## 待确认
- 本地 dev/test 库里是否已经真实 apply 了 manuscript_review 的 workflow seed。
- 当前环境里能否直接复用现成 `mr_*` 测试账号，不需要新增更多 dev-only 账号。
- 当前 `brain-manuscript-review` 代码是否已经把 `submitAndFlowStart / resubmit / cancelProcessApply` 全部接到了真实 workflow 服务，而不是部分接通、部分本地写状态。
- 当前详情聚合里是否已经能稳定拿到 `permissionMatrix + formPath + taskId`。

## 推进方案
1. 先重新对齐基线文件，只以 `R6.19 + R6.18 + R6.17 + R6.14 + PRD` 为当前入口，不按聊天记忆续写。
2. 只在允许范围内检查当前代码和 SQL：
   - `Brain/brain-modules/brain-manuscript-review/**`
   - `Brain/script/sql/update/*manuscript_review*`
   - `doc/manuscript_review/runtime_*.json`
3. 先补齐 runtime 资产和 SQL 分层，再补 manuscript 模块侧的真实 workflow 接通和最小事件监听。
4. 跑 JDK17 定向测试，确认静态验证没有回退。
5. 在环境允许时补最小后端 runtime fresh 证据；如果环境前置缺失，则落阻塞记录，不编造通过结论。

## 完成判定
- `submitAndFlowStart` 调用后，真实创建 workflow 实例。
- 待办里能看到对应任务，且能拿到真实 `taskId / formPath`。
- 发起人撤销后，workflow 实例和业务表都进入取消态，history 正确。
- 流程走到三级后执行驳回终止，workflow 和业务表都进入驳回终止态，history 正确。
- 越权去审批、非法状态下重复提交、撤销、再次提交，在真实运行态被拒绝。
- 以上证据只要求后端 API/runtime 成立，不要求前端审批页已经开发完成。

## 禁止事项
- 不准把这轮目标改写成前端审批页实现。
- 不准为了前端未开工，回退去猜旧 `form/detail` 页。
- 不准默认修改 shared detail VO 扩字段。
- 不准默认修改 `Brain/brain-modules/brain-workflow/**` 通用 Java。
- 不准把单测通过、代码存在、JSON 已写好，冒充成 runtime 已闭环。

## 产出要求
- 新会话结束时，必须在 `doc/manuscript_review` 下新增版本化记录文件，不覆写 `R6.20-R6.22`。
- 如果完成闭环，落“执行完成记录”。
- 如果中途发现 blocker，落“执行阻塞记录”。
- 汇报结构继续使用：
  - 已确认
  - 风险
  - 待确认
  - 推进方案
  - Verification
  - Changed Files
  - Next Gate

## Next Gate
- 做完这轮 BPM 后端闭环后，不是自动放行，也不是直接进入 `R7`。
- 正确口径是：
  - 如果本轮把当前缺的 BPM/runtime fresh 证据补齐，且没有再冒出 shared 或 workflow 通用层 blocker，就可以回主控继续推 `WP6-G1` 复判。
  - 只有主控复判通过后，`WP6-G1` 才能算通过。
  - 在主控复判前，不得自行宣称“第 7 轮可直接启动”。
