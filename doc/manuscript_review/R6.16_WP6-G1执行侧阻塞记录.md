Owner: Codex
Task: manuscript_review WP6-G1 执行侧阻塞记录
Status: Active
Updated: 2026-03-25 08:27 +08:00

# R6.16_WP6-G1执行侧阻塞记录

## 已确认

- 本轮执行入口已按 `R6.15 -> R6.14 -> R6.13 -> R6.8 -> R6.5 -> R6.6 -> R5.7 -> R5 -> R5.5 -> PRD` 复读，并继续遵守：
  - 不进入 `R7`
  - 不新增 frontend 变更
  - 不扩 shared 输出形态
  - 不把旧失败实现当基线
- 本轮只改了 backend private 允许路径：
  - `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewServiceTest.java`
  - `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewService.java`
- 本轮已补成 fresh 绿的缺口：
  - `DR-05`
    - 标题长度上限
    - 说明长度上限
    - 正文长度上限
    - 外链标题长度上限
    - URL 长度上限
  - `DR-07 / NE-03`
    - 非法 URL 协议拦截
    - 同一流程内 URL 重复拦截
  - `TN-03`
    - 非法时间格式拦截
    - 本轮 red test 直接暴露旧实现把 `3:7` 误收为合法 `mm:ss`
    - 已做最小修补：带冒号时间必须满足固定宽度 `mm:ss / HH:mm:ss`
  - `TN-04 / NE-05`
    - 结束时间早于开始时间拦截
    - 超出总时长拦截
    - 总时长未识别拦截
  - `FR-05`
    - service 侧“发起人审批中可撤销 -> 已取消 / 流程已取消 / 写撤销 history” fresh 证据已补
  - `NE-06`
    - 越权修改拦截
    - 非法状态下再次提交拦截
    - 非法状态下重复撤销拦截
- 2026-03-25 08:26 +08:00 按 `R6.15` 指定 JDK17 定向测试实跑通过：
  - `ManuscriptReviewApiControllerTest`: `5/5`
  - `ManuscriptReviewSharedApiMvcTest`: `4/4`
  - `ManuscriptReviewDetailPermissionPolicyTest`: `1/1`
  - `ManuscriptReviewFlowRoutingServiceTest`: `5/5`
  - `ManuscriptReviewReadableServiceTest`: `5/5`
  - `ManuscriptReviewReadableVisibilityTest`: `3/3`
  - `ManuscriptReviewServiceTest`: `33/33`
  - 合计：`56/56` 通过，`0` 失败，`0` 错误
- 本轮未新增：
  - manuscript_review SQL
  - 流程定义准备
  - workflow 事件写回接缝
  - frontend 变更

## 风险

- `submitAndFlowStart / resubmit / cancelProcessApply` 当前仍是 manuscript-review 本地写侧动作，不是 workflow 实例真实启动/再次提交/撤销闭环：
  - `ManuscriptReviewApiController` 只调用 `ManuscriptReviewService`
  - 当前 manuscript_review 代码未见 `IFlwInstanceService / IFlwTaskService` 直连
- `brain-workflow` 当前确认只有通用 `FlowProcessEventHandler` 负责发布 `ProcessEvent / ProcessTaskEvent / ProcessDeleteEvent`；
  - 仓库内已落地的示例消费方是 `leave` 线
  - 本轮未发现 manuscript_review 专属事件消费实现
- 因此：
  - `FR-06` 仍未拿到“三级驳回 -> 已驳回” fresh runtime 证据
  - `FR-05` 当前只补到了 service 侧撤销证据，尚未证明真实 workflow 实例也同步撤销
  - `FR-07 / FR-08 / PM-06 / NE-06` 仍主要停留在 API 分离、permissionMatrix、service 守卫层；没有升格为 BPM/runtime 级 fresh 证据

## 待确认

- 是否要继续在 `WP6-G1` 边界内进入下一轮最小 workflow/BPM 接缝整改，目标仅限：
  - manuscript_review 流程启动
  - manuscript_review 流程撤销
  - manuscript_review 驳回/退回/完成后的 record + history 写回
- 若继续补 runtime，需主控继续明确：
  - 允许使用的 manuscript_review flowCode / 真实定义
  - manuscript_review 对 workflow businessId / instanceId / formPath 的唯一口径
  - 本轮是否允许把“事件发布器已在，但消费方缺失”视为 execution-side 可修复范围

## 推进方案

- 当前建议对外统一口径只上报：
  - `本轮 backend-private 最小补证/最小整改已完成，新增 fresh 定向证据已绿`
  - `当前 WP6-G1 Gate 证据仍不足，未达放行`
- 若继续推进，建议最小顺序为：
  1. 明确 manuscript_review 的 workflow flowCode / businessId / formPath 基线
  2. 新增 manuscript_review 专属 `ProcessEvent / ProcessTaskEvent` 消费接缝
  3. 只补 `FR-06` 与 `FR-05/07/08/PM-06` 所必需的最小 runtime 写回
  4. 补对应 fresh HTTP / BPM / history 证据
- 在主控正式重判前，本轮仍不得自报：
  - `WP6-G1 已放行`
  - `第6轮后端已整体通过`
  - `repo 级冻结口径已闭合`
  - `R7 可启动`

## Verification

```bash
cd Brain
set JAVA_HOME=C:\kuguaHome\env\java\jdk17
set PATH=%JAVA_HOME%\bin;%PATH%
mvn --% -pl brain-modules/brain-manuscript-review -am -Dtest=ManuscriptReviewServiceTest -Dsurefire.failIfNoSpecifiedTests=false -DskipTests=false test
```

- 首次新增红测结果：
  - `shouldRejectVideoMarkWhenStartTimeFormatInvalid` 失败
  - 暴露问题：`3:7` 被当前实现误判为合法时间，落入“结束早于开始”分支
- 最小修补后复跑：
  - `ManuscriptReviewServiceTest`: `33/33`

```bash
cd Brain
set JAVA_HOME=C:\kuguaHome\env\java\jdk17
set PATH=%JAVA_HOME%\bin;%PATH%
mvn --% -pl brain-modules/brain-manuscript-review -am -Dtest=ManuscriptReviewServiceTest,ManuscriptReviewReadableServiceTest,ManuscriptReviewSharedApiMvcTest,ManuscriptReviewApiControllerTest,ManuscriptReviewFlowRoutingServiceTest,ManuscriptReviewDetailPermissionPolicyTest,ManuscriptReviewReadableVisibilityTest -Dsurefire.failIfNoSpecifiedTests=false -DskipTests=false test
```

- 结果：
  - `56/56` 通过
  - `0` 失败
  - `0` 错误

## Changed Files

- `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewServiceTest.java`
- `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewService.java`
- `doc/manuscript_review/R6.16_WP6-G1执行侧阻塞记录.md`

## Next Gate

- 当前可交回主控重判的信息：
  - `DR-05 / DR-07 / NE-03 / TN-03 / TN-04 / NE-05` backend-private fresh 证据已补齐
  - `FR-05` service 侧 fresh 证据已补
  - `NE-06` 的越权修改、非法状态再次提交、非法状态重复撤销已补 service 侧 fresh 证据
  - 当前定向测试 `56/56` 绿
- 当前仍不足以交回主控判 `WP6-G1 放行` 的 blocker：
  - `FR-06` 缺少 manuscript_review runtime 写回闭环
  - `FR-05 / FR-07 / FR-08 / PM-06 / NE-06` 仍缺 workflow/BPM 级 fresh 证据
  - 当前仍卡在 workflow/BPM runtime blocker
