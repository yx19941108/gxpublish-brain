Owner: Codex
Task: manuscript_review 第三队列当前态最小回归
Status: Passed
Updated: 2026-03-29 17:52:00 +08:00

# R7.30_正式测试执行记录-第三队列当前态最小回归

## 1. 本轮承接前提

本轮严格按当前会话给定前提执行：

1. 第二队列已经完成回归并可视为通过。
2. 通用 workflow 页里的 manuscript-review 特判已经清理完成。
3. manuscript-review 当前走自己的专属任务页：
   - `/manuscript/review/my-document`
   - `/manuscript/review/task-waiting`
   - `/manuscript/review/task-finish`
4. SQL 相关改动统一收口在：
   - `/home/kugua/dev/gxpublish-brain/Brain/script/sql/update/manuscript_review/`
5. 本轮不扩到第四队列。

## 2. 第三队列范围与当前判定口径

### 2.1 第三队列范围

按：

1. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.26_正式测试待办基线-四队列收口计划.md`

第三队列范围固定为：

1. `用例-01`
2. `用例-02`
3. `用例-03`
4. `用例-04`
5. `用例-05`
6. `用例-06`
7. `用例-07`
8. `用例-08`
9. `用例-09`
10. `用例-10`
11. `用例-12`

### 2.2 当前判定口径

按：

1. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.4_正式测试方案-全需求覆盖.md`
2. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/uipic/README.md`

本轮采用以下保守口径：

1. `通过`
   - 必须拿到本轮当前态直接证据，且证据与 `R7.4` 预期一致。
2. `未通过`
   - 本轮当前态直接证据已经与 `R7.4` 预期发生明确冲突。
3. `证据不足`
   - 现有证据无法直接证明通过，也没有直接反证。

补充说明：

1. `用例-09` 的 `uipic` 只作为页面承载参考，不作为业务裁决源。
2. 如果脚本入口或等待条件过时，只记为“脚本问题”，不直接算产品失败。
3. 本轮先取“最小但充分”的当前态证据，不追求全量重演历史用例。

## 3. 工作树与运行前置

### 3.1 分支与工作树

已确认：

1. 当前分支为：
   - `manuscript-review-tdd-20260321`
2. 当前工作树存在以下非本轮新增状态：
   - `M plus-ui-ts/.eslintrc-auto-import.json`
   - `?? doc/manuscript_review/R7.30_正式测试执行记录-第二队列回归完成.md`

说明：

1. 上述状态未被本轮回归修改或回滚。
2. 本轮只新增临时脚本与正式执行记录。

### 3.2 运行态

已确认：

1. `http://127.0.0.1:9999/` 返回 `200`
2. `http://127.0.0.1:9888/actuator/health` 返回 `401`
3. `9888`、`9999` 端口均已监听

结论：

1. 本轮无需重启前后端即可执行第三队列最小回归。

### 3.3 工具入口说明

本轮执行中发现：

1. `node`、`npm` 在非交互 shell 中未自动入 `PATH`
2. 但机器基线已确认它们通过 `mise` 安装存在
3. 最终通过 `bash -ic` 执行成功

结论：

1. 这属于运行入口问题，不属于产品失败。

## 4. 本轮复用的现有能力

本轮优先复用了已有能力，没有重造测试体系：

1. `.agent-temp/manuscript_review_r7_28/` 下既有 `login + token 注入 + Playwright + API` 骨架
2. 前端现有冻结规格测试：
   - `ledger-layout.spec.ts`
   - `form-layout.spec.ts`
   - `detail-layout.spec.ts`
   - `approval-route.spec.ts`
   - `detail-approve-jump.spec.ts`
3. 现有样本生成方式：
   - 直接调用 `POST /workflow/manuscript-review/submitAndFlowStart`
   - 直接调用 workflow 待办与退回接口构造待办/退回样本

## 5. 本轮实际执行

### 5.1 前端冻结规格测试

已执行：

```bash
cd /home/kugua/dev/gxpublish-brain/plus-ui-ts
bash -ic 'npm exec vitest run src/views/manuscript-review/ledger-layout.spec.ts src/views/manuscript-review/form-layout.spec.ts src/views/manuscript-review/detail-layout.spec.ts src/views/manuscript-review/approval-route.spec.ts src/views/manuscript-review/detail-approve-jump.spec.ts'
```

结果：

1. `5` 个测试文件全部通过
2. `14` 个测试全部通过

### 5.2 浏览器/API 当前态直证

本轮新增临时脚本：

1. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_30_queue3_direct_evidence.mjs`

输出工件：

1. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_30_queue3_direct_evidence.json`
2. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_30_case01_ledger.png`
3. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_30_case02_create.png`
4. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_30_case03_returned_detail.png`
5. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_30_case03_edit.png`
6. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_30_case04_approver_detail.png`
7. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_30_case04_approval.png`
8. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_30_case06_viewer_detail.png`

补充说明：

1. 首次执行时，脚本把隐藏锚点 `data-testid="manuscript-review-hidden-reviewId"` 误当成“必须可见”，导致超时。
2. 该问题已修正为“挂载即可”，随后复跑成功。
3. 该问题属于脚本等待条件错误，不属于产品失败。

## 6. 逐项结论

### 6.1 通过项

#### 用例-01 列表页入口可达

结论：

1. 通过

当前态证据：

1. `/manuscript/review` 可打开
2. 页面命中：
   - `关键词`
   - `流程类型`
   - `业务状态`
   - `当前节点`
   - `新增`
3. 截图：
   - `r7_30_case01_ledger.png`

#### 用例-02 新增页入口可达且主动作只有提交

结论：

1. 通过

当前态证据：

1. 从列表页进入：
   - `/manuscript/review/form?mode=create`
2. 页标题为：
   - `新增审校单`
3. 页脚按钮为：
   - `返回`
   - `提交`
4. 主动作仅为：
   - `提交`

#### 用例-03 修改页入口可达且主动作只有保存

结论：

1. 通过

当前态证据：

1. 退回样本详情页动作栏为：
   - `修改 / 再次提交 / 返回`
2. 点击 `修改` 后进入：
   - `/manuscript/review/form?...mode=edit...`
3. 修改页标题为：
   - `修改审校单`
4. 页脚按钮为：
   - `返回`
   - `保存`
5. 未混入：
   - `去审批`
   - `再次提交`
   - `保存并提交`

#### 用例-04 详情页与审批页已拆页且语义不串

结论：

1. 通过

当前态证据：

1. 审批人详情页 URL 为：
   - `/manuscript/review/detail?...taskId=...`
2. 详情页标题为：
   - `审校详情`
3. 详情页动作栏为：
   - `修改 / 去审批 / 返回`
4. 点击 `去审批` 后进入：
   - `/manuscript/review/approval?...`
5. 审批页标题为：
   - `审校审批`
6. 审批页动作栏为：
   - `审批办理 / 返回`

#### 用例-05 列表页行内默认且唯一动作为详情

结论：

1. 通过

当前态证据：

1. 以本轮新样本检索列表后，首行按钮仅为：
   - `详情`

#### 用例-06 详情页动作分流正确

结论：

1. 通过

当前态证据：

1. 当前审批人详情页：
   - `修改 / 去审批 / 返回`
2. 被退回发起人详情页：
   - `修改 / 再次提交 / 返回`
3. 非当前办理人查看页：
   - `返回`

#### 用例-07 审批页只承接审批办理

结论：

1. 通过

当前态证据：

1. 详情页动作栏不出现：
   - `审批办理`
2. 审批页动作栏出现：
   - `审批办理`
3. 点击 `审批办理` 后真实拉起办理弹窗
4. 弹窗按钮为：
   - `提交 / 退回 / 取消`

#### 用例-09 页面对照图承载一致性核对

结论：

1. 通过

当前态证据：

1. 对照 `uipic/README.md` 的四页职责约束：
   - 列表页行内仅 `详情`
   - 新增页仅 `提交`
   - 修改页仅 `保存`
   - 详情页负责动作分流，不页内审批
   - 审批动作进入独立审批页
2. 本轮已补齐当前页面截图，可与 `uipic` 的四页职责逐项核对

#### 用例-10 登录与租户规则

结论：

1. 通过

当前态证据：

1. 使用租户：
   - `000000`
2. 使用任意验证码数字探针：
   - `7`
3. 登录接口返回：
   - `code = 200`
4. `getInfo` 返回：
   - `code = 200`

#### 用例-12 普通发起人只带外链也可提交

结论：

1. 通过

当前态证据：

1. 浏览器真实从新增页提交
2. 提交约束为：
   - `attachmentResources = []`
   - `externalLinks.length = 1`
3. 请求命中：
   - `POST /workflow/manuscript-review/submitAndFlowStart`
4. 成功生成新样本并进入详情页

### 6.2 本轮修复后新增通过项

#### 用例-08 页面展示遵守中文业务语义

结论：

1. 已转为通过

本轮修复：

1. 后端当前用户展示名改为：
   - 优先取昵称
   - 无昵称时再回退账号名
2. manuscript-review 页面可视文案已去除英文眉标题与技术提示：
   - `新增提交页 / 修改保存页 / 审校详情页 / 审校审批页`
   - `不推动流程`
   - `流程办理`
   - 去除 `create/edit/BPM/ossId/DELETE /resource/oss/{ossId}` 等用户可见技术文案
3. 第三队列直证脚本样本标题已去除：
   - `WAITING / RETURNED` 等会误伤“英文枚举暴露”探针的样本词

修复后当前态证据：

1. 直证 JSON：
   - `r7_30_queue3_direct_evidence.json`
2. 关键探针均为：
   - `detailShowsNoRawId = true`
   - `detailShowsNoUsername = true`
   - `detailShowsNoEnum = true`
   - `detailTimeFormatOk = true`
   - `visibleEnglishChrome = []`
3. 前端规格测试补强后通过：
   - `6` 个文件
   - `22` 个测试全部通过
4. 后端相关回归通过：
   - `45` 个测试全部通过

### 6.3 证据不足项

结论：

1. 本轮第三队列无“证据不足”项

说明：

1. 本轮对 `01-10、12` 均已拿到当前态直接证据。
2. 本轮修复与复跑后，第三队列不存在未通过项。

## 7. 第三队列整体结论

### 7.1 通过

1. `用例-01`
2. `用例-02`
3. `用例-03`
4. `用例-04`
5. `用例-05`
6. `用例-06`
7. `用例-07`
8. `用例-08`
9. `用例-09`
10. `用例-10`
11. `用例-12`

### 7.2 未通过

1. 无

### 7.3 证据不足

1. 无

## 8. 是否建议进入下一队列

结论：

1. 当前建议进入下一队列

理由：

1. 第三队列 `11 / 11` 当前态已全部通过。
2. 本轮已同时补齐：
   - 前端规格测试
   - 后端相关测试
   - 浏览器/API 直接证据
3. 当前可以进入下一队列，但仍建议沿用“最小但充分”的证据策略继续推进。

## 9. 对“第三队列现在是否通过”的直接回答

直接结论：

1. 第三队列现在 **通过**

原因：

1. `11` 个目标用例中，当前为：
   - `11` 个通过
   - `0` 个未通过
   - `0` 个证据不足
2. 已可进入下一队列。
