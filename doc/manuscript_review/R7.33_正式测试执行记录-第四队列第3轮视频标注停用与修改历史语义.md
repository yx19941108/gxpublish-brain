Owner: Codex
Task: manuscript_review 第四队列第3轮视频标注停用与修改历史语义
Status: Passed
Updated: 2026-03-29 23:59:30 +08:00

# R7.33_正式测试执行记录-第四队列第3轮视频标注停用与修改历史语义

## 1. 本轮承接边界

本轮只从以下文档恢复上下文：

1. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.30_正式测试执行记录-第三队列当前态最小回归.md`
2. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.26_正式测试待办基线-四队列收口计划.md`
3. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.4_正式测试方案-全需求覆盖.md`
4. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.31_正式测试执行记录-第四队列第1轮资源生命周期与追加语义.md`
5. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.32_正式测试执行记录-第四队列第2轮负向校验与用例55补证.md`

沿用当前前提：

1. 第二队列已通过。
2. 第三队列已通过。
3. 第四队列第 1 轮与第 2 轮均已通过。
4. manuscript-review 仍走专属任务页：
   - `/manuscript/review/my-document`
   - `/manuscript/review/task-waiting`
   - `/manuscript/review/task-finish`

## 2. 本轮范围

本轮正式范围固定为：

1. `用例-31`
2. `用例-49`
3. `用例-50`

其中：

1. `用例-31` 是第四队列资源生命周期尾项。
2. `用例-49 / 用例-50` 是第四队列修改历史与追加语义主线。

## 3. 运行态与最小修补

### 3.1 运行态

本轮先恢复并确认当前运行态：

1. 前端：
   - `npm run dev -- --host 127.0.0.1 --port 9999`
   - `http://127.0.0.1:9999/` 返回 `200`
2. 后端：
   - `mvn -pl brain-admin -am package -DskipTests`
   - `java -jar brain-admin/target/brain-admin.jar --spring.profiles.active=dev`
   - `http://127.0.0.1:9888/actuator/health` 返回 `401`
3. 后端本轮明确使用 `JDK 17` 重新打包并重启，不是旧进程复用。

### 3.2 本轮先拿到的当前态失败

本轮首次真实取证时，`用例-49` 结果为：

1. 同一条 `UPDATE` 历史确实只保留了一条。
2. 标题、媒体栏目、说明都能显示旧值和新值。
3. 但正文只显示：
   - `正文已更新`
4. 未明确展示正文旧值与新值。

结论：

1. 这不是脚本问题，而是后端历史文案当前实现缺口。
2. 根因收敛到：
   - `/home/kugua/dev/gxpublish-brain/Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewService.java`

### 3.3 本轮最小修补

本轮只做最小后端修补与对应单测收口：

1. `/home/kugua/dev/gxpublish-brain/Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewService.java`
   - `UPDATE` 历史中的正文差异改为输出旧值/新值预览文案
2. `/home/kugua/dev/gxpublish-brain/Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewServiceTest.java`
   - 把原有 `正文已更新` 断言改为正文旧值/新值断言

本轮不改：

1. 前端资源区
2. BPM 路由
3. 资源写入与停用流程

## 4. 本轮验证

### 4.1 后端单测

已通过：

1. `mvn -pl brain-modules/brain-manuscript-review -am -DskipTests=false -Dtest=ManuscriptReviewServiceTest test`
2. 结果：
   - `40` 个测试全部通过

### 4.2 后端打包与运行态重启

已通过：

1. `mvn -pl brain-admin -am package -DskipTests`
2. 新 `brain-admin.jar` 重新启动成功
3. `9888` 健康探针返回 `401`

### 4.3 第四队列第 3 轮真实证据脚本

本轮使用临时脚本：

1. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_33_queue4_round3_evidence.mjs`

输出工件：

1. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_33_queue4_round3_evidence.json`
2. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_33_case31_mark_disabled.png`
3. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_33_case49_case50_history.png`

## 5. 逐项结论

### 5.1 用例-31 停用视频标注

结论：

1. 通过

本轮证据：

1. 停用前当前有效标注数为：
   - `1`
2. 停用后当前有效标注数为：
   - `0`
3. 详情页“视频标注”分组显示：
   - `当前暂无视频标注数据。`
4. 同一时间线中仍保留：
   - `新增了视频标注《...》`
   - `停用了视频标注《...》`

### 5.2 用例-49 修改历史合并为单条且展示字段旧值新值

结论：

1. 通过

本轮证据：

1. 更新前 `UPDATE` 历史数量为：
   - `0`
2. 更新后 `UPDATE` 历史数量为：
   - `1`
3. 本轮唯一 `UPDATE` 文案同时包含：
   - `标题由“旧值”改为“新值”`
   - `媒体/栏目由“旧值”改为“新值”`
   - `说明由“旧值”改为“新值”`
   - `正文由“旧值”改为“新值”`
4. 不再退化为：
   - `正文已更新`

### 5.3 用例-50 修改时追加新资源的历史文案正确

结论：

1. 通过

本轮证据：

1. 本次修改仍只产生一条 `UPDATE` 历史。
2. 同一条 `UPDATE` 文案同时包含：
   - 字段差异摘要
   - `新增附件《R7.33-Q4-CASE50-追加附件-20260329155850.pdf》`
   - `新增外链《R7.33-Q4-CASE50-追加外链-20260329155850》`
3. 当前有效资源区中同时保留：
   - 原附件 + 追加附件
   - 原外链 + 追加外链

## 6. 第四队列最终结论

结合 `R7.31`、`R7.32` 与本轮 `R7.33`，第四队列内以下用例现已全部通过：

1. `用例-20`
2. `用例-21`
3. `用例-22`
4. `用例-23`
5. `用例-24`
6. `用例-25`
7. `用例-26`
8. `用例-31`
9. `用例-32`
10. `用例-33`
11. `用例-34`
12. `用例-35`
13. `用例-36`
14. `用例-49`
15. `用例-50`
16. `用例-55`
17. `用例-60`

结论：

1. 第四队列当前可以整体标记为：
   - `通过`
2. 本轮不再保留第四队列未收口项。
