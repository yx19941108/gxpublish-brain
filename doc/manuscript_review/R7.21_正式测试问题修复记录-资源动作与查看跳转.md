Owner: Codex
Task: manuscript_review 资源动作与查看跳转修复
Status: Fixed
Updated: 2026-03-28 12:45:00 +08:00

# R7.21_正式测试问题修复记录-资源动作与查看跳转

## 1. 本轮承接范围

- 只承接 `R7.16 / R7.17 / R7.20` 中已经收敛为明确问题的项，不回头重做 `R7.10 / R7.12`。
- 本轮实际落地：
  1. `meeting_01.mp4` 上传上限配置固化
  2. 审校流程在 `我发起的 / 我的待办 / 我的已办` 中的查看跳转修复
  3. 详情页资源区 `停用 / 预览 / 下载` 动作补齐

## 2. 修复结论

### 2.1 大视频上传上限

已确认并保留：

- `Brain/brain-admin/src/main/resources/application.yml`
  - `spring.servlet.multipart.max-file-size = 1024MB`
  - `spring.servlet.multipart.max-request-size = 1100MB`

结论：

- 与 `R4.5` 中“视频默认上限 1GB 且可配置”的本地需求口径一致。
- 本轮未改上传实现，仍保留 `file.getBytes()` 的大文件内存压力风险。

### 2.2 查看跳转口径

前端修复点：

1. `plus-ui-ts/src/views/workflow/task/myDocument.vue`
2. `plus-ui-ts/src/views/workflow/task/taskFinish.vue`
3. `plus-ui-ts/src/views/workflow/task/taskWaiting.vue`
4. `plus-ui-ts/src/views/manuscript-review/detail-navigation.ts`

修复内容：

1. 审校流程行通过 `formPath = /manuscript/review/approval` 识别为 manuscript-review 专属流程。
2. `查看` 统一改跳：
   - `/manuscript/review/detail`
3. 并携带：
   - `reviewId`
   - `taskId`
   - `returnTo`
4. `详情页 -> 返回` 优先按 `returnTo` 回到来源列表，而不是一律回待办。
5. `我的待办` 为审校流程补出 `查看` 按钮，`办理` 继续保持走审批页。

对应问题闭环：

1. `R7.16`
   - 审批人 `我的已办 -> 查看` 落到 `approval` 页
2. `R7.17`
   - 发起人 `我发起的 -> 查看` 落到 `approval` 页
3. `R7.4 用例-59`
   - 审校流程 `我的待办` 现已具备 `查看 + 办理` 两种入口

### 2.3 详情页资源区动作

前端修复点：

1. `plus-ui-ts/src/views/manuscript-review/detail-page-shell.vue`
2. `plus-ui-ts/src/views/manuscript-review/detail.contract.ts`
3. `plus-ui-ts/src/api/manuscript-review/index.ts`
4. `plus-ui-ts/src/api/manuscript-review/types.ts`

后端修复点：

1. `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/controller/response/ManuscriptReviewDetailResponse.java`
2. `Brain/brain-modules/brain-manuscript-review/src/main/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewReadableService.java`

修复内容：

1. 详情接口资源项新增返回 `ossId`。
2. 前端资源模型补齐：
   - `resourceType`
   - `resourceUrl`
   - `ossId`
3. 详情页资源区新增动作：
   - 附件/视频：`停用` `预览` `下载`
   - 外链：`停用` `打开链接`
   - 视频标注：`停用`
4. 纯查看角色不展示 `停用`，仅保留只读动作。
5. 新增前端 API：
   - `PUT /workflow/manuscript-review/video-mark/disable`

对应问题闭环：

1. `R7.16`
   - 详情页资源区未出现 `停用 / 预览 / 下载`

## 3. 自动化验证

### 3.1 前端

执行：

```bash
cd plus-ui-ts
bash -ic 'npm exec vitest run src/views/manuscript-review/detail-navigation.spec.ts src/views/manuscript-review/detail-readable-display.spec.ts src/views/manuscript-review/detail-layout.spec.ts'
```

结果：

- `3` 个测试文件通过
- `18` 个测试用例通过

### 3.2 后端

执行：

```bash
cd Brain
bash -ic 'mvn -pl brain-modules/brain-manuscript-review -am -DskipTests=false -Dtest=ManuscriptReviewApiControllerTest,ManuscriptReviewSharedApiMvcTest,ManuscriptReviewReadableVisibilityTest test'
```

结果：

- 审校模块定向测试通过
- `14` 个测试用例通过
- `0` failures
- `0` errors

## 4. 当前残余风险

1. 本轮未把“历史时间线中的资源/外链名称可点击下载或跳转”做成结构化链接。
2. 本轮未把“多视频切换 + 标注点击跳时”从占位态扩成完整播放器交互。
3. `multipart` 上限问题已修复，但上传实现仍非流式，大文件场景存在内存压力风险。

## 5. 建议下一步

1. 浏览器回归 `R7.4 用例-51 / 52 / 58 / 59`。
2. 若继续补 `R7.4 用例-53 / 54`，下一包聚焦：
   - 历史文案中的资源/外链可点击能力
3. 若继续补资源深水区，再进入：
   - 多视频切换
   - 标注点击跳时
