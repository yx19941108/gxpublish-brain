Owner: Codex
Task: manuscript_review R9 round-3 execution record
Status: Done
Updated: 2026-03-31 22:10:00 +08:00

# R9.7_第3轮执行记录_v01

## 1. 范围

本轮严格锁定：

1. 问题 7：资源区固定高度与区块内滚动
2. 问题 8：单条外链样式异常
3. 问题 9：启用后旧历史项启用按钮自动隐藏
4. 问题 10：视频标注时间展示口径统一

本轮未纳入：

1. 问题 1、2、3、4、5、6、11、12、13
2. 旧 `brain_manuscript_review_history` 数据修补
3. 与 `R9` 无关的历史返修
4. 第 1、2 轮已闭环项回头重做

## 2. 执行前复核结论

### 2.1 代码与文档边界

按 `R9.3` 基线，本轮只允许触达详情资源壳层相关文件：

1. `plus-ui-ts/src/views/manuscript-review/detail.contract.ts`
2. `plus-ui-ts/src/views/manuscript-review/detail-page-shell.vue`
3. `plus-ui-ts/src/views/manuscript-review/detail-readable-display.spec.ts`

本轮不回头改表单、登录、后端读写主链。

### 2.2 运行态样本漂移

原第 2 轮记录中的多视频样本 `reviewId=2038771188202020865` 已失效，不能直接复用。

本轮实际采用的运行态样本如下：

1. `TC-R9-09` 单条外链样本：
   - `reviewId=2038270184161923073`
2. `TC-R9-09` 双外链对照样本：
   - `reviewId=2038284808979980290`
3. `TC-R9-08 / TC-R9-10 / TC-R9-11` 统一承载样本：
   - `reviewId=2038749832083783681`
4. 保留的多视频查看样本：
   - `reviewId=2037916533102526465`

处理原则：

1. 能复用现成样本的优先复用
2. 库里没有现成 `样本 E` 时，只对可编辑样本做最小补样
3. 所有补样动作只服务本轮 `TC-R9-08 ~ 11`

## 3. 本轮变更结果

### 3.1 前端主代码

1. `plus-ui-ts/src/views/manuscript-review/detail-page-shell.vue`
   - 资源区卡片改为固定高度壳层，区块内部滚动
   - 资源主内容布局改为两列网格，避免长文本挤坏卡片
   - 时间线中的视频标注时间改为统一走 `formatVideoMarkTimeRange(...)`
   - 历史区“启用”按钮增加当前有效资源去重判断，已重新生效的旧历史项不再重复暴露启用入口
2. `plus-ui-ts/src/views/manuscript-review/detail.contract.ts`
   - 新增 `buildResourceIdentityKey(...)`
   - 新增 `formatVideoMarkTimeRange(...)`
   - 新增 `shouldShowHistoryEnableAction(...)`
   - 视频标注资源名改为统一时间口径
3. `plus-ui-ts/src/views/manuscript-review/detail-readable-display.spec.ts`
   - 补齐视频标注时间统一断言
   - 补齐历史启用按钮自动隐藏断言

### 3.2 临时回归脚本

本轮新增临时工件：

1. `.agent-temp/r9_round3_browser_regression.mjs`

用途：

1. 对可编辑样本补最小回归数据
2. 自动完成 `TC-R9-08 ~ 11` 浏览器取证
3. 输出截图与 `summary.json`

## 4. 验证结果

### 4.1 静态验证

执行命令：

```bash
cd /home/kugua/dev/gxpublish-brain/plus-ui-ts
pnpm exec eslint src/views/manuscript-review/detail.contract.ts src/views/manuscript-review/detail-page-shell.vue src/views/manuscript-review/detail-readable-display.spec.ts
pnpm exec vitest run src/views/manuscript-review/detail-readable-display.spec.ts -t "normalizes frozen detail payload without leaking technical fields into summary or permissions into ledger space|formats video mark time ranges consistently across resource and playback views|hides enable actions for history items whose resources are already effective again|keeps the pending video mark target synchronized with the selected video|groups video marks by resource and keeps them sortable for playback interaction"
```

结果：

1. 定向 `eslint` 通过
2. 定向 `vitest` 通过
3. `detail-readable-display.spec.ts` 本轮相关 5 条断言全部为绿

### 4.2 浏览器级验证

运行态：

1. 前端：`http://127.0.0.1:9999`
2. 后端：`http://127.0.0.1:9888`
3. 样本补数账号：`mr_initiator / 666666 / 1234`

#### TC-R9-08

使用样本：

1. `reviewId=2038749832083783681`

本轮补样：

1. 新增 1 个超长附件名附件
2. 新增 4 条超长 URL 外链
3. 新增 1 条无结束时间的长文本视频标注

浏览器与脚本证据：

1. 四个资源组 `clientHeight` 均稳定在约 `318px`
2. 附件组 `listScrollHeight=370 > listClientHeight=256`
3. 外链组 `listScrollHeight=834 > listClientHeight=256`
4. 视频标注组 `listScrollHeight=412 > listClientHeight=256`

结论：

1. 固定高度生效
2. 长文本不再把资源卡片无限拉高
3. 超出内容在卡片内部滚动

#### TC-R9-09

对照样本：

1. 单条外链：`reviewId=2038270184161923073`
2. 双外链：`reviewId=2038284808979980290`

浏览器证据：

1. 单条外链卡片高度为 `318px`
2. 双外链卡片高度同样为 `318px`
3. 单条与双条外链均未出现异常放大

结论：

1. 单条外链样式已回到与多条外链一致的视觉规则
2. 外链项不再因条数为 1 而异常拉伸

#### TC-R9-10

使用样本：

1. `reviewId=2038749832083783681`

本轮补样：

1. 新增标注 `R9R3-TOGGLE-MARK-20260331140630`
2. 先通过接口将其停用，制造真实“历史可启用”前置态

浏览器证据：

1. 启用前，目标历史项存在且 `启用` 按钮数量为 `1`
2. 浏览器点击启用并确认后，刷新详情页
3. 刷新后同一历史项上的 `启用` 按钮数量变为 `0`
4. 当前有效视频标注区中重新出现该标注

结论：

1. 启用成功后，旧停用历史项不会重复暴露启用动作
2. 当前有效资源与历史区的状态口径已一致

#### TC-R9-11

使用样本：

1. `reviewId=2038749832083783681`

浏览器证据：

1. 当前视频标注跳转列表显示：
   - `00:00:05 - 00:00:08`
   - `00:00:11`
   - `00:00:21 - 00:00:24`
2. 资源区“视频标注”列表显示：
   - `00:00:05 - 00:00:08`
   - `00:00:11`
   - `00:00:21 - 00:00:24`
3. 历史时间线中，无结束时间标注显示为：
   - `（00:00:11）`

结论：

1. 有结束时间时统一展示 `开始 - 结束`
2. 无结束时间时统一只展示开始时间
3. 当前视频标注、资源区、时间线三处口径一致

## 5. 产物路径

### 5.1 正式文档

1. `doc/manuscript_review/R9.7_第3轮执行记录_v01.md`

### 5.2 临时证据

1. `.agent-temp/r9_round3_browser_regression.mjs`
2. `.agent-temp/r9_round3/summary.json`
3. `.agent-temp/r9_round3/tc-r9-08-resource-shell-full-page.png`
4. `.agent-temp/r9_round3/tc-r9-09-single-link.png`
5. `.agent-temp/r9_round3/tc-r9-09-multi-link.png`
6. `.agent-temp/r9_round3/tc-r9-10-before-enable.png`
7. `.agent-temp/r9_round3/tc-r9-10-after-enable.png`
8. `.agent-temp/r9_round3/tc-r9-11-jump-list.png`
9. `.agent-temp/r9_round3/tc-r9-11-resource-group.png`
10. `.agent-temp/r9_round3/tc-r9-11-timeline.png`

## 6. 残留风险

1. 本轮为保证运行态证据完整，对 `reviewId=2038749832083783681` 做了最小补样；后续若继续复用该样本，需要意识到它已不再是最初的 `R8.5.2` 原始状态
2. `R9.2` 中样本 `C/D/E` 仍是抽象称谓，未绑定固定 `reviewId`；若下一轮还要接着用，建议把本轮实际命中的样本编号继续固化进后续文档
3. 当前工作树仍保留本轮前端代码改动与一个无关的 `plus-ui-ts/.eslintrc-auto-import.json` 变更，提交前需要注意不要混入无关文件

## 7. 下一轮是否可进入

按 `R9.3` 基线，可进入下一轮。

建议顺序：

1. 若严格按既定分轮执行，进入第 4 轮：首页高保真设计评审
2. 若游工选择暂缓首页设计，可直接进入第 5 轮总回归与收口判断

本轮结论：

1. 问题 7、8、9、10 已完成代码闭环
2. 已拿到真实浏览器截图与结构化摘要证据
3. 不需要再把第 3 轮保留为“待验证”状态
