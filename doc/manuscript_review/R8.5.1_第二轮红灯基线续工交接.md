Owner: Codex
Task: manuscript_review R8 第二轮红灯基线续工交接
Status: In Progress
Updated: 2026-03-30 17:28:00 +08:00

# R8.5.1_第二轮红灯基线续工交接

## 1. 当前是否建议切新会话

建议：可继续当前会话，但已经适合做一次 checkpoint 后切到新会话。

原因：
1. 本轮已经完成指定文档重读、第二轮边界锁定、代码位点核对、红灯测试补齐与红灯验证。
2. 现在的上下文里同时包含：
   - `R8.0 / R8.2 / R8.3 / R8.2.5 / R8.4.5 / R8.5`
   - 第二轮后端读写代码位点
   - 前端详情/表单代码位点
   - 前端 vitest 红灯
   - 后端 JDK17 + Maven red baseline
3. 还未开始真正实现；此时切会话成本最低，最适合把“红灯前状态”冻结。

## 2. 本轮已确认事实

1. 第二轮只做问题 `1 / 8 / 4 / 5`。
2. 问题 `4` 继续并入问题 `8`，不得拆开。
3. 问题 `2` 仍只补证，不改代码。
4. 问题 `6` 不进入。
5. 第一轮 gate 直接承接 `R8.4.5`，无需回退到问题 `9 / 3 / 7`。
6. 当前工作树用户边界外文件仍为：
   - `plus-ui-ts/.eslintrc-auto-import.json`
   - `test_qa_flow.py`
   这两个文件未触碰。

## 3. 本轮已完成动作

### 3.1 已读基线文档
1. `doc/manuscript_review/R8.0_问题排查续工交接.md`
2. `doc/manuscript_review/R8.2_问题池统一修复方案.md`
3. `doc/manuscript_review/R8.3_问题池分轮修复执行基线.md`
4. `doc/manuscript_review/R8.2.5_问题池统一回归测试用例.md`
5. `doc/manuscript_review/R8.4.5_第一轮运行态回归记录.md`
6. `doc/manuscript_review/R8.5_第二轮修复执行提示词.md`

### 3.2 已补的第二轮 failing tests
后端：
1. `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/controller/ManuscriptReviewApiControllerTest.java`
   - 新增 `shouldExposeSecondRoundPendingDeleteAndPreviewEndpoints`
   - 锁定：
     - `deletePendingResource`
     - `previewResource`
     - 路径：
       - `/resource/pending/{ossId}`
       - `/resource/preview/{resourceId}`
2. `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/controller/ManuscriptReviewSharedApiMvcTest.java`
   - 将详情返回里的附件/视频 `resourceUrl` 断言改为模块内同域预览路径：
     - `/workflow/manuscript-review/resource/preview/9401`
     - `/workflow/manuscript-review/resource/preview/9403`
     - `/workflow/manuscript-review/resource/preview/9404`
3. `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewReadableServiceTest.java`
   - 将读侧详情资源 URL 断言改为：
     - `/workflow/manuscript-review/resource/preview/1`
     - `/workflow/manuscript-review/resource/preview/2`
     - `/workflow/manuscript-review/resource/preview/4`
4. `Brain/brain-modules/brain-manuscript-review/src/test/java/com/gxpublish/brain/manuscript/review/service/ManuscriptReviewServiceTest.java`
   - 新增 `shouldExposePendingResourceDeletionMethodForSecondRoundIssueOne`
   - 锁定服务层应存在 `deletePendingResource(Long)`

前端：
1. `plus-ui-ts/src/views/manuscript-review/second-round-contract.spec.ts`
   - 锁定问题 1：
     - 前端 API 必须改为 `deletePendingManuscriptReviewResource`
     - 路径必须改为 `/workflow/manuscript-review/resource/pending/`
     - 不得继续保留 `url: \`/resource/oss/${ossId}\``
     - `form.vue` 不得继续使用 `deletePendingOssResource`
   - 锁定问题 5：
     - 详情页必须出现“新增视频标注”
     - 必须调用 `addManuscriptReviewVideoMark`
     - 提交后必须 `await fetchDetail()`

## 4. 已验证的红灯结果

### 4.1 前端 vitest 红灯
命令：
```powershell
& 'C:\kuguaHome\tools\devTools\nvm\v20.19.6\pnpm.cmd' exec vitest run src/views/manuscript-review/second-round-contract.spec.ts src/views/manuscript-review/detail-layout.spec.ts src/views/manuscript-review/form-state.spec.ts
```

结论：红灯有效，且落点符合第二轮预期。

失败点：
1. `index.ts` 里不存在 `deletePendingManuscriptReviewResource`
2. `detail-page-shell.vue` 里不存在“新增视频标注”操作区与新增调用

### 4.2 后端 JDK17 + Maven 定向红灯
注意：`Brain/pom.xml` 默认有 `<skipTests>true</skipTests>`，必须显式覆盖：
```powershell
$env:JAVA_HOME='C:\kuguaHome\env\java\jdk17'
$env:Path='C:\kuguaHome\env\java\jdk17\bin;' + $env:Path
mvn -pl brain-modules/brain-manuscript-review -am '-DskipTests=false' '-Dtest=ManuscriptReviewApiControllerTest#shouldExposeSecondRoundPendingDeleteAndPreviewEndpoints,ManuscriptReviewSharedApiMvcTest#shouldReturnFrozenDetailVoFromNewWriteSideFieldsForApi02,ManuscriptReviewReadableServiceTest#shouldAssembleReadableDetailWithFrozenFieldsAndCurrentResources,ManuscriptReviewServiceTest#shouldExposePendingResourceDeletionMethodForSecondRoundIssueOne' test
```

结论：4 条定向测试全部红灯，且全部落在第二轮目标上。

失败点：
1. `ManuscriptReviewApiControllerTest`
   - `deletePendingResource` endpoint 不存在
2. `ManuscriptReviewServiceTest`
   - `deletePendingResource(Long)` 方法不存在
3. `ManuscriptReviewSharedApiMvcTest`
   - 详情附件 `resourceUrl` 仍是 `https://files.example/current.pdf`
4. `ManuscriptReviewReadableServiceTest`
   - 读侧附件 `resourceUrl` 仍是 `https://files.example/a.pdf`

## 5. 当前尚未开始的实现

还没有开始写生产实现代码。

当前状态仍是：
1. 问题 1：表单暂存删除还走全局 `/resource/oss/{ossId}`
2. 问题 8 / 4：详情预览主能力还建立在 `fileUrl / 预签名 URL`
3. 问题 5：详情页还没有“新增视频标注”入口

## 6. 下一步唯一建议顺序

1. 先实现问题 1：
   - 新增 `deletePendingResource(Long)` 服务方法
   - 新增模块内 `DELETE /workflow/manuscript-review/resource/pending/{ossId}`
   - 前端 API 与 `form.vue` 切换到新接口
2. 再实现问题 8 / 4：
   - 新增同域预览接口 `/workflow/manuscript-review/resource/preview/{resourceId}`
   - `ReadableService` 资源 URL 改为模块内 preview 路径
   - PDF 走 inline
   - 视频支持 `Range / 206`
   - 下载链路保持独立
3. 最后实现问题 5：
   - 详情页补“新增视频标注”操作区
   - 复用现有 `POST /video-mark`
   - 成功后刷新标注列表与时间线

## 7. 执行禁区重申

1. 不得把问题 `4` 再拆成独立实现架构
2. 不得把问题 `2` 扩成代码修复轮
3. 不得回退到“继续用 OSS 预签名凑合”的主方案
4. 不得为问题 `1` 给审校角色开放全局 `system:oss:remove`
5. 不得顺手进入问题 `6`
6. 不得触碰 `plus-ui-ts/.eslintrc-auto-import.json`
7. 不得触碰 `test_qa_flow.py`
