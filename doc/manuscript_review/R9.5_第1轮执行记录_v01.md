Owner: Codex
Task: manuscript_review R9 round-1 execution record
Status: Done
Updated: 2026-03-31 16:32:00 +08:00

# R9.5_第1轮执行记录_v01

## 1. 范围

本轮严格锁定：

1. 问题 3：流程类型冻结
2. 问题 5：资源区不再展示技术链接
3. 问题 6：资源区补齐操作人、操作时间、文件大小
4. 问题 12：主动退出登录不再串页

未纳入本轮：

1. 问题 1、2、4、7、8、9、10、11
2. 问题 13 首页设计或实现
3. 旧 `brain_manuscript_review_history` 数据修补
4. 与 `R9` 无关的历史返修

## 2. 执行前复核结论

### 2.1 文档与真实代码冲突

`R9.1` 中问题 3、问题 6 的“当前状态”已经落后于真实代码：

1. 后端 `ManuscriptReviewService.update(...)` 当前已存在：
   - `assertProcessTypeImmutable(...)`
   - `applyWriteFields(... requireProcessType(existing.getProcessType()) ...)`
   - `entity.setFlowCode(existing.getFlowCode())`
2. 后端 `ManuscriptReviewDetailResponse.ResourceItemVO` / `ManuscriptReviewReadableService` 当前已存在：
   - `operatorName`
   - `operatorTime`
   - `fileSizeBytes`
   - `fileSizeLabel`

结论：

1. 本轮不回退这些现有后端补丁。
2. 本轮主要补齐前端冻结展示、修改页已入库资源展示、前端契约与验证证据。

### 2.2 工作树边界

执行前工作树已脏，且后端以下文件存在未提交修改：

1. `ManuscriptReviewService.java`
2. `ManuscriptReviewReadableService.java`
3. `ManuscriptReviewDetailResponse.java`
4. `ManuscriptReviewServiceTest.java`
5. `ManuscriptReviewReadableServiceTest.java`

本轮策略：

1. 视这些后端改动为当前基线。
2. 只在其上补最小前端与验证闭环。
3. 不覆盖、不回滚不属于本轮的已有未提交内容。

## 3. 本轮变更结果

### 3.1 前端

1. `plus-ui-ts/src/views/manuscript-review/form.vue`
   - 修改页 `processType` 改为禁用态，满足 `TC-R9-03`
   - 修改页已入库资源区新增元信息行展示
2. `plus-ui-ts/src/views/manuscript-review/detail.contract.ts`
   - 资源归一化时不再把附件/视频 `resourceUrl` 映射到 `note`
   - 为附件/视频/外链/视频标注补齐 `metaLines`
3. `plus-ui-ts/src/views/manuscript-review/components/formState.ts`
   - 修改页已入库资源区不再展示附件/视频技术链接
   - 外链继续展示真实 URL
   - 已入库资源区补齐操作人、操作时间、文件大小
4. `plus-ui-ts/src/views/manuscript-review/detail-page-shell.vue`
   - 资源区增加元信息行渲染
5. `plus-ui-ts/src/layout/components/Navbar.vue`
   - 主动退出登录后固定跳 `/login`
   - 不再携带旧 `redirect`
6. `plus-ui-ts/src/api/manuscript-review/types.ts`
   - 前端类型补齐资源元信息字段

### 3.2 测试

1. `plus-ui-ts/src/views/manuscript-review/detail-readable-display.spec.ts`
   - 补齐资源区元信息与不泄露技术链接断言
2. `plus-ui-ts/src/views/manuscript-review/form-state.spec.ts`
   - 补齐修改页已入库资源展示口径断言
3. `plus-ui-ts/src/views/manuscript-review/form-layout.spec.ts`
   - 增加修改页流程类型禁改断言
4. `plus-ui-ts/src/views/manuscript-review/login-redirect.spec.ts`
   - 新增主动退出登录不带旧 `redirect`
   - 保留未登录拦截回跳断言
5. `Brain/.../ManuscriptReviewReadableServiceTest.java`
   - 修正新增资源元信息断言中的 `Integer/Long` 类型不一致

## 4. 验证结果

### 4.1 后端定向测试

执行命令：

```powershell
mvn -pl brain-modules/brain-manuscript-review -am `
  "-Dtest=ManuscriptReviewServiceTest#shouldRejectUpdateWhenProcessTypeChangesAfterCreation+shouldKeepExistingFlowCodeWhenUpdateSavesMainForm,ManuscriptReviewReadableServiceTest#shouldAssembleReadableDetailWithFrozenFieldsAndCurrentResources" `
  "-Dsurefire.failIfNoSpecifiedTests=false" "-DskipTests=false" "-Dmaven.test.skip=false" test
```

结果：

1. `ManuscriptReviewReadableServiceTest#shouldAssembleReadableDetailWithFrozenFieldsAndCurrentResources` 通过
2. `ManuscriptReviewServiceTest#shouldRejectUpdateWhenProcessTypeChangesAfterCreation` 通过
3. `ManuscriptReviewServiceTest#shouldKeepExistingFlowCodeWhenUpdateSavesMainForm` 通过

结论：

1. 已证明 `update(...)` 不再接受流程类型变更
2. 已证明 `update(...)` 保留已有 `flowCode`
3. 已证明详情读侧输出资源元信息字段

### 4.2 前端单测

通过的 targeted 用例：

1. `detail-readable-display.spec.ts`
   - `normalizes frozen detail payload without leaking technical fields into summary or permissions into ledger space`
   - `keeps external links clickable in readable resource items`
2. `form-state.spec.ts`
   - `creates edit draft state from detail payload without polluting pending resources`
   - `creates edit draft state from raw detail response payload for edit-page hydration`
3. `form-layout.spec.ts`
   - `freezes processType on edit and does not require submitDepartment`
4. `login-redirect.spec.ts`
   - 全部 2 个用例通过

说明：

1. 这些是本轮新增或本轮直接相关的 targeted 断言
2. 未将同文件内与本轮无关的历史 baseline 红项混写为“本轮失败”

### 4.3 浏览器级验证

运行态：

1. 前端 `http://127.0.0.1:9999`
2. 后端 `http://127.0.0.1:9888`

`TC-R9-13`：

1. 使用 `mr_initiator / 666666 / 1234` 登录进入 `/index`
2. 点击右上角“退出登录”
3. 跳转到纯 `/login`
4. 再用 `mr_observer / 666666 / 1234` 登录
5. 成功落到 `/index`
6. 未被带回旧的审校页面

`TC-R9-14`：

1. 未登录直接访问 `/manuscript/review/my-document`
2. 被拦截到 `/login?redirect=/manuscript/review/my-document`
3. 使用 `mr_initiator / 666666 / 1234` 登录
4. 成功回跳到 `/manuscript/review/my-document`

结论：

1. 主动退出串页问题已关闭
2. 未登录拦截后的正常 `redirect` 回跳机制保留

### 4.4 运行态说明

1. 初始尝试 `mvn ... spring-boot:run` 失败，原因是 Maven 前缀插件命令在当前 reactor 入口下不可用，不是 JDK 问题
2. 改为直接运行 `Brain/brain-admin/target/brain-admin.jar` 后，后端正常启动
3. 后端运行中存在 `snail-job` / `spring-boot-admin` 外围连接告警，但不阻断本轮 manuscript_review 登录与浏览器验证

## 5. 残留风险

1. 后端主代码相关未提交补丁并非本轮新写，当前仍处于脏工作树状态；若后续会话误以为这些改动已入库，存在承接偏差风险
2. manuscript-review 后端全类测试目前仍存在与本轮无关的 baseline 红项，不能把“方法级 3 条绿”误报成“后端全绿”
3. 本轮未覆盖问题 7、8、9、10、11，因此资源区布局、单条外链样式、启用按钮隐藏、视频标注时间统一仍待下一轮
4. 首页仍是默认 RuoYi 页面，不属于本轮范围

## 6. 下一轮是否可进入

可进入 `R9` 第 2 轮。

进入前提已满足：

1. `flowCode` 不再被修改页篡改
2. 资源区已切换为业务元信息展示，不再直接暴露附件/视频技术链接
3. 主动退出登录串页问题已关闭

建议下一轮起点：

1. 继续读 `R9.1 / R9.2 / 本文`
2. 范围锁到问题 1、2、4、11
3. 不回头重做本轮已闭环项
