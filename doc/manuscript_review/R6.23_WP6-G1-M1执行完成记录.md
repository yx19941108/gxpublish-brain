Owner: Codex
Task: manuscript_review WP6-G1-M1 执行完成记录
Status: Final
Updated: 2026-03-25 15:26 +08:00

# R6.23_WP6-G1-M1执行完成记录

## 已确认

- 本轮继续严格锁定在后端 `manuscript_review` BPM/runtime 主线；未改 `plus-ui-ts/**`，未改 `Brain/brain-modules/brain-workflow/**` 通用 Java，未进入 `R7`。
- 本地 dev/test 库已具备 manuscript workflow seed：
  - `flow_definition` 已落 `manuscript_review_audit_flow`、`manuscript_review_proofread_flow`
  - 两条流程的 `formPath` 均为 `/manuscript/review/approval`
  - 每条流程已落 `6` 个节点、`5` 条 skip
  - `brain_manuscript_review_flow_config` 已落 `AUDIT / PROOFREAD` 两条配置
- JDK17 定向静态验证 fresh 通过：`62/62` 绿。
- 已重打并重启 fresh `brain-admin.jar`，`GET /auth/code` 返回 `200`。
- 真实登录态已 fresh 确认：
  - `clientId=e5cd7e4891bf95d1d19206ce24a7b32e`
  - `grantType=password`
  - `mr_*` 账号密码为 `666666`
  - 受保护接口需同时携带 `Authorization: Bearer <token>` 与 `clientid: e5cd7e4891bf95d1d19206ce24a7b32e`
- `create -> resource -> submitAndFlowStart -> detail` 已 fresh 打通：
  - `reviewId=2036703922518052866`
  - `resourceId=2036703922912317442`
  - `submitAndFlowStart` 返回 `200`
  - 业务详情显示 `businessStatus=WAITING`、`currentNodeLabel=待一级审批`
- “去审批”真值三件套已在运行态确认成立：
  - 一级审批人读取 manuscript 详情时，`permissionMatrix.canGotoApproval=true`
  - `GET /workflow/instance/getInfo/2036703922518052866` 返回 `formPath=/manuscript/review/approval`
  - 一级审批待办 `GET /workflow/task/pageByTaskWait?...businessId=2036703922518052866` 返回真实 `taskId=2036703925995130882`
- 发起人撤销闭环已 fresh 打通：
  - 撤销对象：`reviewId=2036703922518052866`
  - `PUT /workflow/manuscript-review/cancelProcessApply` 返回 `200`
  - 业务详情变更为 `businessStatus=CANCEL`、`businessStatusLabel=已取消`
  - 当前节点变更为 `currentNodeCode=FLOW_CANCELED`、`currentNodeLabel=流程已取消`
  - 实例接口变更为 `flowStatus=cancel`
  - `flowHisTaskList` 已出现撤销记录
  - 一级审批待办已清空
- 三级驳回终止闭环已 fresh 打通：
  - 驳回对象：`reviewId=2036705684377391106`
  - 一级待办 `taskId=2036705685727956994` 已通过
  - 二级待办 `taskId=2036705687074328578` 已通过
  - 三级待办 `taskId=2036705687925772289` 已终止
  - 业务详情变更为 `businessStatus=REJECT`、`businessStatusLabel=已驳回`
  - 当前节点变更为 `currentNodeCode=FLOW_REJECTED`、`currentNodeLabel=流程已驳回`
  - 实例接口变更为 `flowStatus=termination`
  - `flowHisTaskList` 已出现一级通过、二级通过、三级终止三条历史
  - 三级待办已清空
- 运行态负向约束已 fresh 确认：
  - 越权审批被拒绝：无关用户尝试办理一级任务返回 `code=500`，`msg=无法跳转到该节点,请检查当前用户是否有权限!`
  - 审批中重复提交被拒绝：返回 `code=500`，`msg=该单据已提交过申请,正在审批中！`
  - 已取消流程重复撤销被拒绝：返回 `code=500`，`msg=仅审批中的流程可撤销`
  - 已驳回流程再次提交被拒绝：返回 `code=500`，`msg=仅退回给发起人的流程可再次提交`

## 风险

- 当前 manuscript 详情契约仍未承载 `taskId` 与 `formPath` 字段；虽然运行态已证明三件套可以从 `permissionMatrix + workflow instance + workflow task wait` 拼出，但 manuscript 单接口尚未直接给出完整“去审批”上下文。
- 本轮未补“退回给发起人后再次提交”的 runtime fresh 证据；`resubmit` 的运行态正向闭环仍待单独补证。
- 本轮未补“持证发起人跳过一级审批”的 runtime fresh 证据；当前只确认普通发起人三层链路。
- 本轮结论只覆盖后端 API/runtime，不覆盖前端审批页交互，也不代表 `WP6-G1` 已自动通过。

## 待确认

- 主控是否接受“`permissionMatrix + formPath + taskId` 已在运行态分端点成立，但 manuscript 详情未聚合”的现状作为本轮 Gate 证据边界。
- 是否还要求补 `resubmit` runtime 正向闭环与 `mr_certified*` 跳级 runtime 正向闭环，再回主控复判。
- 若主控要求 manuscript 详情直接承载完整审批上下文，是否明确授权新增 shared/detail 契约字段。

## 推进方案

1. 以本记录回主控，先做 `WP6-G1` 复判，不把当前结果外推成 `R7 可启动`。
2. 若主控认为“分端点三件套”足够，则本轮后端 BPM/runtime 主线可视为已补齐当前缺口。
3. 若主控认为详情聚合仍必须补齐，则下一最小包只应围绕 manuscript 详情承载 `taskId/formPath` 的边界修补，不应顺手扩前端或 workflow 通用层。
4. 若主控要求再补正向链路，则优先补：
   - `resubmit` runtime 正向闭环
   - `mr_certified*` 跳级 runtime 正向闭环

## Verification

- JDK17 定向测试：
  - `mvn -f Brain/pom.xml --% -pl brain-modules/brain-manuscript-review -am -Dtest=TableBackedManuscriptReviewSerialGatewayTest,ManuscriptReviewServiceTest,ManuscriptReviewReadableServiceTest,ManuscriptReviewSharedApiMvcTest,ManuscriptReviewApiControllerTest,ManuscriptReviewFlowRoutingServiceTest,ManuscriptReviewDetailPermissionPolicyTest,ManuscriptReviewReadableVisibilityTest,ManuscriptReviewServiceSpringConstructorTest -Dsurefire.failIfNoSpecifiedTests=false -DskipTests=false test`
  - 结果：`62/62` 通过
- 重新打包：
  - `mvn -f Brain/pom.xml -pl brain-admin -am -DskipTests package`
  - 结果：`BUILD SUCCESS`
- 服务存活：
  - `GET /auth/code` -> `200`
- 核心 fresh 运行态对象：
  - submit 成功样本：`reviewId=2036703922518052866`
  - cancel 成功样本：`reviewId=2036703922518052866`
  - reject 成功样本：`reviewId=2036705684377391106`
  - negative 样本：`reviewId=2036706017933611010`
- 核心 fresh taskId：
  - 初次一级待办：`2036703925995130882`
  - reject 样本一级待办：`2036705685727956994`
  - reject 样本二级待办：`2036705687074328578`
  - reject 样本三级待办：`2036705687925772289`

## Changed Files

- 新增本文档：`doc/manuscript_review/R6.23_WP6-G1-M1执行完成记录.md`
- 本轮未新增或修改源码文件。
- 本轮存在本地 dev/test 数据库运行态变更与 `.agent-temp/` 临时取证文件，但不属于版本化源码交付。

## Next Gate

- 本轮可上报口径仅为：
  - `manuscript_review` 后端 BPM/runtime 主线已补到 fresh 运行态证据
  - `FR-05 / FR-06 / FR-07 / FR-08 / PM-06 / NE-06` 已拿到本轮要求的关键后端运行态样本
  - 可回主控继续推 `WP6-G1` 复判
- 本轮不可上报为：
  - `WP6-G1 已自动通过`
  - `R7 可直接启动`
  - `前后端联调已完成`
