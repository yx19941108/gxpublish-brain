Owner: Codex
Task: manuscript_review 正式测试续工提示词-普通主链闭环后继续持证L3与分支场景
Status: Ready
Updated: 2026-03-27 15:31 +08:00

# R7.12_正式测试续工提示词-普通主链闭环后继续持证L3与分支场景

你在 `C:\kuguaHome\project\gxpublish-brain` 继续 `manuscript_review` 的正式全量测试。

## 1. 先读这些文件，不要回到旧聊天里找线索

1. `doc/manuscript_review/R7.4_正式测试方案-全需求覆盖.md`
2. `doc/manuscript_review/R7.8_正式测试进度记录-fresh提交流程阻塞修复与双链路复测.md`
3. `doc/manuscript_review/R7.10_正式测试问题记录-审批通过后原审批人被带回无权详情页.md`
4. `doc/manuscript_review/R7.11_正式测试进度记录-普通主链闭环与审批后回跳权限问题复现.md`

## 2. 当前已经确认的真实事实

1. 前后端环境已重新恢复：
   - `http://127.0.0.1:9999` -> `200`
   - `http://127.0.0.1:9888` -> `200`
2. 普通 fresh 主链样本已完整闭环：
   - `reviewId = 2037413044715327490`
   - 已完成 `L1 -> L2 -> L3 -> FINISH`
   - MySQL 当前：
     - `flow_status_label = 已完成`
     - `current_node_label = 流程完成`
     - `current_node_status = FLOW_FINISHED`
3. 持证 jump 样本已继续推进到三级待办：
   - `reviewId = 2037413420613046274`
   - 历史已包含：
     - `CREATE`
     - `SKIP_LEVEL_1`
     - `APPROVE`
   - MySQL 当前：
     - `current_node_label = 待三级审批`
     - `current_node_status = LEVEL_3`
4. 新问题 `R7.10` 已确认不是单点一级问题，而是通用问题：
   - 审批提交 `completeTask` 返回 `200`
   - MySQL 真实推进
   - 但前端把刚完成审批的原审批人跳回详情页
   - 详情接口真实返回：
     - `code = 500`
     - `msg = 当前用户无权查看该流程`
   - 浏览器显示：
     - `详情数据加载失败，请稍后重试。`
5. `R7.10` 已在这些场景复现：
   - 普通链路一级审批后
   - 普通链路二级审批后
   - 普通链路三级审批后
   - 持证链路二级审批后

## 3. 当前不要重复做的事

1. 不要再回头重新证明旧 jar 为什么错配。
2. 不要再重新证明普通链路 L1/L2/L3 是否已经审批成功。
3. 不要把 `R7.10` 误记成“审批失败”，它是“审批成功后的错误回跳/承接问题”。

## 4. 下一步应该直接做什么

按下面顺序继续：

1. 先确认环境还活着：
   - `http://127.0.0.1:9999`
   - `http://127.0.0.1:9888`
2. 先把持证样本继续推进到三级审批并确认是否最终闭环：
   - `mr_approver_l3`
   - `reviewId = 2037413420613046274`
3. 再继续分支场景：
   - 退回后修改保存与再次提交
   - 审批中修改后再去审批
   - 发起人撤销
   - 三级审批驳回终止
4. 分支场景稳定后，再继续：
   - 附件 / 视频 / 视频标注
   - 资源停用 / 预览 / 下载
   - 我的待办 / 我的已办 / 菜单权限 / 按钮跳转

## 5. 执行纪律

1. 每发现一个新问题，单独在 `doc/manuscript_review/` 新增问题单，不要混写进已有进度文件。
2. `R7.10` 已经是通用问题，不要因 L3 或持证链再次复现就重复新增同类问题单；只有新类型问题才新增。
3. 继续保持普通链路和持证链路分开留证，不要压成一句“fresh 已通过”。
4. 如果上下文再次变长，优先再落新的进度记录或续工提示词，不把关键判断链只留在聊天里。
