Owner: Codex
Task: manuscript_review 第四队列第2轮负向校验与用例55补证
Status: Passed
Updated: 2026-03-29 23:05:00 +08:00

# R7.32_正式测试执行记录-第四队列第2轮负向校验与用例55补证

## 1. 本轮承接边界

本轮只从以下文档恢复上下文：

1. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.30_正式测试执行记录-第三队列当前态最小回归.md`
2. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.26_正式测试待办基线-四队列收口计划.md`
3. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.4_正式测试方案-全需求覆盖.md`
4. `/home/kugua/dev/gxpublish-brain/doc/manuscript_review/R7.31_正式测试执行记录-第四队列第1轮资源生命周期与追加语义.md`

沿用当前前提：

1. 第二队列已通过。
2. 第三队列已通过。
3. 第四队列第 1 轮已完成，且本轮只以 `R7.31` 为唯一承接依据。
4. manuscript-review 仍走专属任务页：
   - `/manuscript/review/my-document`
   - `/manuscript/review/task-waiting`
   - `/manuscript/review/task-finish`
5. SQL 改动统一收口在：
   - `/home/kugua/dev/gxpublish-brain/Brain/script/sql/update/manuscript_review/`

## 2. 第四队列第 2 轮范围与判定口径

### 2.1 本轮正式范围

本轮正式范围固定为：

1. `用例-32`
2. `用例-33`
3. `用例-34`
4. `用例-35`
5. `用例-36`
6. `用例-55`

其中：

1. `用例-32` 至 `用例-36` 为本轮负向主线。
2. `用例-55` 是按 `R7.31` 结论追加补盖章的唯一非负向项。

### 2.2 本轮不纳入

本轮不扩到：

1. 第四队列第 1 轮资源生命周期项重铺
2. 第三轮修改历史与追加语义项：
   - `用例-49`
   - `用例-50`
3. 未做的第四队列剩余项：
   - `用例-31`

### 2.3 当前判定口径

继续沿用 `R7.31` 的保守口径：

1. `通过`
   - 本轮当前态直接证据与 `R7.4` 预期一致。
2. `未通过`
   - 本轮当前态直接证据与 `R7.4` 预期明确冲突。
3. `证据不足`
   - 现有证据不足以证明通过，也没有直接反证。

补充说明：

1. 脚本入口或等待条件过时，只记为“脚本问题”，不直接算产品失败。
2. 若当前页面没有对应录入控件，但业务接口存在，则允许以接口当前态直证为主。

## 3. 工作树与运行前置

### 3.1 分支与工作树

已确认：

1. 当前分支仍为：
   - `manuscript-review-tdd-20260321`
2. 本轮开始时存在上一轮未跟踪正式工件：
   - `R7.31_正式测试执行记录-第四队列第1轮资源生命周期与追加语义.md`
3. 本轮实际新增/修改的持久化文件只有：
   - `plus-ui-ts/src/views/manuscript-review/form.vue`
   - `plus-ui-ts/src/views/manuscript-review/form-layout.spec.ts`
   - 本文

### 3.2 运行态

本轮开始时已确认当前运行态仍可直接执行：

1. `http://127.0.0.1:9999/` 返回 `200`
2. `http://127.0.0.1:9888/actuator/health` 返回 `401`
3. 监听情况：
   - `127.0.0.1:9999 -> node(vite)`
   - `*:9888 -> java(brain-admin.jar)`

结论：

1. 本轮无需重启前后端即可执行。

## 4. 本轮复用能力

本轮继续复用：

1. `.agent-temp/manuscript_review_r7_28/` 下既有 `login + token 注入 + Playwright + API` 骨架
2. 现成样本文件：
   - `doc/manuscript_review/test_file/meeting_01.mp4`
   - `doc/manuscript_review/test_file/2b16ea3347bb4138af5a26e4233b9cae.pdf`
3. 现有生成方式：
   - `POST /workflow/manuscript-review/submitAndFlowStart`
   - `POST /workflow/manuscript-review/video-mark`
   - `GET /workflow/manuscript-review/{id}`
   - `GET /workflow/manuscript-review/list`

## 5. 本轮实际执行

### 5.1 当前态证据脚本

本轮新增临时脚本：

1. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_32_queue4_round2_evidence.mjs`

输出工件：

1. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_32_queue4_round2_evidence.json`
2. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_32_case34_empty_resource.png`
3. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_32_case35_invalid_protocol.png`
4. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_32_case36_required_validation.png`
5. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_32_case55_create_page.png`
6. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_32_case55_edit_page.png`
7. `/home/kugua/dev/gxpublish-brain/.agent-temp/manuscript_review_r7_28/r7_32_case55_detail_page.png`

### 5.2 `用例-55` 当前态失败与最小修复

本轮首次跑当前态时，`用例-55` 结果为：

1. 新增页外链区域无可点击锚点
2. 修改页已入库外链区域无可点击锚点
3. 详情页外链可点击跳转

结论：

1. 这不是脚本问题，而是前端当前实现面缺口
2. 根因收敛到：
   - `plus-ui-ts/src/views/manuscript-review/form.vue`

本轮在同一边界内做了最小前端修补：

1. 新增页外链录入区补充“预览跳转”链接
2. 修改页已入库外链改为真实可点击链接
3. 不改后端，不碰第 3 轮修改历史逻辑

### 5.3 前端最小规格测试

已执行：

```bash
cd /home/kugua/dev/gxpublish-brain/plus-ui-ts
bash -ic 'npm exec vitest run src/views/manuscript-review/form-layout.spec.ts src/views/manuscript-review/form-state.spec.ts'
```

结果：

1. `2` 个测试文件全部通过
2. `11` 个测试全部通过

## 6. 逐项结论

### 6.1 通过项

#### 用例-32 非法时间格式拦截

结论：

1. 通过

本轮证据：

1. 对含视频样本执行：
   - `POST /workflow/manuscript-review/video-mark`
2. 入参：
   - `startTimeText = 3:7`
   - `endTimeText = 00:00:10`
3. 当前返回：
   - `code = 500`
   - `msg = 标注开始时间格式不合法`
4. 标注数量前后均为：
   - `0`

#### 用例-33 结束时间早于开始时间拦截

结论：

1. 通过

本轮证据：

1. 对同一视频样本执行：
   - `startTimeText = 00:00:10`
   - `endTimeText = 00:00:05`
2. 当前返回：
   - `code = 500`
   - `msg = 标注结束时间不能早于开始时间`
3. 标注数量仍为：
   - `0`

#### 用例-34 附件与外链全空拦截

结论：

1. 通过

本轮证据：

1. 浏览器新增页填写主表字段后，不上传附件、不填外链，真实点击 `提交`
2. 当前请求命中：
   - `/dev-api/workflow/manuscript-review/submitAndFlowStart`
3. 当前返回：
   - `code = 500`
   - `msg = 稿件至少需要一种有效资源`
4. 页面仍停留在：
   - `/manuscript/review/form?mode=create`
5. 以本轮唯一标题检索正式台账结果为：
   - `listCountByKeyword = 0`

#### 用例-35 外链非法协议拦截

结论：

1. 通过

本轮证据：

1. 浏览器新增页录入：
   - `ftp://example.com/ref`
2. 真实点击 `提交` 后当前返回：
   - `code = 500`
   - `msg = 外链只允许http/https协议`
3. 页面仍停留在：
   - `/manuscript/review/form?mode=create`
4. 以本轮唯一标题检索正式台账结果为：
   - `listCountByKeyword = 0`

#### 用例-36 字段长度与必填校验

结论：

1. 通过

本轮证据按“最小但充分”拆为三组：

1. 页面必填直证：
   - 清空正文后点击 `提交`
   - 页面直接提示：
     - `请输入正文内容`
   - `submitRequestCount = 0`
2. 后端长度直证：
   - 超长标题返回：
     - `标题长度不能超过200个字符`
   - 超长说明返回：
     - `说明长度不能超过1000个字符`
3. 作者分隔符直证：
   - `authorName = 张三,李四`
   - 返回：
     - `作者仅允许使用中文顿号分隔`
   - 正式台账无新增记录

#### 用例-55 外链在新增暂存区、修改页、详情页都可点击跳转

结论：

1. 通过

本轮先拿到当前态失败证据，再经最小修补后复跑通过。

最终通过证据：

1. 新增页：
   - 外链录入后已出现可点击：
     - `预览跳转`
2. 修改页：
   - 已入库外链已显示为真实链接：
     - `https://example.com/r7-32/q4/55/20260329150038`
3. 详情页：
   - 外链名称点击后真实打开：
     - `https://example.com/r7-32/q4/55/20260329150038`
   - 页面标题：
     - `Example Domain`

### 6.2 未通过项

本轮正式范围内：

1. 无

### 6.3 证据不足项

本轮正式范围内：

1. 无

## 7. 本轮覆盖清单

本轮覆盖的第四队列用例如下：

1. `用例-32`
2. `用例-33`
3. `用例-34`
4. `用例-35`
5. `用例-36`
6. `用例-55`

结果汇总：

1. 通过：
   - `用例-32`
   - `用例-33`
   - `用例-34`
   - `用例-35`
   - `用例-36`
   - `用例-55`
2. 未通过：
   - 无
3. 证据不足：
   - 无

## 8. 是否建议进入第四队列第 3 轮

建议：

1. 是

原因：

1. 第四队列第 2 轮已收口。
2. 但第四队列整体仍未完全收口，剩余至少包括：
   - `用例-31`
   - `用例-49`
   - `用例-50`
3. 其中：
   - `用例-31` 属于视频标注停用尾项
   - `用例-49 / 50` 属于修改历史与追加语义项，正是下一轮应承接的主范围

## 9. 最终结论

对“第四队列第 2 轮现在是否可收口”的直接回答是：

1. **可以收口。**

本轮原本唯一的高风险项是 `用例-55`。
在拿到当前态失败证据并完成最小前端修补后，`用例-55` 已复跑通过，因此：

1. 第四队列第 2 轮范围内所有用例均已通过
2. 可以进入第四队列第 3 轮，继续处理：
   - `用例-31`
   - `用例-49`
   - `用例-50`
