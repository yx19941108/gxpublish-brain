Owner: Codex
Task: manuscript_review R8 第三轮启用能力扩展轮收口
Status: Done
Updated: 2026-03-31 11:35:00 +08:00

# R8.6.3_第三轮启用能力扩展轮收口记录

## 1. 文档定位

本文用于正式收口 `gxpublish-brain / manuscript_review / R8` 第三轮 `问题 6: enable 能力` 的实现、静态验证、运行态回归与补修缺陷处理结果。

本文承接并裁决以下基线：

1. `doc/manuscript_review/R8.2_问题池统一修复方案.md`
2. `doc/manuscript_review/R8.2.5_问题池统一回归测试用例.md`
3. `doc/manuscript_review/R8.3_问题池分轮修复执行基线.md`
4. `doc/manuscript_review/R8.6.0_第三轮启用能力扩展轮新会话提示词.md`
5. `doc/manuscript_review/R8.6.1_第三轮启用能力扩展轮阶段交接.md`
6. `doc/manuscript_review/R8.6.2_第三轮启用能力扩展轮下一轮提示词.md`

本文不重新讨论第一轮、第二轮方案，不扩展问题 `2` 为代码修复，不回退已冻结的第三轮边界。

## 2. 本轮边界与最终结论

### 2.1 第三轮边界

第三轮只处理问题 `6`：

1. 资源 enable
2. 视频标注 enable
3. enable 仅挂在停用 history 上
4. 旧 disable history 保留
5. enable 追加新 history

冻结规则继续成立：

1. 所有停用必须落历史
2. 所有启用仅在历史中做
3. 当前有效资源区与当前有效标注列表仍只展示有效项
4. 不新增“已停用资源区”
5. 前端停用/启用按钮仅在 `permissionMatrix.canEdit = true` 时显示
6. 后端 service 继续用 `ensureCanModify(...)` 做等价权限兜底

### 2.2 最终结论

结论：

1. 第三轮问题 `6` 已完成实现闭环
2. 第三轮最小回归 `TC-15 / TC-16 / TC-17` 已通过
3. 相邻能力补证 `TC-18 / TC-13 / TC-14 / TC-23` 已通过
4. 第三轮运行态 gate 已达到通过条件

### 2.3 本轮补修缺陷结论

在第三轮运行态回归后，又发现一个真实缺陷：

1. enable 后 `enabled=1`
2. 但 `disabled_by / disabled_time` 未真正清空

根因已确认：

1. 原实现走 `updateById`
2. null 字段未被真实写回数据库

补修结果：

1. 已改为显式 `UpdateWrapper` 更新
2. 对以下三条链路统一清空：
   - `disabled_by`
   - `disabled_time`
   - `remark`
3. 修复后运行态复测已确认：
   - 资源 enable 后停用字段被清空
   - 视频标注 enable 后停用字段被清空

## 3. 已确认事实

### 3.1 后端实现

已存在并已验证：

1. `PUT /workflow/manuscript-review/resource/enable`
2. `PUT /workflow/manuscript-review/video-mark/enable`
3. history action:
   - `RESOURCE_ENABLE`
   - `VIDEO_MARK_ENABLE`
4. service 权限兜底仍为 `ensureCanModify(...)`

### 3.2 读侧与前端行为

已存在并已验证：

1. timeline 已保留 enable 所需结构化目标：
   - `eventCode`
   - `relatedResourceId`
   - `relatedResourceType`
2. enable 按钮只挂在停用 history 项，不挂在当前有效资源区
3. `canEdit=false` 账号不显示 enable / disable
4. `canEdit=true` 账号显示 enable / disable
5. enable 成功后详情页重新 `fetchDetail()`

### 3.3 问题 2 的补充裁决

游工已在本轮明确补充：

1. 问题 `2` 已被证实不是当前代码主线缺陷
2. 该问题是游工本机环境问题
3. 在其他电脑上表现正常

因此当前口径调整为：

1. 问题 `2` 不再作为本轮代码修复 blocker
2. 问题 `2` 不再阻塞第三轮和 `R8` 主线收口

## 4. 静态验证

### 4.1 第三轮定向测试

已通过：

1. `ManuscriptReviewApiControllerTest#shouldExposeThirdRoundEnableEndpoints`
2. `ManuscriptReviewReadableServiceTest#shouldExposeNewHistoryActionTypesInTimelineItems`
3. `ManuscriptReviewThirdRoundServiceTest`
   - 资源 enable
   - 外链 enable
   - 视频标注 enable

### 4.2 本轮补修针对性结论

修复前：

1. 定向测试已被改造成先红
2. 明确证明实现仍走 `updateById`

修复后：

1. 定向测试恢复为绿色
2. 明确证明实现已改为显式更新停用字段

## 5. 运行态回归结果

### 5.1 必测用例

1. `TC-15`：通过
2. `TC-16`：通过
3. `TC-17`：通过

### 5.2 补测用例

1. `TC-18`：通过
2. `TC-13`：通过
3. `TC-14`：通过
4. `TC-23`：通过

## 6. 关键证据摘要

### 6.1 浏览器证据

1. `mr_initiator` 视角：
   - `canEdit=false`
   - 不显示 enable / disable
2. `mr_approver_l1` 视角：
   - `canEdit=true`
   - 停用 history 下出现 enable
3. 资源 enable 后：
   - 附件重新回到当前有效资源区
4. 视频标注 enable 后：
   - 标注重新回到当前有效标注列表
5. 新增标注后：
   - 当前有效标注区和时间线立即刷新
6. 视频播放仍可继续推进，未破坏 `206` 预览链路

### 6.2 接口证据

1. `PUT /dev-api/workflow/manuscript-review/resource/enable -> 200`
2. `PUT /dev-api/workflow/manuscript-review/video-mark/enable -> 200`
3. `POST /dev-api/workflow/manuscript-review/video-mark -> 200`
4. 视频资源预览仍返回：
   - `206 Partial Content`
   - `Accept-Ranges: bytes`
   - `Content-Range: bytes ...`

### 6.3 数据证据

修复前旧样本曾出现：

1. `enabled=1`
2. 但 `disabled_by / disabled_time` 未清空

修复后复测样本已确认：

1. 视频标注 `id=2038806266252709889`
   - 停用后：`enabled=0, disabled_by=64001, disabled_time=2026-03-31 11:29:53`
   - 启用后：`enabled=1, disabled_by=NULL, disabled_time=NULL`
2. 附件 `id=2038409700235587585`
   - 停用后：`enabled=0, disabled_by=64001, disabled_time=2026-03-31 11:32:30`
   - 启用后：`enabled=1, disabled_by=NULL, disabled_time=NULL`
3. history 追加正常：
   - 新 `DISABLE`
   - 新 `ENABLE`
   - 旧 `DISABLE` 未被覆盖

## 7. 对 R8 主线的影响判断

### 7.1 当前判断

1. 第一轮 `9 / 3 / 7`：已闭环
2. 第二轮 `1 / 8 / 4 / 5`：已闭环
3. 第三轮 `6`：已闭环

因此：

1. `R8` 三轮主线修复工作可视为完成

### 7.2 关于问题 2

由于游工已补充确认：

1. 问题 `2` 为游工本机环境问题
2. 其他电脑表现正常

当前建议口径为：

1. 问题 `2` 不再作为当前主线阻塞项
2. `R8` 主线可进入上线前最终收口与发布判断

## 8. 风险与未决项

### 8.1 已接受项

1. 历史脏数据不追补

说明：

1. 本次修复保证“修复后的新 enable 动作”会把停用字段清空
2. 不自动回填修复前已写脏的老记录
3. 游工已明确表示历史脏数据不需要处理

### 8.2 当前剩余未决

当前剩余未决已不在第三轮实现内，而在上线口径：

1. 是否在上线前再做一次压缩守门回归
2. 是否直接进入最终 handoff / 发布判断

## 9. 建议的下一步

建议下一步转入“上线前收口”，不再开新实现：

1. 落最终 handoff / 下一会话提示词
2. 根据上线口径决定是否执行一次压缩守门回归
3. 输出最终发布判断

建议的压缩守门回归集：

1. 第一轮守门：
   - `TC-26`
   - `TC-28`
   - `TC-05`
   - `TC-18`
2. 第二轮守门：
   - `TC-22`
   - `TC-23`
3. 第三轮守门：
   - `TC-15`
   - `TC-16`
   - `TC-17`

## 10. 收口结论

最终结论：

1. 第三轮启用能力扩展轮已收口完成
2. 第三轮补修缺陷“enable 后停用字段未清空”已修复并复测通过
3. 问题 `2` 已被确认是游工本机环境问题，不再阻塞当前主线
4. `R8` 三轮主线已具备进入上线前最终收口的条件
