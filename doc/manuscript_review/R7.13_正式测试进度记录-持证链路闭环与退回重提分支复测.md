Owner: Codex
Task: manuscript_review 正式测试进度记录-持证链路闭环与退回重提分支复测
Status: In Progress
Updated: 2026-03-27 16:12 +08:00

# R7.13_正式测试进度记录-持证链路闭环与退回重提分支复测

## 1. 本文在承接什么

本文承接 `R7.11` 之后的正式全量测试，记录两类新增事实：

1. 持证链路样本 `2037413420613046274` 是否已经真实完成三级审批并最终闭环
2. 分支场景“退回后修改保存与再次提交”是否能真实走通，以及本轮是否发现新的分支类问题

## 2. 本轮已确认事实

### 2.1 环境仍然存活

本轮继续执行前重新核活：

1. `http://127.0.0.1:9999/` -> `HTTP 200`
2. `http://127.0.0.1:9888/` -> `HTTP 200`
3. 当前后端进程：
   - `java.exe = C:\kuguaHome\env\java\jdk17\bin\java.exe`
   - `PID = 77684`
4. 当前 jar：
   - `Brain/brain-admin/target/brain-admin.jar`
   - `LastWriteTime = 2026-03-27 14:08:35`

### 2.2 持证链路样本已真实闭环

继续使用 `R7.11` 中已推进到三级待办的持证样本：

- `reviewId = 2037413420613046274`
- `manuscript_code = SH20260327006`

#### 浏览器层

1. 使用 `mr_approver_l3` 打开详情页，真实看到：
   - `审批中 / 待三级审批`
2. 点击 `去审批` 成功进入：
   - `/manuscript/review/approval?id=2037413420613046274&type=approval&taskId=2037431878432071682`
3. 点击 `审批办理`，填写意见后提交

#### 接口层

三级审批真实命中：

- `POST /dev-api/workflow/task/completeTask`
- 请求体关键字段：
  - `taskId = 2037431878432071682`
  - `message = 正式测试 持证链 L3 审批通过 - 2026-03-27 15:46`
- 返回：
  - `code = 200`
  - `msg = 操作成功`

#### MySQL 层

主表最终闭环为：

- `flow_status_label = 已完成`
- `current_node_label = 流程完成`
- `current_node_status = FLOW_FINISHED`

历史链路最终为：

1. `CREATE`
2. `SKIP_LEVEL_1`
3. `APPROVE`
4. `FINISH`

结论：

- 持证 fresh 样本 `2037413420613046274` 已拿到浏览器 / 接口 / MySQL 三层证据，确认真实完成三级审批并最终闭环。

### 2.3 `R7.10` 在持证三级审批后继续命中

三级审批提交成功后，前端仍自动跳回：

- `/manuscript/review/detail?reviewId=2037413420613046274`

随后详情接口真实返回：

- `code = 500`
- `msg = 当前用户无权查看该流程`

页面最终显示：

- `详情数据加载失败，请稍后重试。`

这说明：

1. 持证三级审批业务动作本身成功
2. `R7.10` 的适用范围继续扩大到：
   - 持证链路三级审批后

本轮未单独新增同类问题单，仍按 `R7.10` 通用回跳问题处理。

### 2.4 退回后修改保存与再次提交分支已完成闭环复测

本轮新建普通 fresh 样本：

- `reviewId = 2037437986613764098`
- `manuscript_code = SH20260327007`

#### A. 一级退回

浏览器层：

1. `mr_approver_l1` 打开样本详情页
2. 点击 `去审批 -> 审批办理 -> 退回`
3. 驳回节点真实选中：
   - `审校申请`

接口层：

1. `GET /dev-api/workflow/task/getBackTaskNode/2037437987259686914/first-review-node` -> `200`
2. `POST /dev-api/workflow/task/backProcess` -> `200`

MySQL 层：

- `flow_status_label = 已退回`
- `current_node_label = 待发起人处理`
- `current_node_status = RETURN_TO_INITIATOR`

历史新增：

- `BACK`

补充说明：

退回成功后，原一级审批人也被带回无权详情页：

- `GET /dev-api/workflow/manuscript-review/2037437986613764098`
- `code = 500`
- `msg = 当前用户无权查看该流程`

这和 `R7.10` 的“动作成功后错误回跳无权详情页”是同一类承接问题，但动作类型从“审批通过”扩到了“退回”。

#### B. 发起人回到详情页

使用 `mr_initiator` 打开详情页后，真实看到：

1. 状态：
   - `已退回 / 待发起人处理`
2. 动作栏：
   - `修改 / 再次提交 / 返回`

说明退回后的详情页权限分流是正确的。

#### C. 新问题：修改页未回填既有数据

发起人点击 `修改` 进入：

- `/manuscript/review/form?reviewId=2037437986613764098&mode=edit`

后端详情接口：

- `GET /dev-api/workflow/manuscript-review/2037437986613764098`
- `code = 200`
- 返回完整 data

但浏览器 edit 表单为空，未回填已有数据。

该问题已单独落单：

- `doc/manuscript_review/R7.12_正式测试问题记录-退回后修改页未回填既有数据.md`

#### D. 修改保存

在 edit 页手工补填后执行保存，验证“保存不推 BPM”：

接口层：

- `PUT /dev-api/workflow/manuscript-review` -> `200`

MySQL 层：

1. 主表仍保持：
   - `flow_status_label = 已退回`
   - `current_node_label = 待发起人处理`
   - `current_node_status = RETURN_TO_INITIATOR`
2. 历史新增：
   - `UPDATE`

结论：

- 即使存在 edit 页未回填缺陷，保存动作本身仍真实成立，且没有推进 BPM。

#### E. 再次提交

从详情页点击 `再次提交` 后：

接口层：

- `POST /dev-api/workflow/manuscript-review/resubmit` -> `200`

MySQL 层：

1. 主表恢复到：
   - `flow_status_label = 审批中`
   - `current_node_label = 待一级审批`
   - `current_node_status = LEVEL_1`
2. 历史新增：
   - `RESUBMIT`
3. 待办重新生成：
   - `first-review-node`
   - `taskId = 2037441915628036097`

结论：

- “退回 -> 修改保存 -> 再次提交”后半链路已真实走通。

## 3. 本轮新增问题与风险

### 3.1 已新增问题单

本轮新增问题文件：

- `doc/manuscript_review/R7.12_正式测试问题记录-退回后修改页未回填既有数据.md`

### 3.2 当前风险

1. `R7.10` 不再只限“审批通过后”，其同类回跳问题已经扩到“退回后”
2. 退回分支虽然整体链路可继续跑，但 edit 页未回填会污染“修改保存”体验判断
3. `审批中修改后再去审批`
4. `发起人撤销`
5. `三级审批驳回终止`
6. 附件 / 视频 / 视频标注 / 资源停用 / 下载 / 待办已办 / 菜单权限仍未展开

## 4. 当前结论

当前可以明确说：

1. 持证主链 `2037413420613046274` 已真实闭环
2. `R7.10` 已进一步覆盖到持证 L3 审批后
3. 分支“退回后修改保存与再次提交”已拿到真实闭环证据
4. 本轮新增的新类型问题不是审批动作失败，而是：
   - 退回后 edit 页未回填既有数据

## 5. 下一步建议

后续应直接继续：

1. 使用新的 clean fresh 样本跑：
   - 审批中修改后再去审批
2. 再用新的 clean fresh 样本跑：
   - 发起人撤销
3. 再用新的 clean fresh 样本跑：
   - 三级审批驳回终止
4. 分支稳定后，再展开：
   - 附件 / 视频 / 视频标注
   - 资源停用 / 预览 / 下载
   - 我的待办 / 我的已办 / 菜单权限 / 按钮跳转
