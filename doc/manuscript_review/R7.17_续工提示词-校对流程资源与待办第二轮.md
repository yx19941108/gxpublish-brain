Owner: Codex
Task: manuscript_review 续工提示词-校对流程资源与待办第二轮
Status: Ready
Updated: 2026-03-27 18:05:00 +08:00

# R7.17_续工提示词-校对流程资源与待办第二轮

## 1. 新会话启动要求

- 只从以下文件恢复上下文，不回旧聊天：
  1. `doc/manuscript_review/R7.15_正式测试执行计划-R7.4剩余范围补测.md`
  2. `doc/manuscript_review/R7.16_正式测试执行记录-校对流程资源与待办首轮.md`
- 当前分支：`manuscript-review-tdd-20260321`
- 当前任务仍是 `R7.4` 剩余正式用例补测，不回头重做 `R7.10 / R7.12` 已闭环内容。

## 2. 已确认事实

### 2.1 校对流程新样本

1. 校对流程资源主样本：
   - `reviewId = 2037465008115294210`
   - `manuscriptCode = JD20260327001`
   - 当前状态：`审批中 / 待一级审批`
   - 当前有效资源：`pdf + docx + 外链`
2. 校对流程仅附件分支样本：
   - `reviewId = 2037466003721428994`
   - `manuscriptCode = JD20260327002`
   - 当前状态：`审批中 / 待一级审批`
   - 当前有效资源：`仅 pdf`

### 2.2 已确认问题候选

1. `meeting_01.mp4` 上传失败：
   - `POST /dev-api/resource/oss/upload`
   - 真实现象：`net::ERR_CONNECTION_RESET`
   - 对照事实：同页 `pdf` 和 `docx` 上传都成功
2. 详情页资源区未出现资源级操作按钮：
   - 未见 `停用`
   - 未见 `预览`
   - 未见资源级 `下载`
3. 审批人 `我的已办 -> 查看` 落到 `approval` 页，不是 `detail` 页。
4. 发起人 `我发起的 -> 查看` 同样落到 `approval` 页，不是 `detail` 页。

### 2.3 菜单语义

1. `mr_approver_l1`
   - 有：`我的待办` `/task/taskWaiting`
   - 有：`我的已办` `/task/taskFinish`
   - 无：`我的抄送`
2. `mr_initiator`
   - 有：`我发起的` `/task/myDocument`
   - 无：`我的待办 / 我的已办`
3. `mr_certified`
   - 当前菜单语义与发起人侧一致：`我发起的 + 审校台账`
4. `mr_certified_initiator`
   - 当前菜单语义与 `mr_certified` 一致

## 3. 测试文件

直接使用仓库内文件，不要再自行生成：

1. `doc/manuscript_review/test_file/2b16ea3347bb4138af5a26e4233b9cae.pdf`
2. `doc/manuscript_review/test_file/meeting_01.mp4`
3. `doc/manuscript_review/test_file/网络学院观看线上课程与下载证书操作手册.docx`

## 4. 下一轮优先顺序

### Priority 1

- 深挖 `meeting_01.mp4` 上传失败根因
  1. 继续补网络证据
  2. 看后端/网关/对象存储是否有对应错误
  3. 不改业务实现，先拿全证据

### Priority 2

- 补发起人退回态按钮口径
  1. 找一个 `RETURN_TO_INITIATOR` 的真实样本
  2. 验证 `我发起的` 列表里是否出现 `编辑`
  3. 验证 `查看 / 撤销 / 编辑` 跳转是否符合 `R7.4`

### Priority 3

- 继续 `mr_approver_l1` 的待办/已办闭环
  1. `我的待办 -> 办理` 是否稳定落到 `approval`
  2. `我的已办 -> 查看` 是否持续错误落到 `approval`
  3. 必要时补截图和路由证据

### Priority 4

- 视剩余上下文，再决定是否补：
  1. `mr_certified / mr_certified_initiator` 的待办归属运行态
  2. 视频标注链路

## 5. 执行纪律

1. 继续只做验证，不顺手修复。
2. 先核环境 `9999 / 9888`。
3. 如果准备改代码，先给：
   - `Goal / Affected Files / Risk / Verification / Rollback`
4. 浏览器只保留一个工作页顺序切账号，不要再开一排隔离页。
5. 新结论继续写入 `doc/manuscript_review/`，不要把关键证据只留在聊天里。

## 6. 推荐开场提示词

```text
在 C:\kuguaHome\project\gxpublish-brain 继续 manuscript_review。

只读这两个文件恢复上下文，不回旧聊天：
1. doc/manuscript_review/R7.15_正式测试执行计划-R7.4剩余范围补测.md
2. doc/manuscript_review/R7.16_正式测试执行记录-校对流程资源与待办首轮.md

当前分支是 manuscript-review-tdd-20260321。
这轮只做 R7.4 剩余正式用例补测，不重做 R7.10 / R7.12。

优先顺序：
1. 深挖 meeting_01.mp4 上传 reset 根因
2. 补发起人退回态编辑按钮口径
3. 继续审批人我的待办 / 我的已办闭环

继续使用 doc/manuscript_review/test_file 下现成文件，不要再生成临时测试文件。
先核 9999/9888，再开始。
若要改代码，先给 Goal / Affected Files / Risk / Verification / Rollback。
浏览器只保留一个工作页顺序切账号。
新结论继续写入 doc/manuscript_review/。
```
