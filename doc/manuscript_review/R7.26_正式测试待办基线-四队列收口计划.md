Owner: Codex
Task: manuscript_review 正式测试待办四队列基线
Status: Baseline
Updated: 2026-03-29 00:28:00 +08:00

# R7.26_正式测试待办基线-四队列收口计划

## 1. 本文目的

- 基于 `R7.4` 正式测试池，固定当前“已通过 / 已做待盖章 / 未闭环”口径。
- 将剩余正式测试待办按四个队列分组，供后续会话直接承接。
- 本文只做待办基线，不重写历史执行细节，不替代各轮正式执行记录。

## 2. 统计口径

### 2.1 总测试池

以：

- `doc/manuscript_review/R7.4_正式测试方案-全需求覆盖.md`

为正式测试总池，当前纳入统计的测试项为：

1. 主用例：
   - `用例-01` 至 `用例-60`
   - 共 `60` 条
2. 分支用例：
   - `用例-12A`
   - 共 `1` 条

合计：

- `61` 个正式测试项

### 2.2 当前保守结论

按已落盘运行证据与最终回归证据，当前保守口径为：

1. 已闭环通过：
   - `17 / 61`
2. 已做过但未最终盖章：
   - `5 / 61`
3. 还没展开或未闭环：
   - `39 / 61`

说明：

1. 本文故意采用保守口径。
2. “代码已修复”或“定向测试已通过”不自动等价于“正式测试用例最终通过”。
3. 只有拿到独立运行态闭环证据的项，才计入“已闭环通过”。

## 3. 已闭环通过项

当前已确认闭环的测试项如下：

1. `用例-11`
2. `用例-12A`
3. `用例-13`
4. `用例-14`
5. `用例-15`
6. `用例-16`
7. `用例-17`
8. `用例-18`
9. `用例-19`
10. `用例-27`
11. `用例-28`
12. `用例-29`
13. `用例-30`
14. `用例-38`
15. `用例-40`
16. `用例-53`
17. `用例-54`

主要依据：

1. `R7.11`：普通主链闭环
2. `R7.13`：持证主链闭环、退回后修改保存与再次提交
3. `R7.23`：`用例-53 / 54` 浏览器最终回归
4. `R7.25`：多视频切换与标注跳时浏览器最终回归

## 4. 已做待盖章项

以下测试项已经展开、已拿到部分运行态证据或代码修复结论，但尚未单独形成“最终通过”落盘结论：

1. `用例-51`
2. `用例-52`
3. `用例-56`
4. `用例-57`
5. `用例-59`

当前口径：

1. 这些项不应再按“从零开始”重复探索。
2. 下一轮优先目标应是补齐浏览器最终回归或最小闭环证据。

## 5. 四队列待办

### 5.1 第一队列：已做待盖章，优先补最终闭环

目标：

- 以最小成本把“已做过但未最终盖章”的项收口。

测试项：

1. `用例-51`
2. `用例-52`
3. `用例-56`
4. `用例-57`
5. `用例-59`

当前判断：

1. 这组最适合优先继续。
2. 原因不是它们最简单，而是前序证据最多、收口性最好。

### 5.2 第二队列：权限、流程动作、治理配置

目标：

- 补齐最影响业务流转正确性的权限与流程动作边界。

测试项：

1. `用例-37`
2. `用例-39`
3. `用例-41`
4. `用例-42`
5. `用例-43`
6. `用例-44`
7. `用例-45`
8. `用例-46`
9. `用例-47`
10. `用例-48`
11. `用例-58`

### 5.3 第三队列：页面承载、入口、基础提交流程

目标：

- 补齐正式测试池中页面入口、页面职责、中文语义、基础提交流程的全量盖章。

测试项：

1. `用例-01`
2. `用例-02`
3. `用例-03`
4. `用例-04`
5. `用例-05`
6. `用例-06`
7. `用例-07`
8. `用例-08`
9. `用例-09`
10. `用例-10`
11. `用例-12`

### 5.4 第四队列：资源、负向校验、修改留痕与追加语义

目标：

- 成组补齐资源生命周期、负向拦截、修改历史、追加语义等剩余大面。

测试项：

1. `用例-20`
2. `用例-21`
3. `用例-22`
4. `用例-23`
5. `用例-24`
6. `用例-25`
7. `用例-26`
8. `用例-31`
9. `用例-32`
10. `用例-33`
11. `用例-34`
12. `用例-35`
13. `用例-36`
14. `用例-49`
15. `用例-50`
16. `用例-55`
17. `用例-60`

## 6. 下一轮建议边界

下一轮只建议承接：

- 第一队列

即：

1. `用例-51`
2. `用例-52`
3. `用例-56`
4. `用例-57`
5. `用例-59`

不建议下一轮同时展开：

1. 第二队列
2. 第三队列
3. 第四队列
4. 已闭环通过项的回头重做

## 7. 下一轮执行目标

第一队列下一轮的目标不是重新定位问题，而是：

1. 核对当前 `9888 / 9999` 是否仍承载最新代码
2. 用真实浏览器补齐 `用例-51 / 52 / 56 / 57 / 59` 的最终运行态结论
3. 若发现仍有单点失败，则只在该队列范围内做最小修复
4. 新结论继续落盘到 `doc/manuscript_review/`，沿用现有命名风格

## 8. 当前使用建议

如果进入新会话，建议优先携带：

1. 本文
2. `R7.21`
3. `R7.23`
4. `R7.25`

原因：

1. 第一队列和 `R7.21 / R7.23` 强相关
2. 当前资源与视频链路的最新真实结论已经以 `R7.25` 为准
3. 这样可以最大限度避免新会话重复统计与重复收敛
