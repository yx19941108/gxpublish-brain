Owner: Codex
Task: manuscript_review WP7-G1 运行态补证与审批承载修复记录
Status: Draft
Updated: 2026-03-27 08:52 +08:00

# R7.1_WP7-G1运行态补证与审批承载修复记录

## 1. 文档定位

- 本文用于固化 `R7.0` 之后新增获得的 fresh 运行态证据，以及本轮为 `WP7-G1` 补上的最小前端修复。
- 本文不回写 `R6.*` 与 `R7.0`，只承接它们继续往前推进。
- 本轮仍以以下材料为业务裁决源：
  - `R6.28_前后端执行基线-新增提交一体化.md`
  - `R6.27_测试方案及结论-新增提交一体化.md`
  - `R6.26_冻结契约包-新增提交一体化.md`
  - `R7.0_WP7-G1前端修复执行记录.md`

## 2. 本轮目标

本轮目标不是重开方案，也不是扩写新的实现蓝图，而是继续把 `WP7-G1` 从“静态已绿、浏览器证据不足”推进到“关键页面真实可用、关键动作真实可用”，重点覆盖：

1. 补齐登录后的真实页面证据
2. 证明台账页“审批中”查询真实可用
3. 证明详情页真实渲染已符合冻结口径
4. 证明新增页 / 修改页真实页面入口可用
5. 证明“去审批”不再 404
6. 证明“再次提交”真实可用并且状态回到审批中

## 3. 已确认完成项

### 3.1 登录与台账页 fresh 证据

- 使用账号 `mr_initiator / 666666` 在默认租户 `000000` 登录成功。
- 登录后真实落到 `/index`，说明前端登录链可用。
- 真实打开 `/manuscript/review` 后，页面查出 2 条记录：
  - `SH20260326003 / 审批中 / 待一级审批`
  - `SH20260326001 / 审批中 / 待一级审批`
- 这说明前端列表页已真实消费后端状态值口径，`WAITING` 对应的“审批中”不再出现“查不出来”的旧问题。

### 3.2 详情页 / 新增页 / 修改页 fresh 证据

- 真实打开 `reviewId=2037085206922715137` 的详情页成功。
- 详情页真实渲染结果与本轮冻结口径一致：
  - 不再暴露 `permissionMatrix / 动作矩阵 / 当前节点说明 / 按钮分流板`
  - 外链真实渲染为可点击 `<a>` 链接
  - 统一时间线真实可读
- 真实打开新增页 `/manuscript/review/form?mode=create` 成功：
  - 主动作仍为 `提交`
  - 无 `保存草稿`
  - `报送部门` 为只读展示
- 真实打开修改页 `/manuscript/review/form?mode=edit&reviewId=2037085206922715137` 成功：
  - 主动作仍为 `保存`
  - 已入库资源区存在独立区块

### 3.3 去审批 404 根因与修复

- fresh 浏览器复现表明：
  - `mr_approver_l1` 在详情页看到动作 `修改 / 去审批 / 返回`
  - 点击“去审批”后，前端真实跳到
    - `/manuscript/review/approval?id=2037085206922715137&type=approval&taskId=2037085714328641538`
  - 在修复前，该路径前端没有承载页，因此真实落到 `404`
- 根因已确认：
  - 后端 / 冻结口径给出的 `formPath` 是 `/manuscript/review/approval`
  - 前端此前没有注册该路由
- 本轮最小修复已落地：
  - 新增前端路由 `review/approval`
  - 复用 `detail.vue` 作为审批承载页
  - 在审批页模式下，动作栏收口为 `审批办理 / 返回`
  - 点击 `审批办理` 时，使用路由 query 中的 `taskId` 拉起 BPM `submitVerify` 办理弹窗

### 3.4 去审批修复后的 fresh 浏览器证据

- `mr_approver_l1 / 666666` 真实登录成功。
- 详情页真实显示动作：
  - `修改`
  - `去审批`
  - `返回`
- 点击“去审批”后，真实进入：
  - `/manuscript/review/approval?id=2037085206922715137&type=approval&taskId=2037085714328641538`
- 审批承载页真实渲染：
  - 标题 `MANUSCRIPT REVIEW / APPROVAL`
  - 动作 `审批办理 / 返回`
- 点击“审批办理”后，真实拉起 BPM 办理弹窗，弹窗正文可见：
  - `审批意见`
  - `提交`
  - `转办`
  - `终止`
  - `退回`
  - `取消`

### 3.5 再次提交 fresh 运行态闭环

- 选用样本：
  - `reviewId=2037085206922715137`
  - 一级待办 `taskId=2037085714328641538`
- 本轮通过真实 workflow 接口将该样本退回给发起人后，再用浏览器验证发起人行为：
  - `mr_initiator` 真实看到动作变为 `修改 / 再次提交 / 返回`
  - 页面真实状态变为 `已退回 / 待发起人处理`
- 点击“再次提交”后，页面真实恢复为：
  - `审批中 / 待一级审批`
- MySQL 回看同一条记录，已确认状态恢复：
  - `flow_status_label=审批中`
  - `current_node_status=LEVEL_1`
  - `current_node_label=待一级审批`

## 4. 本轮实际修改文件

### 4.1 正式代码文件

1. `plus-ui-ts/src/router/index.ts`
2. `plus-ui-ts/src/views/manuscript-review/detail.vue`
3. `plus-ui-ts/src/views/manuscript-review/approval-route.spec.ts`

### 4.2 本轮继续沿用但未新增改动的既有 WP7-G1 文件

1. `plus-ui-ts/src/views/manuscript-review/index.vue`
2. `plus-ui-ts/src/views/manuscript-review/form.vue`
3. `plus-ui-ts/src/views/manuscript-review/components/formState.ts`
4. `plus-ui-ts/src/views/manuscript-review/detail.contract.ts`
5. `plus-ui-ts/src/views/manuscript-review/form-state.spec.ts`
6. `plus-ui-ts/src/views/manuscript-review/form-layout.spec.ts`
7. `plus-ui-ts/src/views/manuscript-review/ledger-layout.spec.ts`
8. `plus-ui-ts/src/views/manuscript-review/detail-layout.spec.ts`
9. `plus-ui-ts/src/views/manuscript-review/detail-readable-display.spec.ts`

### 4.3 临时验证产物

以下均为 `.agent-temp/` 临时验证产物，不进入正式版本主线：

1. `.agent-temp/wp7_g1_browser_probe.py`
2. `.agent-temp/wp7_g1_approver_probe.py`
3. `.agent-temp/wp7_g1_resubmit_probe.py`
4. `.agent-temp/wp7_g1_browser/**`
5. `.agent-temp/wp7_g1_approver/**`
6. `.agent-temp/wp7_g1_resubmit/**`

## 5. 验证结果

### 5.1 静态定向验证

- 既有 WP7-G1 页面定向回归：
  - `pnpm exec vitest run src/views/manuscript-review/form-state.spec.ts src/views/manuscript-review/form-layout.spec.ts src/views/manuscript-review/ledger-layout.spec.ts src/views/manuscript-review/detail-layout.spec.ts src/views/manuscript-review/detail-readable-display.spec.ts`
  - 结果：`Test Files 5 passed (5)`、`Tests 24 passed (24)`
- 本轮新增审批承载页定向回归：
  - `pnpm exec vitest run src/views/manuscript-review/approval-route.spec.ts`
  - 结果：`Test Files 1 passed (1)`、`Tests 1 passed (1)`
- 本轮收口后的联合定向回归：
  - `pnpm exec vitest run src/views/manuscript-review/approval-route.spec.ts src/views/manuscript-review/detail-layout.spec.ts src/views/manuscript-review/detail-readable-display.spec.ts`
  - 结果：`Test Files 3 passed (3)`、`Tests 10 passed (10)`

### 5.2 浏览器级 fresh 证据

- `mr_initiator`：
  - 登录成功
  - 台账页成功
  - 详情页成功
  - 新增页成功
  - 修改页成功
  - “再次提交”成功
- `mr_approver_l1`：
  - 登录成功
  - 详情页成功
  - “去审批”成功跳入审批承载页
  - “审批办理”成功拉起 BPM 办理弹窗

### 5.3 MySQL fresh 证据

- 当前样本 `2037085206922715137` 最终状态已恢复为：
  - `审批中`
  - `LEVEL_1`
  - `待一级审批`

## 6. 当前结论

### 6.1 本轮可确认结论

- `R7.0` 中“还缺登录后真实页面证据”的主 blocker 已被清掉。
- `WP7-G1` 的前端最小页面闭环现在已拿到以下真实证据：
  - 登录
  - 台账
  - 详情
  - 新增
  - 修改
  - 去审批
  - 再次提交
- “去审批”此前的真实 404 缺口已在前端最小范围内修复。

### 6.2 仍不能直接写成“整包最终通过”的原因

当前还不能把 `WP7-G1` 写成“完全收口结束”，原因不是主路径还坏，而是还有剩余验证面尚未补完：

1. 还没有继续拿 `mr_approver_l2 / mr_approver_l3` 的浏览器级审批链路证据
2. 还没有继续补 `mr_certified / mr_certified_initiator` 的跳级场景浏览器证据
3. 本轮审批承载页是“最小承载修复”，不是重新设计一张独立审批业务页

## 7. 未闭环项

1. `mr_approver_l2` 的“去审批 -> 审批办理”浏览器级链路
2. `mr_approver_l3` 的“去审批 -> 审批办理”浏览器级链路
3. `mr_certified / mr_certified_initiator` 的跳级场景页面级链路
4. 是否需要把当前审批承载页继续收口成更明确的 manuscript 专属审批视觉壳，而不是复用 detail 承载

## 8. 下个会话建议起点

下个会话建议直接从以下顺序开始，不要回退到旧实现讨论：

1. 先读：
   - `doc/manuscript_review/R7.1_WP7-G1运行态补证与审批承载修复记录.md`
   - `doc/manuscript_review/R7.0_WP7-G1前端修复执行记录.md`
   - `doc/manuscript_review/R6.28_前后端执行基线-新增提交一体化.md`
2. 先补浏览器级证据：
   - `mr_approver_l2`
   - `mr_approver_l3`
   - `mr_certified / mr_certified_initiator`
3. 再决定：
   - 是否将当前审批承载页视为 `WP7-G1` 可接受的最小通过面
   - 是否需要为后续阶段再做审批页视觉 / 信息结构收口

## 9. 本轮对外口径

当前允许上报：

- `WP7-G1 已拿到登录后真实页面证据`
- `审批中列表查询已在真实页面确认可用`
- `去审批 404 缺口已修复，并已拿到真实审批承载页证据`
- `再次提交已拿到真实闭环证据`

当前不建议上报：

- `WP7-G1 已整包最终通过`
- `所有角色审批链路已全部验证完成`
- `跳级场景页面级证据已全部补齐`
