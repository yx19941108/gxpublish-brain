Owner: Codex
Task: manuscript_review R8 第三轮启用能力扩展轮新会话提示词
Status: Ready
Updated: 2026-03-31 06:49:30 +08:00

# R8.6.0_第三轮启用能力扩展轮新会话提示词

```text
你现在接手的是 `gxpublish-brain` 的 `manuscript_review` R8 第三轮启用能力扩展轮，直接承接已冻结材料，不要重新发明范围，不要回退到第一轮/第二轮，不要顺手重开问题 2，只基于已通过与已落盘材料继续。

需要一定遵守一个约束：资源的相关操作按钮，和详情中的修改按钮的操作权限一致，后端接口也要校验这个约束。这个限制包括：启用/停用资源（附件、视频、外链），增加/停用/启用视频标注等。
当发现上下文过长以至于你有遗忘风险时，停止并落盘工件到 `doc/manuscript_review/`，以 `R8.6.x_` 为前缀命名。

先读这些文件，不要跳过：
1. `doc/manuscript_review/R8.0_问题排查续工交接.md`
2. `doc/manuscript_review/R8.2_问题池统一修复方案.md`
3. `doc/manuscript_review/R8.3_问题池分轮修复执行基线.md`
4. `doc/manuscript_review/R8.2.5_问题池统一回归测试用例.md`
5. `doc/manuscript_review/R8.4.5_第一轮运行态回归记录.md`
6. `doc/manuscript_review/R8.5.3_第二轮预览鉴权稳方案续工交接.md`
7. `doc/manuscript_review/R8.5.4_第二轮预览鉴权稳方案运行态回归记录.md`
8. `doc/manuscript_review/R8.6.0_第三轮启用能力扩展轮新会话提示词.md`

执行裁决规则：
1. 分轮与实现边界以 `R8.3` 为准
2. 问题级修复目标与游工意见以 `R8.2` 为准
3. 修复后回归与最终验收以 `R8.2.5` 为准
4. 第一轮 gate 与运行态基线以 `R8.4.5` 为准
5. 第二轮已闭环与可进入第三轮的结论以 `R8.5.4` 为准

当前必须承接的事实：
1. 第一轮已闭环，不要回退到问题 `9 / 3 / 7`
2. 第二轮已闭环，浏览器 gate 已通过，不要回退到问题 `1 / 8 / 4 / 5`
3. 当前允许进入第三轮
4. 第三轮只做问题 `6`
5. 问题 `2` 继续只补证，不改代码
6. 第二轮稳方案 preview token 已成立，不要改回 Bearer 拼 URL，不要改回 OSS 直链主方案
7. 当前工作树边界外文件仍不要碰：
   - `plus-ui-ts/.eslintrc-auto-import.json`
   - `test_qa_flow.py`

第三轮硬边界如下：
1. 只修问题 `6：前后端都没有 enable 能力`
2. 不重开第一轮、第二轮已收口问题
3. 不把“启用”实现成覆写旧停用记录
4. 不把问题 `2` 扩成代码修复
5. 不顺手扩 scope 到新的治理或预览改造

第三轮目标固定为：
1. 资源 enable
   - 附件
   - 视频
   - 外链
2. 视频标注 enable
3. 启用入口挂在约定位置
   - 停用历史
   - 或资源动作链路的冻结位置
4. 启用后追加新 history
   - 不能覆写旧停用 history
   - 新 history 仍要可直接读出“谁做了什么”
   - `actor_user_id / actor_user_name` 仍必须有值

本轮实现要求：
1. 前端按钮权限与详情页 `canEdit` 一致
2. 后端接口权限与 service 二次校验也要与 `canEdit` 等价
3. 启用后资源重新进入当前有效资源区
4. 启用后视频标注重新进入当前有效标注列表，并恢复跳转能力
5. 时间线中保留旧停用 history，同时新增启用 history
6. 不修改已冻结通过的 preview token 主链路，除非第三轮 enable 实现直接阻塞编译或运行

开始时先给：
1. 已确认
2. 风险
3. 待确认
4. 推进方案

然后给本轮代码变更计划；每一步必须包含：
1. Goal
2. Affected Files
3. Risk
4. Verification
5. Rollback

实现前默认先补最小 red/green，优先锁定以下缺口：
1. enable 相关 controller / service / 前端动作是否不存在
2. timeline / history 是否还缺 enable 动作文案
3. 停用历史与当前有效资源区的回刷是否会冲突

实现后必须严格按 `R8.2.5` 跑第三轮最小回归集：
1. `TC-15`
2. `TC-16`
3. `TC-17`

如第三轮实现改动触及历史写入或详情动作权限，附加至少做以下防回归检查：
4. `TC-18`
5. 对第二轮做最小冒烟：
   - 视频预览仍可 `206`
   - 详情页视频仍可播放

汇报时必须给出：
1. 实际执行的用例编号
2. 每条通过/失败
3. 浏览器证据
4. 接口证据
5. 数据证据
6. 第三轮 gate 是否达到
7. R8 问题池是否全部完成
```
