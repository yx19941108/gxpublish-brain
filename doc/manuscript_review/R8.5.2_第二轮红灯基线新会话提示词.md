Owner: Codex
Task: manuscript_review R8 第二轮红灯基线新会话提示词
Status: Ready
Updated: 2026-03-30 17:28:00 +08:00

# R8.5.2_第二轮红灯基线新会话提示词

```text
你现在接手的是 `gxpublish-brain` 的 `manuscript_review` R8 第二轮修复执行，直接承接 `R8.5.1_第二轮红灯基线续工交接.md` 与 `R8.5_第二轮修复执行提示词.md`，不要重新发明方案，不参考旧失败实现，只基于已冻结与已通过材料承接。

需要一定遵守一个约束：资源的相关操作按钮，当且仅当在canModify为true时才展示，后端接口也要校验这个约束。这个限制包括：启用/停用资源（附件、视频、外链），增加/停用视频标注等。

先读这些文件，不要跳过：
1. `doc/manuscript_review/R8.0_问题排查续工交接.md`
2. `doc/manuscript_review/R8.2_问题池统一修复方案.md`
3. `doc/manuscript_review/R8.3_问题池分轮修复执行基线.md`
4. `doc/manuscript_review/R8.2.5_问题池统一回归测试用例.md`
5. `doc/manuscript_review/R8.4.5_第一轮运行态回归记录.md`
6. `doc/manuscript_review/R8.5_第二轮修复执行提示词.md`
7. `doc/manuscript_review/R8.5.1_第二轮红灯基线续工交接.md`

执行裁决规则：
1. 分轮与实现边界以 `R8.3` 为准
2. 问题级修复目标与游工意见以 `R8.2` 为准
3. 修复后回归与最终验收以 `R8.2.5` 为准
4. 第一轮 gate 结论与当前运行态基线以 `R8.4.5` 为准

本轮只做第二轮，硬边界如下：
1. 只修问题 1、问题 8、问题 4、问题 5
2. 问题 4 继续并入问题 8，不得拆成独立实现架构
3. 不回退到问题 9、问题 3、问题 7
4. 不进入问题 6
5. 问题 2 继续只补证，不改代码
6. 不得顺手扩 scope

已确认的现状直接承接：
1. 第二轮 failing tests 已经补好，并已拿到红灯
2. 前端红灯：
   - `index.ts` 还没有 `deletePendingManuscriptReviewResource`
   - `detail-page-shell.vue` 还没有“新增视频标注”操作区
3. 后端红灯：
   - `deletePendingResource` endpoint 不存在
   - `ManuscriptReviewService.deletePendingResource(Long)` 不存在
   - 详情附件/视频 `resourceUrl` 仍然是外部 URL，不是模块内 `/workflow/manuscript-review/resource/preview/{resourceId}`
4. `Brain/pom.xml` 默认 `<skipTests>true</skipTests>`，跑后端测试必须显式加 `-DskipTests=false`
5. 当前运行态 `9999/9888` 可用，优先继续使用本机 JDK17
6. 当前工作树用户边界外文件：
   - `plus-ui-ts/.eslintrc-auto-import.json`
   - `test_qa_flow.py`
   这两个文件不要碰

本轮执行要求：
1. 不重新补计划，直接从已补红灯开始实现
2. 先收问题 1
3. 再收问题 8 / 4
4. 最后收问题 5
5. 问题 2 仍只补证，不改码
6. 不得进入问题 6

当前红灯验证命令：
前端：
`& 'C:\kuguaHome\tools\devTools\nvm\v20.19.6\pnpm.cmd' exec vitest run src/views/manuscript-review/second-round-contract.spec.ts src/views/manuscript-review/detail-layout.spec.ts src/views/manuscript-review/form-state.spec.ts`

后端：
`$env:JAVA_HOME='C:\kuguaHome\env\java\jdk17'; $env:Path='C:\kuguaHome\env\java\jdk17\bin;' + $env:Path; mvn -pl brain-modules/brain-manuscript-review -am '-DskipTests=false' '-Dtest=ManuscriptReviewApiControllerTest#shouldExposeSecondRoundPendingDeleteAndPreviewEndpoints,ManuscriptReviewSharedApiMvcTest#shouldReturnFrozenDetailVoFromNewWriteSideFieldsForApi02,ManuscriptReviewReadableServiceTest#shouldAssembleReadableDetailWithFrozenFieldsAndCurrentResources,ManuscriptReviewServiceTest#shouldExposePendingResourceDeletionMethodForSecondRoundIssueOne' test`

实现后必须严格按 `R8.2.5` 跑第二轮最小回归集：
1. `TC-01 ~ TC-02`
2. `TC-11 ~ TC-12`
3. `TC-13 ~ TC-14`
4. `TC-22 ~ TC-25`
附加第一轮存量回归：
5. `TC-26`
6. `TC-28`
7. `TC-05`
8. `TC-18`

汇报时必须给出：
1. 实际执行的用例编号
2. 每条通过/失败
3. 浏览器证据
4. 接口证据
5. 数据证据
6. 第二轮 gate 是否达到
7. 是否允许进入第三轮
```
