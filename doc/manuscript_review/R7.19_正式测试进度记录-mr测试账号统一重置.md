Owner: Codex
Task: manuscript_review mr测试账号统一重置
Status: In Progress
Updated: 2026-03-28 11:29:30 +08:00

# R7.19_正式测试进度记录-mr测试账号统一重置

## 1. 变更背景

- 在继续 `manuscript_review` 第二轮补测前，Playwright 浏览器探针已恢复可执行。
- 但使用历史文档口径 `mr_initiator / 666666 / 1234` 登录时，真实接口返回：
  - `POST /dev-api/auth/login -> {"code":500,"msg":"密码输入错误2次","data":null}`
- 继续盲试密码存在扩大账号锁定风险，因此本轮按游工确认，直接对当前运行库中的全部 `mr_*` 测试账号统一重置密码。

## 2. 执行范围

- 数据库：`gxpublish_brain`
- 表：`sys_user`
- 条件：
  - `tenant_id = '000000'`
  - `user_name like 'mr\\_%' escape '\\'`

## 3. 更新前事实

更新前快照保存在：

- `.agent-temp/manuscript_review_r7_18/mr_user_password_snapshot_before_reset.tsv`

更新前共命中 `10` 个账号：

1. `mr_initiator`
2. `mr_approver_l1`
3. `mr_approver_l2`
4. `mr_approver_l3`
5. `mr_certified`
6. `mr_certified_initiator`
7. `mr_approver_l1_b`
8. `mr_approver_l2_b`
9. `mr_approver_l3_b`
10. `mr_observer`

关键异常事实：

1. 前 `5` 个早期 `mr_*` 账号的 `password` 字段当前为**空串**。
2. 后 `5` 个执行矩阵补充账号的 `password` 字段已是同一条 bcrypt 哈希。
3. 当前登录失败与历史文档口径不一致的直接原因，就是前述空密码状态。

## 4. 本轮数据库变更

统一将上述 `10` 个 `mr_*` 测试账号重置为：

- 明文口令：`666666`
- 口令哈希：`$2a$10$b8yUzN0C71sbz.PhNOCgJe.Tu1yWC3RNrTyjSQ8p1W0.aaUXUJ.Ne`

执行结果：

1. 实际更新行数：`10`
2. 更新后回查确认：
   - 上述 `10` 个账号的 `password` 字段已全部统一为同一条 bcrypt 哈希

## 5. 变更后验证

### 5.1 数据层

数据库回查已确认：

1. `mr_initiator`
2. `mr_approver_l1`
3. `mr_approver_l2`
4. `mr_approver_l3`
5. `mr_certified`
6. `mr_certified_initiator`
7. `mr_approver_l1_b`
8. `mr_approver_l2_b`
9. `mr_approver_l3_b`
10. `mr_observer`

以上账号均已写成统一哈希。

### 5.2 浏览器层

使用 Playwright 最小登录探针验证：

1. 账号：`mr_initiator`
2. 密码：`666666`
3. 验证码：`1234`
4. 真实结果：
   - `POST /dev-api/auth/login -> 200`
   - `GET /dev-api/system/user/getInfo -> 200`
   - 页面真实跳转到 `/index`

结论：

- 本轮 `mr_*` 测试账号统一重置已生效。
- 浏览器补测所需的基础登录执行面已恢复。

## 6. 对后续补测的影响

1. 后续正式补测统一使用：
   - `mr_*` 账号密码：`666666`
2. 本轮不继续修改其他非 `mr_*` 账号口令。
3. 若后续需要回滚，只能基于本轮更新前快照恢复原始哈希，不能从历史文档直接推回。
